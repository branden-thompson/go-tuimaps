# DISCOVER — HUM LEAD Rulings

Rulings on the open questions carried out of Tier 1 ([`tier1-synthesis.md`](tier1-synthesis.md) §5). Presented one at a time at HUM LEAD's direction (2026-09-18: "Present questions requiring my ruling 1 by 1 so I can read and take the time to make good decisions"). Each ruling is recorded verbatim. **Where HUM LEAD chose against the coordinator's recommendation,** the rows from D-61 on say so in the last column; for earlier rulings the mark (◇) is in [the options as presented](rulings-options-as-presented.md), which lists all fourteen. Decision numbers continue from the project brief (D-10). **What each lettered answer stood for is recorded in [`rulings-options-as-presented.md`](rulings-options-as-presented.md).**

| # | Date | Question | Ruling (verbatim) | Recorded implication |
|---|---|---|---|---|
| D-11 | 2026-09-18 | R-1 / OQ-1 — what "parity with TerminalMap" means | "Behavioral Parity approved (don't necessarily need pixel parity)" · on the code-as-baseline confirmation: "Understood" · on the defect ledger as its own review: "Approved" | Parity is behavioural: same features, same controls, same intentional constants, measured against the **code** at TerminalMap `3b96072`. README-only claims (mouse pan, fit-to-terminal opening zoom) are not parity rows; either may return as a deliberate extension. The 17 upstream defects go in a closed ledger, each marked *fix* or *replicate*, reviewed by HUM LEAD before the parity matrix is frozen. Anything outside the ledger matches upstream. Byte-for-byte frame equality with upstream is not required. RS-1 moves to mitigated once the matrix is frozen. |
| D-12 | 2026-09-18 | R-2 / OQ-16 — layers upstream styles but never draws (waterways, parks, airports, airport labels) | "A" — *draw them, as a flagged extension* | go-tuiMaps draws waterways, parks, airport runways/taxiways and airport labels. Each is recorded in the parity matrix as an **extension beyond upstream**, outside the M3 denominator (which stays upstream's behaviours at `3b96072`). Their position in the draw order, and any thinning by zoom level, is settled from rendered specimens, not prose. |
| D-13 | 2026-09-18 | R-3 / OQ-5 — library, standalone app, headless render | "A - I intend to release this under MIT just like terminalMap, and it will be a dependency for maps in Watchpost (likely imported as a package)" | Three deliverables: a framework-neutral core library; a standalone app matching upstream's controls, **intended for release**; and a one-shot headless render (function call and app flag). All 72 parity rows are in scope. *[Note 2026-09-18: the matrix was later frozen with 68 rows in the M3 denominator, corrected to 70 by D-49 and to 75 by D-61 — see `parity-matrix.md`.]* Headless rendering is a stated requirement: a complete frame with no terminal attached and no TUI framework loaded (M5). **Licence: MIT**, with both upstream notices carried (C-3). Watchpost will consume go-tuiMaps as an imported Go package, so: the module must be fetchable by the Go toolchain (bears on OQ-8, path and visibility); every module in its dependency graph must carry a LICENSE file and pass the host's vulnerability scan (AI-4 §6); and the library's dependency weight lands in Watchpost's binary and licence file (bears on the PLAN dependency decision, X-5). The app imports the library exactly as an outside host would. |
| D-14 | 2026-09-18 | R-4 / OQ-3 + OQ-15 — overlay kinds in v1; radar | "D - if we can get radar data from our APIs then our maps should be able to support that overlay." | **All five input shapes are in v1:** features (points, lines, polygons, circles); scalar grids; vector grids; georeferenced images; tile-image providers. **Radar is a v1 overlay.** *[Withdrawn 2026-09-18 by D-39: the recorder certified HUM LEAD's own condition. HUM LEAD's words: the first host **can add** these public APIs.]* ~~The stated condition is met on the evidence:~~ Evidence that such sources exist: keyless radar is available from NOAA MRMS (WMS), nowCOAST (WMS) and the Iowa Environmental Mesonet (XYZ tiles and WMS), all as pre-coloured images (AI-5 §2). Carried to PLAN *[settled since by D-36, clarified by D-39; whether the table is mandatory was then open; **D-45 made it required**]*: how pre-coloured radar is handled (placed as-is vs. palette-to-intensity), and a build order across the five shapes. Unchanged: every overlay *rendering approach* is still ratified from rendered specimens, never prose (SH-5). RS-2 stays High; RS-5 (generality vs. YAGNI) is escalated to High, since the contract is now at its widest and becomes expensive to change once Watchpost imports it (D-13). |
| D-15 | 2026-09-18 | R-5 / OQ-4 — who fetches overlay data | "A" — *the host fetches; the library draws* | go-tuiMaps has no knowledge of any weather service. Hosts hand it shapes 1–4 as plain values and supply the function for shape 5. Data-provider terms, rate limits and keys stay with whoever makes the request, never with the library. **Basemap tiles:** the library ships a default fetcher (timeout, identifying User-Agent, status checking — none of which upstream has) and accepts a host-supplied replacement, so a host's tile traffic can run through its own client. Ready-made source packages are not in v1; they remain possible later as a separate companion module. Worked examples of fetching from keyless sources belong in the standalone app and the documentation, outside the library's contract. T-C is satisfied by construction. RS-13 (overlay source terms) is closed for the library. |
| D-16 | 2026-09-18 | R-6 / OQ-14 — who simplifies and clips large shapes | "A" — *the library does both; no host bypass in v1* | Hosts pass feature overlays at full detail in latitude/longitude. The library simplifies to the current dot tolerance and clips to the view. Requirements carried to PLAN: the simplified result is **cached per zoom level** until the host replaces the data; that cache counts **inside the M4 memory ceiling**; the work stays **off the drawing path**, so a large shape appears a moment late rather than stalling a frame; and polygon clipping is added (upstream clips lines only, with a known defect, and does not clip fills). An "already simplified, draw as-is" flag is left out of v1; it can be added later without breaking any host. Sizing evidence: zone outlines 914–14,001 vertices, wildfire perimeters up to 47,068, up to 58 zones on one alert (AI-5 §1, re-measured). |
| D-17 | 2026-09-18 | OQ-17 — pointer operations in the library (pan by cells; zoom around a chosen point) | "A; but keyboard controls for non-mouse machines must also support the functionality 1:1" | The library gains "pan by cells" and "zoom around this cell"; the standalone app wires them to drag-to-pan and zoom-toward-the-pointer. Both are flagged extensions in the parity matrix (as D-12). **Input parity rule (new requirement):** no capability may be reachable by pointer only — every pointer operation has a keyboard equivalent that does the same thing. Pan already has one. Zoom-around-a-point does not in upstream, so the app needs a keyboard way to choose the point: a movable reticle, or stepping focus between markers and overlay features, then zooming around the focused target. The mechanism is a PLAN decision, ratified from a live look. Because the library never reads input, the rule binds the app's bindings and the library's method set: anything a pointer host can ask for, a keyboard host can ask for through the same methods. This is also the accessibility baseline for the red-team a11y persona. |
| D-18 | 2026-09-18 | OQ-18 — local map files as a tile source in v1 | "C - maps need to "just work" - the only data I should really care about being fresh is the weather data." | **A dependency-free PMTiles reader is in v1**, as a flagged extension beyond upstream. Two principles are recorded from the rationale. **(1) Basemap resilience (new requirement):** the map must keep working when the network or the tile service does not; the tile source is a replaceable part with at least network, embedded, disk-cache and local-file implementations. **(2) Basemap freshness is not a goal:** the disk cache may be long-lived by design (upstream's no-expiry behaviour, P-48, is kept deliberately rather than treated as a defect); freshness effort is spent on overlays only. RS-6 moves toward mitigated. **Open, carried to Tier 2 research (AI-9) — not yet verified:** a PMTiles file can hold any tile schema. The statement made when this question was presented, that PMTiles basemaps do not match the OpenMapTiles styles, is true of the Protomaps daily builds but was not checked for files converted from OpenFreeMap's weekly MBTiles download, which would keep the OpenMapTiles schema. AI-9 must establish: the PMTiles v3 read path and its real size in code; whether OpenMapTiles-schema PMTiles can be produced or obtained, and at what size for a region and for the planet; reading over HTTP range requests from a static host as well as from disk; and what "just work" then requires of the embedded tiles (OQ-11) and of first-run behaviour. Whether schema mapping for Protomaps-schema files is also v1 is decided after AI-9, from evidence. *[Decided 2026-09-18 by D-46: OpenMapTiles only in v1; the schema stays a seam.]* |
| D-19 | 2026-09-18 | OQ-8 — module path; when the repository becomes public | "A/1" — *path `github.com/branden-thompson/go-tuimaps`; local only for now, visibility decided at SHIP* | **Module path: `github.com/branden-thompson/go-tuimaps`**, all lowercase; the package is `tuimaps`; the display name stays go-tuiMaps. When a GitHub repository is eventually created it is named in lowercase to match. **No remote is created and nothing is pushed**; visibility is a SHIP decision. Consequences accepted with this ruling: there is no off-machine copy of the work until then; and Watchpost can consume the library only through a local path override on this machine, which its hosted CI cannot follow, so the first Watchpost commit that imports go-tuiMaps waits on the visibility decision. `go mod init` proceeds under this ruling to unblock the P10 gate. *Executed: `go.mod` committed with the Go directive set to 1.25.0 to match the first host's toolchain floor (T-B); a library declaring a newer Go than its host would force the host to upgrade.* |
| D-20 | 2026-09-18 | When the P10 code-quality gate turns on (raised by D-19: the gate cannot certify a module with no Go code) | "A" — *remove the `languages:` declaration now; restore it at BUILD entry* | The `languages: [go]` declaration is removed from the project config until BUILD entry, matching the first host's own sequence (it activated the declaration and ran its first P10 check as a BUILD-entry task, and passed DISCOVER and PLAN exits with the gate not yet declared). the framework's structure check returns to 100% (18/18, two checks skipped with declaration). **Carried to PLAN as a BUILD-entry checklist item:** restore `languages: [go]`, run the first the framework's code-quality check, confirm validate is 100% with the gate on. **Process note:** the declaration was introduced two phases early by the G-5 proposal, which copied the host's present-day config without checking when that line had been added. Rejected alternatives: an empty package file to make the gate pass (a check that passes with nothing to check), and carrying a failing validate across a phase exit. |
| D-21 | 2026-09-18 | OQ-2 — the default basemap source ("OpenMaps" in the brief) | "A" — *confirm* | "OpenMaps" means OpenStreetMap data served as vector tiles by **OpenFreeMap**: the library's default *network* source, beside the embedded tiles, long-lived cache and local files of D-18. OpenMapTiles schema, zoom 0–14, stretched to 18 as upstream does. Any host or user can replace it by URL. **The library never bulk-downloads from the public server** (its terms forbid automated collection); any pre-download feature targets a user's own server or a local file only. Carried to PLAN: a task to track the provider's stated schema migration so a change does not break every shipped copy at once. With D-18 in place the provider is a convenience default rather than a dependency. R-3's wording is sharpened accordingly. *[See D-65: the library connects to nothing until a host names a source; OpenFreeMap is the default of the app and of the examples.]* |
| D-22 | 2026-09-18 | OQ-10 — pure Go, no C toolchain | "A" — *hard requirement* | **The core library, the standalone app and every dependency of either build with CGO disabled.** Enforced mechanically, not by memory: the build includes a cross-compile with CGO off for macOS (arm64, amd64), Linux (amd64, arm64) and Windows (amd64), the first host's release matrix. No labelled-exception tier for add-on packages in v1; relaxing later is a deliberate step. Nothing in scope is affected: radar images (D-14) arrive as PNG, which the standard library decodes without C; the PMTiles reader (D-18) is dependency-free. Added as a technical constraint. |
| D-23 | 2026-09-18 | OQ-6 — platform and terminal floor; which renderers are first-class | "A" — *match the first host's floor, with two first-class renderers, legible at all four colour depths* | **Platforms:** macOS, Linux, Windows. **Terminal:** UTF-8; usable from a 69×12-cell rectangle upward (the first host's smallest map window at its 80×24 minimum); works over SSH, with nothing that depends on querying the terminal. **Renderers:** braille **and** block are both first-class, each with its own reference-frame tests and approved specimens; the block renderer is built properly rather than ported (upstream's is defective, ledger #1). **Colour:** legible in truecolor, 256-colour, 16-colour and no colour (`NO_COLOR` honoured). No 7-bit ASCII renderer in v1. **Open, decided from specimens (Tier 2):** a field drawn as cell background cannot exist without colour, so the no-colour case for shapes 2–5 needs either a glyph-based fallback (shade characters or contours) or a stated limit that field overlays need colour while feature overlays do not. A no-colour field specimen is added to the Tier 2 set. The narrower alternative offered (16-colour and no-colour as "doesn't break, may be plain") was not taken. *[See D-42: braille is the default; the block renderer is opt-in.]* |
| D-24 | 2026-09-18 | OQ-12 — where the map styles come from | "A" — *write fresh styles directly against OpenMapTiles* | go-tuiMaps ships its own `dark` and `bright` styles, authored for this project in the OpenMapTiles vocabulary, MIT, with nothing inherited. **Upstream's style files are not copied into the repository.** The layer-renaming shim (upstream `tile.rs:71-162`, P-40) is not ported; parity row P-40 is recorded as *superseded by design* with this ruling as the reason. Upstream's colours may serve as a visual reference. The styles are tuned for terminal legibility and for sitting under weather overlays, include the D-12 layers, and are ratified from rendered specimens at all four colour depths and in both renderers (D-23). The style *format* (Mapbox GL-style JSON with filters and zoom rules) remains a parity behaviour, so a user can still supply their own style. Resolves X-4 and the style part of RS-10. The "keep now, restyle later" sequence offered was not taken. |
| D-25 | 2026-09-18 | OQ-13 — how map attribution is shown | "A" — *the library reports credits and offers an optional on-map line, on by default; the host decides placement* | **New requirement beyond upstream, which shows no attribution.** The library exposes the credits currently in force, assembled from the basemap source plus the credit text each overlay declares when the host hands it in (D-15), so the list always matches what is on screen. The library can draw a one-line credit in a corner of the map, **on by default**, so a host that does nothing still ships a credited map; a host that displays the credit elsewhere turns it off. Shortest basemap line: `© OpenMapTiles © OpenStreetMap` (30 characters). The standalone app shows the line and has an About screen with the full text and licence links. Carried to specimens: behaviour on a 12-row map, including fading after a few seconds or on first pan as the OpenStreetMap guidelines allow. Reported as documented by the licensors; not legal advice. Resolves the attribution part of RS-10. Also carried to PLAN: a notices file in the repository crediting both upstreams (MIT), OpenMapTiles and OpenStreetMap (ODbL) for any embedded tile data. |
| D-26 | 2026-09-18 | OQ-7 — how a host's theme reaches the map; relationship to go-studs | "A" — *the host supplies a plain palette; no go-studs dependency* | The library defines a small palette of named roles (land, water, roads, borders, labels, overlay severities), each a plain colour value, with defaults supplied by its own styles (D-24). A host may override any or all roles. **go-tuiMaps does not import go-studs or any design system.** The translation from a host's theme tokens to the palette lives in the host; for the first host that is its existing seam layer. Requirements carried to PLAN from the evidence: **(1)** the palette can change at runtime without re-parsing cached tiles, so colours are resolved at draw time rather than baked into parsed tiles as upstream does — same behaviour, different internals, flagged in the matrix against P-41/P-44; **(2)** the library exposes a cheap change counter so a host's frame cache knows when to redraw. An optional adapter package for go-studs remains possible later if go-studs becomes a published module; nothing is built for it now. |
| D-27 | 2026-09-18 | OQ-11 — the offline tiles built into the binary | "A" — *a separate opt-in package; depth decided after it is measured* | Embedded tiles live in their own package; the core library carries none and accepts offline tiles from any source. The standalone app imports the package, and so does the README's quick-start example, so following the README yields a map that works with no network; a host that wants a lean binary leaves it out. **Depth (zoom 0–1, 0–2 or 0–3) is decided by HUM LEAD after Tier 2 research (AI-9) measures real sizes**; the 2–5 MB figure for zoom 0–3 is an estimate, not a measurement, and the effect on a host's resident memory is to be measured rather than assumed. Three requirements recorded regardless: embedded tiles are a fallback of last resort and **never override a source the user has chosen** (upstream consults them first even for a custom server — ledger #13, marked *fix*); they are **cut from the provider's free weekly planet download, never collected from the public server**; and their OpenStreetMap / OpenMapTiles notices ship beside them (D-25). Upstream's zoom 0–1 embed (P-47) is recorded as *met by the assets package*. |
| D-28 | 2026-09-18 | OQ-9 — how changes reach `main` | "A, move to B on our first release milestone (0.01.0)" | **Until the first release:** `main ← release/v0.1.0 ← feature/*`. Feature branches merge into the release branch locally at approved phase exits and milestones, always as a true merge commit (never fast-forward) whose message follows the PR template's headings. `main` changes only through a pull request carrying the full A2DH PR template; the first is `release/v0.1.0 → main` at SHIP, when the remote is created and visibility is decided (D-19). **From the first release milestone onward:** every merge, at every level, is a hosted pull request with the template (option B). PR titles and bodies follow the same commit-hygiene rule as commit messages. *Executed: `release/v0.1.0` created from `main`; nothing merged.* Carried to PLAN: provision the PR template into the repository before the first PR. **Reading recorded for confirmation** *[confirmed 2026-09-18 by D-51]*: "0.01.0" is taken as **v0.1.0**. Semantic Versioning does not permit a leading zero in a version number, and the Go toolchain only recognises tags that are valid semantic versions, so the tag will be `v0.1.0`. |
| D-29 | 2026-09-18 | The numbers for M4 — Embed Cost | "A" — *set a ceiling now, to be confirmed by measurement at PLAN exit* | **M4 target: ≤ 8 MB of added resident memory** with one map mounted at 149×38 cells showing the basemap, one feature overlay and one field or radar overlay. **Every cache is capped by bytes, with host-configurable caps** (decoded tiles, simplified shapes per D-16, decoded overlay images). **Near-zero added allocations when nothing has changed** — the change counter (D-26) lets the host reuse its previous frame. **Heap flat over a one-hour soak.** CPU within the first host's existing animated-view budget while panning, about zero while idle. **Status: a target, not a measurement.** Nothing has been built. The figure is derived from the first host's 10.7 MB margin under its 126 MB gate (AI-4 §5) and is **validated or revised at PLAN exit against a measured prototype**; if it proves tight once radar animation is included, the choice between trimming the feature and moving the host's gate returns to HUM LEAD with measurements. The structural rules stand regardless of the figure. Upstream habits not carried over: a tile cache bounded by count, a canvas reallocated every frame, a deep copy on every cache hit (ledger #11, marked *fix*). RS-7 stays High until measured. |
| D-30 | 2026-09-18 | The numbers for M2 — Time to Placed View | "A" — *as drafted, plus a never-blank rule* | **M2 targets: warm ≤ 1 s** (tiles in memory or on disk) to the full placed view; **cold ≤ 3 s** (nothing cached) to full detail, measured against a **local test server with a fixed simulated delay** — tests never touch the public tile server (its terms forbid automated collection, and its speed varies). **The clock starts when the host has handed the overlay data in**; a host's own time to fetch weather is the host's metric (D-15). **Never-blank rule (new requirement):** the first frame after a request always shows a basemap at whatever detail is on hand — a stretched lower-zoom tile, including the embedded ones (D-27) — plus the place, plus the overlay, and sharpens as tiles arrive. This requires stand-in tiles and parallel fetching, internal extensions beyond upstream (which shows nothing until every tile has arrived, fetched serially with no timeout); flagged in the matrix against P-51. **Status: targets, validated or revised at PLAN exit by measurement**, as D-29. A stand-in-tile specimen is added to the Tier 2 set: whether a coarse stretched basemap helps or confuses at terminal resolution is settled from a rendering, and the rule takes its final form from that. |
| D-31 | 2026-09-18 | Tier 2 — the rendered-specimen spike (AI-6) and PMTiles research (AI-9) | "A" — *the full set, staged* | **Nine specimens**, each at the first host's two real rectangles (69×12 and 149×38 cells): (1) a field as cell background under braille map lines — RS-3, go/no-go; (2) the same view in the block renderer; (3) the same field with no colour; (4) an alert polygon as outline plus tint with a place marker; (5) a wind-arrow lattice coloured by speed; (6) a radar image placed under the map; (7) a stand-in basemap beside the detailed one; (8) rivers and parks with draw-order variants; (9) the attribution line on a 12-row map. **Staged:** specimen 1 first and alone; if the approach is clearly illegible, work stops and the finding returns to HUM LEAD before the rest. **Throwaway code, outside the repository**, using off-the-shelf libraries; it is not the start of the implementation and none of it is copied into the project, so FULL TDD is untouched. Only learnings and specimen files enter the repo, under `02-analysis/specimens/`, as plain text and as colour-coded files; colour is judged in HUM LEAD's own terminal, since the conversation shows plain text only. Real data where obtainable responsibly (one view's worth of tiles fetched once and cached; a live alert polygon; a public-domain radar tile; a small temperature lattice or, failing that, a synthetic field), each specimen labelled with what it uses. No location was named, so specimens are drawn where a live polygon alert is active. **AI-9 runs in parallel**, read-only, and feeds the D-27 depth decision. |
| D-32 | 2026-09-18 | Specimen 1 go / no-go — a field as cell background under braille map lines (RS-3) | "so far these look good - proceed when ready" | **GO**, judged by HUM LEAD from the colour files in HUM LEAD's own terminal. The field-as-background approach is the working basis for scalar grids, and by extension for radar and tinted polygons. **RS-3 moves from High to Medium**; it closes when the remaining specimens (block renderer, no-colour, reduced colour depths) are reviewed. Findings S1-1..S1-7 stand as recorded in `specimens/README.md`. Specimens 2–9 proceed. |
| D-33 | 2026-09-18 | Completes D-27 — depth of the embedded offline tiles, and whether to strip them | "A" — *zoom 0–3, translations stripped (1.7 MB)* | **The opt-in assets package embeds zoom 0–3 with place-name translations stripped**, keeping the local name, the Latin-script name and the English name, plus class, rank, capital and country code. **Measured** (AI-9 §5): 16.1 MB as served; 1.7 MB stripped; largest decoded tile falls from 1.56 MB to 159 KB, which matters under the M4 ceiling (D-29). **Why zoom 3** (specimens `07a`, `07d`, `07e`, `07f`; finding S7-2): at regional scale zoom-3 tiles alone give a complete map, so national and regional weather maps work with no network; at city scale no embedded depth helps, which is the cache's and the local file's job (D-18). **Build:** a small script committed to the repository generates the set from a **pinned** version of the provider's published planet file by range request, never from the tile server (D-21, D-27); the generated asset is regenerated deliberately, not on every build. **Licence:** the embedded data is ODbL, not MIT; the package carries its own notice with the OpenStreetMap and OpenMapTiles credits, and committing the script publishes the method of alteration as ODbL asks (AI-9 §5; reported as documented by the licensor, not legal advice). **Accepted consequence:** when running from embedded tiles only, low-zoom labels appear in local, Latin-script and English forms; live and cached tiles keep every language. A variant keeping a few major languages was offered and not taken; it remains a cheap later change, since the set is generated. P-47 is met by this package. *[Refined by D-82: every translation but English is stripped; the asset's new size is measured when the generator is built.]* |
| D-34 | 2026-09-18 | Closes D-23's open choice — how a scalar field appears with no colour | "C (specimen first), with A as a fall-back" | **Preferred: contour lines with value labels**, as a printed weather map handles monochrome — shape and values together. **Not ratified until a specimen shows it works**: contours share the screen with braille map lines and must be told apart without colour, which cannot be promised from prose. **Fall-back (S3-2): a sparse lattice of values** — *[corrected 2026-09-18: shown to work at 149×38 only; an earlier wording here said "at both sizes", and no 69×12 lattice specimen exists.]* Density glyphs (S3-1) are rejected. The contract is unaffected either way: the host hands over the same grid. *[Withdrawn 2026-09-18 by D-39 — silence is never consent; and the limit itself was withdrawn by D-35.]* ~~Recorded limit, not objected to when presented:~~ placed images such as radar have no numeric or contour form; with no colour they are reported as needing colour, while feature overlays (polygons, markers, wind arrows) still draw. A contour specimen is added to the Tier 2 set. |
| D-35 | 2026-09-18 | Completes D-34 — the no-colour form of overlays, after the contour and radar specimens | "show precipitation specimen - that w/radar will be the most common use-case for maps" · then, on the specimens: "By Kind of Data approach approved" | **With no colour, the form follows the kind of data.** Smooth scalar fields (temperature, pressure): **contour lines with value labels** (S3-3). Patchy data (radar, precipitation): **block shades** by intensity (S11-2). In both, the basemap thins — roads dropped, water as outline (S3-4). The value lattice (S3-2) stays on record as the fall-back for smooth fields. **Withdrawn:** the limit recorded under D-34 that placed images would be reported as needing colour. **Priority recorded from HUM LEAD's statement:** precipitation with radar is the most common map view, so the georeferenced-image shape and its reduced-colour and no-colour forms are first-rank work, not an add-on. Carried to PLAN: **(1)** the form defaults from the input shape (grid → contours, image → shades) and is a property the host can set, because a dense model precipitation grid is patchy too; **(2)** a placed image carries an optional colour-to-intensity table so the library can re-ramp it per colour depth and shade it with no colour (S6-4, correcting S6-1); **(3)** a proper contouring pass with diagonal strokes; **(4)** verify box-drawing and block glyph widths in the supported terminals; **(5)** tune the light-rain shade (S11-5). Evidence that a point lattice is not a usable precipitation source: S11-1. |
| D-36 | 2026-09-18 | How radar, and any placed image, looks in colour | "C - we can provide color -> intensity ranges, and we have to support apps (watchpost) that might want to theme their maps - so the data ranges for overlays/projects have to be themeable *and* make sense in that context." | **The library re-colours every placed image with its own ramp.** It reads intensity back from the image through a colour-to-intensity table, then draws the classes with a ramp it controls; the provider's colours are not shown, at any colour depth. **The table is required with every placed image and every tile-image provider** (shapes 4 and 5), and is supplied by the host, which knows its source (D-15); the library carries no provider's palette. Worked examples belong in the app and the documentation. **Overlay ramps are themeable (new requirement, extends D-26):** the data ranges of every overlay — field ramps, image intensity ramps, severity colours, wind-speed colours — are palette roles a host can override, at runtime, like the basemap roles. **And they must make sense in that context:** a themed ramp stays ordered so that more reads as more, keeps its steps distinct at the colour depth in use, and keeps map lines and labels readable on it (S1-3, S6-2, S10-2). Carried to PLAN: how a ramp is validated or repaired when a theme supplies a poor one; how the 256- and 16-colour forms are derived from, or supplied alongside, a theme's ramp; and a **legend is now required, not optional** — since the colours are the library's and the host's rather than the provider's, the library exposes every overlay's class breaks and colours (S1-6). Side effect to measure: holding an image as one class byte per pixel rather than four colour bytes should cut its memory to about a quarter (bears on D-29). Supersedes the "placed as-is" reading of S6-1 and the optional table of S6-4. *[See D-45 (the table is required, with a tested example), D-53 and D-55 (ramp safety; temperature is not themeable).]* |
| D-37 | 2026-09-18 | The upstream defect ledger (the separate review approved under D-11) | "Approved as drafted" | [`defect-ledger.md`](defect-ledger.md) is approved and **closed**: 17 entries — 14 fixes, 2 fixes already ruled (L-4 by D-12, L-13 by D-27), L-17 (a)–(f) replicated as conventions, L-14 not applicable. Nothing is replicated as a bug. **L-17 (f) stays *replicate*:** the map does not repeat across the antimeridian, as upstream; the option to fix it was presented with its consequence for Pacific views and not taken; it remains flagged for a later look. Anything not in the ledger matches upstream. The parity matrix can now be frozen. |
| D-38 | 2026-09-18 | The red-team persona set at DISCOVER exit | "confirmed" | Four personas run as overlays on the DISCOVER-exit red-team, as the red-team skill requires human confirmation before any persona runs: **Accessibility**, **Performance**, **InfoSec**, **Junior developer / newcomer** — each matched to this phase's scope by its own activation rules. **Safety-critical was not proposed:** its rules activate on compiled-language code under the P10 gate, which is declared at BUILD entry (D-20). The four fixed axes (Code Quality solo; Project Hygiene; Business Quality and Docs Quality sectioned with independent verdicts) and the DISCOVER phase lens run regardless. Every reviewer is read-only and records the tree state it reviewed. |
| D-39 | 2026-09-18 | Clarifications by HUM LEAD of three rulings, after the docs review set HUM LEAD's verbatim words beside the recorded implications and found the widest gaps at D-14, D-34 and D-36 | On D-14: "D-14 shows public NOAA (watchpost has at least one of these APIs) so that informs me watchpost can ADD APIs to get this data for overlays" · On D-34: "D-34 Okay, I approved off the 'by data type' for overlay draws" · On D-36: "D-36 I apporved us having color values for intensity bands in overlays - stemming from the needs to theme radar/precip to match watchpost themes" | **D-14:** the condition "if we can get radar data from our APIs" is met, in HUM LEAD's words, because the first host **can add** the public NOAA-family APIs — not because it holds radar today. The recorder's phrase "the stated condition is met on the evidence" is withdrawn; it certified the human's own condition. Radar stays in v1 scope. The host-side work is real and is listed per overlay shape in the requirements (CD-1). **D-34 / D-35:** the phrase "not objected to when presented" is withdrawn — silence is never consent. What stands is the explicit approval of the by-kind-of-data approach (D-35). **D-36:** what was approved is that overlay intensity bands have colour values, and the reason is theming radar and precipitation to match the first host's themes. **Still open, and not settled by this clarification:** whether the colour-to-intensity table is *required* with every placed image and whether a provider's own colours may ever be shown. Both came from the wording of option C as presented, not from HUM LEAD's words; two reviewers challenged them. To be put to HUM LEAD as its own question. *[The item left open here — whether the table is mandatory — was ruled in D-45.]* |
| D-40 | 2026-09-18 | Red-team Q1 — which specimens HUM LEAD has actually viewed | "I have no seen any other than the one you showed me - if you want to create an artifact with all of these screens that would be helpful" · and, on the review page's type: "Jetbrains Mono is also a good monospaced font to use as well if available" | **HUM LEAD has viewed specimen 1 only.** Everything else in the specimen set is unreviewed by HUM LEAD. Consequences recorded honestly: **RS-3 stays Medium**; the specimen findings S2–S11 are the coordinator's assessments from browser approximations, not ratified visual decisions; and three rulings were made from the coordinator's *descriptions* of specimens HUM LEAD had not seen — D-33 (embedded tile depth, from the stand-in specimens), D-12's follow-through (rivers and parks) and D-25's default line — while D-35 was made from no-colour specimens pasted as text into the conversation, which HUM LEAD did see. Each stands as ruled, and each is open to revision once HUM LEAD has looked. **Action taken at HUM LEAD's request:** a private review page showing all 37 specimens, redrawn from their real colour codes with braille and block characters drawn as shapes so nothing depends on browser fonts, grouped A–G to match the question, with a verdict recorder that builds a reply to paste back; monospaced text set in JetBrains Mono as HUM LEAD suggested. The page is outside the repository. HUM LEAD's terminal remains the final test. |
| D-41 | 2026-09-18 | Red-team Q1, completed — HUM LEAD's verdicts on the specimen set, given after viewing all of it on the review page | "A. Alert polygons: seen — looks fine · B. Radar: seen — looks fine · C. Wind: seen — looks fine · D. Block renderer: seen — I have concerns — blocks for roads are hard to distinguish - the braille renders are much better · E. Reduced colour: seen — looks fine · F. No colour: seen — looks fine · G. Stand-ins, rivers, attribution: seen — looks fine · 1. Specimen 1 — already ruled: seen — looks fine" | **Seen and fine: groups A, B, C, E, F, G and the specimen-1 variants.** The visual approaches they show now rest on HUM LEAD's own look rather than on the coordinator's descriptions: alert areas as tint plus outline; radar under the map; wind arrows; purpose-built ramps at 256 and 16 colours; contours and block shades with no colour; stand-in tiles, rivers and parks, the in-map credit line. D-33, D-12's follow-through and D-25, which D-40 recorded as ruled from descriptions, are now backed by HUM LEAD's viewing. **Viewed on the review page, not in HUM LEAD's terminal** — a faithful redraw of colour and layout; HUM LEAD's terminal remains the final test and nothing here claims otherwise. **Concern: group D, the block renderer** — in HUM LEAD's words, "blocks for roads are hard to distinguish - the braille renders are much better". This agrees with finding S2-1 and is in tension with D-23, which made both renderers first-class. Not resolved here: a block specimen with the road net thinned is produced first, then the question is put to HUM LEAD. **RS-3 moves to Low for the braille renderer**; block-renderer legibility is carried as its own risk. The specimen findings' known gaps stand (the default ramp is not colour-vision-safe; feature overlays and wind speed mean nothing with no colour; no re-coloured radar yet). |
| D-42 | 2026-09-18 | HUM LEAD's verdicts on specimens 12–15 (review-page groups H–K), and with them what "first-class" means for the block renderer (RS-22) | "H. NEW · Block renderer, road net thinned: seen — I have concerns — Better; but the motorways only still looks a bit too much, but it's \"okay\" if the user opts-into this view, so long as they can opt-back to braille or remove the roadways · I. NEW · No colour: alert areas and wind: seen — looks fine · J. NEW · A colour-blind-safe ramp: seen — looks fine · K. NEW · Radar re-coloured and themed (ruling D-36): seen — looks fine" — groups A–G and specimen 1 were re-stated unchanged from D-41. | **Seen and fine: I, J, K.** Feature overlays with no colour (hatched, labelled areas; arrow-plus-speed wind), a brightness-ordered colour-vision-safe ramp with per-cell line work, and radar re-coloured and themed are now backed by HUM LEAD's own look; **D-36 no longer rests on a description.** **Block renderer (H): better when thinned, motorways-only still "a bit too much", and acceptable on stated terms.** Recorded as what "first-class" means, amending D-23's wording without widening it: **braille is the default renderer; the block renderer is one a user opts into; a user can switch back to braille at any time; and the road layer can be removed.** The block renderer stays supported and tested, with a sparser default profile than braille (no roads by default is consistent with HUM LEAD's words; PLAN settles the profile from specimens). Two requirements follow: the renderer is switchable at runtime in both directions (upstream's toggle, parity row P-68, already implies it), and **basemap layers — roads at least — can be switched off by intent** (new). Viewed on the review page, not in HUM LEAD's terminal. RS-22 moves to Low. *[Groups I and J as seen for this verdict were files later found defective — 13a had no place marker, 14 showed the wrong legend and, until round 3, the weaker text colour on one band. Corrected files are filed; a second look is asked for before PLAN exit.]* *[The second look at the corrected groups I and J was taken on 2026-09-19 and both found fine (D-89).]* |
| D-43 | 2026-09-18 | Red-team Q2 — how M1 is judged: the scenario set, the judge, and where the live session sits | "Recommendation approved" — *the recommendation put to HUM LEAD: the seven-scenario set as drafted; HUM LEAD judges against a computed answer key; M1 gates go-tuiMaps' SHIP on the library with host-shaped fixtures, and the session inside the first host becomes the host's own metric after it integrates* | **M1 now has a denominator:** seven scenarios — an alert with its own polygon; an alert defined by a zone shape with the place near the edge; radar with rain near the place; a temperature field; wind; point hazards; a storm track — each at both map sizes, in braille, at truecolor and with no colour. Recorded in [`m1-scenarios.md`](../01-objectives/m1-scenarios.md) under the same change control as the parity matrix. **Judge:** HUM LEAD, against an answer key computed from geometry and data; a frame may never contradict the key, which doubles as an automated test. **Live session:** M1 gates this project's SHIP on the library, shown live in the standalone app; the session inside the first host is that host's metric, after it integrates. This **replaces the brief's wording** ("a live HUM LEAD acceptance session inside Watchpost"), which D-19 made impossible as a SHIP gate. The limit was stated when ruled: passing on fixtures is a claim about the library, not about a person using the host. RS-11 stays High for the host's readiness; the circularity the phase lens found is removed. |
| D-44 | 2026-09-18 | Red-team Q3 — what the first release contains. Put to HUM LEAD with the aggregate HUM LEAD had never been shown: a reviewer's estimate of about 7,800 hand-written lines for the library and 1,200 for the app, against a research figure of 1,500 that predated most scope rulings. | "B - then we'll integrate into watchpost at v0.17.0 as our first in-app use-case/test - then come back and build the other data shapes." — *option B as presented: the radar-first slice plus temperature* | **v0.1.0 contains:** the braille basemap to parity on the rows its scope touches; network source, long-lived disk cache and embedded zoom 0–3 tiles; on-demand and headless render with the asynchronous-work model; **three overlay shapes — features (with simplification), georeferenced images re-coloured through a table (radar), and scalar grids with ramps and contours (temperature)**; legend, credits, valid time, distance reference, text safety, input validation and every security requirement; truecolor, 256 colours and no colour; static and blinking markers; a minimal standalone app. **Sequence, in HUM LEAD's words:** v0.1.0 → **integrate into Watchpost at its v0.17.0 as the first in-app use and test** → come back and build the other data shapes. **Deferred past v0.1.0, all still in v1 by earlier rulings:** vector grids (wind), tile-image providers, the PMTiles reader, the block renderer, the 16-colour depth, camera tours, pointer and focus-zoom operations, airport layers, style expressions beyond legacy filters. **Consequences recorded:** M1 for v0.1.0 covers scenarios 1, 2, 3, 4, 6 and 7 — wind (5) waits; M3 is stated per release, its v0.1.0 denominator being the parity rows this scope touches (to be set under the matrix's change control, Q8); the first host's session (D-43) now has a date — its v0.17.0; and the visibility decision D-19 left for SHIP falls at v0.1.0, since the host's builds must be able to fetch the library. **Carried to PLAN as a safeguard proposed with the question, not part of HUM LEAD's words:** the contract is *designed* for all five shapes and *built* for three, so the deferred shapes constrain the design and wind does not force a breaking change later (RS-2). RS-5 and RS-21 move from High to Medium. |
| D-45 | 2026-09-18 | Red-team Q4 — must every placed image come with a colour-to-intensity table? (Left open by D-36 and D-39.) | "C" — *required, but the project ships tables as examples* | **The table is required with every placed image and every tile-image provider**: one rendering path, one consistent look at every colour depth and with no colour, and a host's theme can never be bypassed. **The project ships a complete, tested table for at least one keyless radar provider under `examples/`**, outside the library's contract, so a newcomer copies it rather than reverse-engineering a palette; the library itself still knows no provider (D-15). An exact table has not been built yet — the specimens read intensity by hue — and building one is now a v0.1.0 task, needed for the M1 radar scenario and the reduced-colour and no-colour forms in any case. **Safeguard against a provider changing its palette:** matching is nearest-colour within a stated tolerance; unmatched pixels become no-data and are **counted and reported**, so a stale table shows up as a count of unknown colours rather than as silently wrong intensities (RS-19). The "place as served, dimmed" fallback (option A) was offered and not taken: a provider's own colours are never shown. |
| D-46 | 2026-09-18 | Red-team Q5 — closing the item D-18 left open: which tile schemas v1 reads | "A" — *OpenMapTiles only in v1; keep the seam* | go-tuiMaps v1 reads **OpenMapTiles-schema** tiles from any source — network, single-file archive or embedded. Files in the Protomaps schema are not supported in v1 and produce a clear "unsupported schema" error (NFR-20), not a half-drawn map. **The tile schema stays a seam (FR-35)**, so another schema — including the one the default provider has said it may move to — is a new mapping later, not a new renderer. **Closes D-18's open item**, and with it the coordinator's statement there that a PMTiles reader would force schema mapping into v1: AI-9 found the default provider publishes a full-planet PMTiles file in the OpenMapTiles schema, byte-identical at low zoom to its tile server, so the project's own styles (D-24) work against it with no mapping. The PMTiles reader itself is deferred past v0.1.0 (D-44). |
| D-47 | 2026-09-18 | Red-team Q6 — is radar animation in scope, and when? (An assumption had said so; no requirement or ruling did.) | "A" — *in v1, after v0.1.0* | **Frame sequences are in v1 and are built after the first host's integration.** The contract lets an image overlay carry a sequence of timed frames; that is *designed* in PLAN, with the rest of the contract (D-44), and *built* after v0.1.0. **v0.1.0 shows the latest frame**; a host can show motion in a still frame with a feature overlay — a motion arrow from the storm motion that warning polygons and tropical-storm data already carry. No new machinery is needed when it is built: animation time already comes from the host (FR-25). **Memory, recorded so it is not rediscovered:** at one intensity byte per pixel (D-36) a typical view is about 0.39 MB a frame, so a ten-frame loop is about 3.9 MB — half the 8 MB ceiling; this is why the v0.1.0 memory fixture (Q7) is single-frame, and why the loop's frame count must sit inside the image cache's byte cap. The limit was stated when ruled: a loop reads far more fluently than a still frame with an arrow, and the first host's integration is where that will first be felt. Assumption A-1 is corrected accordingly. |
| D-48 | 2026-09-18 | Red-team Q7 — what the 8 MB memory test contains. (D-29 set the figure; two reviewers showed the test as written was unsized, unrepeatable, and beaten by 12.4 MB of source geometry in the ruled worst case.) | "A" — *a typical-day fixture, plus a separate worst-case rule* | **The pinned fixture for M4 / NFR-3:** one regional view; one alert overlay of **10 zone shapes, about 50,000 vertices**; one **single-frame** radar image; one temperature grid. **Pass:** live heap added ≤ **4 MB** and peak heap ≤ **8 MB**, read from the runtime's own metrics after a forced collection, at the default collector setting; the first host's resident-memory protocol is confirmatory only. The fixture's exact contents are committed to the repository so the PLAN-exit measurement cannot drift. **The worst case is tested separately, against a rule it can meet:** the 58-zone, 812,058-vertex alert must be accepted and drawn **without the library copying the source geometry** (held by reference, FR-11), its simplified form must fit the shape cache's byte cap, and growth must be bounded — correctness and boundedness, not the 8 MB figure. The host's memory for its own data is the host's. D-29's figure stands, still a target to be validated or revised at PLAN exit by measurement. The limit was stated when ruled: the "typical day" is the coordinator's invention, and severe-weather days are when the map matters most; the worst-case rule is what guards those days. |
| D-49 | 2026-09-18 | Red-team Q8 — six corrections to the frozen parity matrix, under its own change control, plus two notes | "A" — *approve all six corrections plus the two notes* | **(1)** P-23, P-24, P-25 and P-41, written in the vocabulary of the renaming shim D-24 removed, are **restated in OpenMapTiles terms** — labels deferred by the style's layer type, draw order in real layer names, style rules matched on the real layer name; same behaviour. **(2)** P-08, the cell-colour vote, becomes **Extended** with three recorded exceptions: foreground chosen for contrast under a field or image; water does not vote when it owns the background; the block renderer's equivalent is settled in PLAN. **(3)** The **"Open for PLAN" disposition is deleted** as a loophole: P-06 and P-07, the colour-output form, return to the denominator as *Extended, form pending* and must be demonstrated whatever form PLAN picks. **The denominator is 70**, with P-40 and P-46 excluded by named ruling. **(4)** P-34 matches the collision *rectangle semantics*, not the linear scan. **(5)** P-11 matches the behaviour — a wide character takes two cells — not the hand-rolled width table; P-59's flash rate awaits Q13. **(6)** A Release column marks each row: **v0.1.0's denominator is 62 rows**; 8 rows wait for their scope (the block renderer, the camera and tours, mouse input). **Notes:** FR-12 (compositing order) and FR-19 (style profiles), which traced to specimen findings HUM LEAD had not then reviewed, are ratified by HUM LEAD's viewing of those specimens (D-41, D-42); the two row exclusions are named in the matrix. The first four columns of every row remain the script-verified copy of the source read; corrections sit only in the Disposition, Release and Note columns, and the copy was re-verified by diff after the change. |
| D-50 | 2026-09-18 | Red-team Q9 — hygiene before this repository is ever published: (a) a superseded commit kept only in the local undo history; (b) how much tracked documents say about commit-hygiene rules; (c) the process framework's name and command names in tracked files | "9a-C, 9b-A, 9c-A" | **(a) Purged, and publish only from a fresh clone.** Before anything was removed HUM LEAD was shown exactly what was unreachable: two superseded commits from the project's first hour, no stashes, all 27 commits on branches. After the purge: branch tips unchanged, 27 commits still on branches, no unreachable commits, integrity check clean, working tree clean. **Standing rule: the repository is published from a fresh `git clone`, never by copying the folder.** The repository carries its own personal identity setting; a fresh clone does not inherit it, so identity is checked at every phase exit and, later, in continuous integration. **(b) Neutral wording in tracked documents:** they say "sole-author commits; no tool-generated trailers or watermarks", and no more. The brief is amended to v1.0.2 accordingly, with the change logged. **(c) The framework's name stays**, as ruled public in the first host's repository; its command names are replaced by plain descriptions, and the glossary explains the process vocabulary. *[Scope settled by D-70: records of decisions are exempt; the rule binds everything that describes the project.]* |
| D-51 | 2026-09-18 | Red-team Q10 — confirming the reading of "0.01.0" in D-28, recorded there "for confirmation" and never confirmed | "A is fine" — *v0.1.0* | **The first release is v0.1.0.** The reading recorded in D-28 is confirmed; the branch `release/v0.1.0` stands; D-44, D-47 and D-49, which build on it, need no change. The form "0.01.0" cannot be used as written: Semantic Versioning does not permit a leading zero, and the Go toolchain recognises only tags that are valid semantic versions, so a host could not fetch a release tagged that way. |
| D-52 | 2026-09-18 | Red-team Q11 — a plain-text answer to "where", alongside the picture (accessibility A-1, rated Critical; reached independently by the phase lens) | "A - watchpost already has say/piper integrations to also read out map descriptions - so we satisfy the A11y/PM requirement in both ways" — *option A: a requirement, in v0.1.0* | **The library exposes a description of the view, as data, for each place the host names — in v0.1.0.** For each place: inside or outside every area feature, with distance and compass bearing to the nearest edge; the field value and its band, and which way it rises; the image intensity class here and the nearest heavier intensity with its distance and bearing; each overlay's valid time. **Computed from the unsimplified shapes and the data, not from the drawn cells**, so it stays right when a drawn edge is a cell out (RS-19). Deterministic; contains no braille, block or box-drawing characters. **Exposed as data the host renders**, not sentences the library writes, so the library never decides what a host says to its users (D-15); the standalone app prints a plain rendering of it with a flag. **Why both ways, in HUM LEAD's words:** the first host already has speech integrations that can read a map description aloud, so the same data serves a screen-reader user as text and any user as speech — it should therefore be speakable: values with units, compass words, no symbols a voice cannot say. It is the same computation as the M1 answer key (D-43), exposed to the host. The limit was stated when ruled: a second output surface must stay in step with the picture; both are tested against the one answer key. |
| D-53 | 2026-09-18 | Red-team Q12 — must colour ramps be safe for colour-blind users? (Accessibility A-3, A-4: the default temperature ramp's brightness rises to amber then falls, so under red-green colour blindness the 20 °C and 27 °C bands nearly coincide.) | "A, approved for divergent ramp specimen - we MAY decide to make certain scalars non-themable for depending on the answer." — *option A: required for the defaults, checked for themes* | **Every default ramp the library ships is brightness-ordered and keeps adjacent steps distinguishable under the three common colour-vision deficiencies**, by a standard simulation with a stated threshold (proposed: a colour difference of at least 10 between adjacent steps; PLAN confirms). **A themed ramp is run through the same checker and violations are reported, not refused**; a host can run the checker in its own tests, as the first host already does for contrast. The contrast numbers already in FR-16 stand: line work at least 3:1 and labels at least 4.5:1 against every cell they are drawn on, with the foreground chosen per cell. **Specimen approved:** a *diverging* temperature ramp — blue through pale to red, brightness-ordered on each side of a labelled midpoint — to see whether the weather convention can be kept safely. **Held open by HUM LEAD, in HUM LEAD's words:** depending on that answer, certain scalars may be made **non-themeable**. Recorded as a possible narrowing of D-36's themeable ramps, not yet a decision; it returns to HUM LEAD with the specimen. *[The specimen asked for here is specimen 16, judged in D-54; the non-themeable scalar is ruled in D-55.]* *[D-69 withdrew the non-themeable scalar; the rest of this ruling stands and now covers every overlay.]* |
| D-54 | 2026-09-18 | HUM LEAD's verdict on specimen 16 (review-page group L): a diverging blue–pale–red temperature ramp | "Group L - Seen, Looks fine" | **The diverging ramp is seen and fine**, on the review page. With its measurements (brightness ordered on each side of the midpoint; blue and red never confused under the three common deficiencies; every step clearing 4.5:1 with per-cell line work; one marginal adjacent pair at 9.4 under protanopia, to be tuned), it shows that a colour-vision-safe default for temperature can keep the weather convention. It becomes the working basis for the default temperature ramp; PLAN tunes the marginal pair and settles how the midpoint is supplied. **Not answered by this verdict, and not inferred from it:** the follow-up HUM LEAD raised in D-53 — whether certain scalars become non-themeable. It remains open and is put to HUM LEAD again. *[Red-team round 2 found the legends in the files seen here showed the previous ramp. Corrected files are filed; a second look is asked for.]* *[The midpoint and the non-themeable question left open here were ruled in D-55 and D-62: temperature's scale, midpoint and breaks are the library's.]* *[Second look at the corrected files taken 2026-09-19: "L looks fine as well" (D-89).]* |
| D-55 | 2026-09-18 | The follow-up HUM LEAD raised in D-53 — should any scalars be non-themeable? | "B" — *temperature is fixed; the rest is themeable* | **Temperature always uses the library's colour-vision-safe diverging ramp** (D-54); a host's theme can adjust its line work and labels, **not its bands**. **Radar and other image intensity, alert severities and wind stay themeable**, since matching the first host's themes was D-36's purpose; their themed ramps are checked and violations reported (D-53). This **narrows D-36** for one scalar and no other. Reason recorded: temperature is read by convention rather than by legend, and is where a red-green mistake does most harm; fixing it protects it in every host with a rule that fits in a sentence. The cost was stated when ruled: a themed host shows one map that does not follow its theme, and host authors must know which overlays do. Carried to PLAN: how "temperature" is identified in the contract — a scalar grid declares its kind — and whether other convention-bound scalars are added to the fixed list later, each by ruling. *[See D-62: the scale is absolute — midpoint and breaks are the library's too — and the protection is a convention, not a control; "protects it in every host" overstated it.]* ***[WITHDRAWN by D-69, 2026-09-19: presets, not locks — temperature is themeable like every other overlay, and the fixed scale survives as the temperature preset's definition.]*** |
| D-56 | 2026-09-18 | Red-team Q13 — the flashing marker, and reduce-motion (accessibility A-5) | "A" — *slow the flash, add reduce-motion, never hide on a timer* | **Flash runs at no more than 2.5 per second** — a recorded deviation from upstream's 3.33 per second (parity row P-59, now *Fix* for flash), which is above the three-per-second accessibility threshold and, sampled by the first host's 300 ms clock, would never have been seen to flash at all. Blink (1.25 per second) and pulse keep upstream's rates. **Reduce-motion:** one setting under which markers draw in a steady "on" form, camera moves jump to their target, and nothing animates; a host can set it, and the app honours the terminal convention if one exists. **No marker is ever invisible because time stopped:** a frozen clock shows the "on" form. **The credit line never disappears on a timer alone**; it may collapse on interaction, as D-25 allows. Blink is in v0.1.0; flash, pulse and tours are built later (D-44), under these rules. The counter-argument was stated when ruled: the threshold was written for large flashing areas, and a 3×3-dot marker almost certainly falls under its small-area exception; the rate is cheap to revisit when flash is built. |
| D-57 | 2026-09-19 | Red-team round 2, R2-Q1 — a braille-only first release against the first host's stated glyph floor of block and arrow characters | "A" — *keep braille-only; the first host states the requirement* | **v0.1.0 stays braille-only (D-44 unchanged).** The contract gives a host a way to decide whether the map is drawn at all, and the description as data (FR-29, D-52) is the fallback where it is not — including under the host's plain-character mode. The first host's README gains braille as a stated need for maps; that line joins the host's companion backlog (CD-1). The block renderer stays in v1, built after the integration (D-42, D-44). *Put to HUM LEAD with this limit stated: nobody has measured which fonts lack braille; that most modern terminal fonts carry it is an assumption, to be tested in the glyph matrix at PLAN entry.* |
| D-58 | 2026-09-19 | Red-team round 2, R2-Q2 — how the embedded tiles are generated when the archive reader is deferred past v0.1.0 | "A" — *a minimal reader now, internal, hardened* | **v0.1.0 contains a small single-file-archive reader as an internal package**: header, directories, one tile by range request; under the untrusted-input limits (NFR-10) and fuzzed like every other decoder. The tile generator (FR-28a) uses it, so no unfuzzed parser and no outside tool touches the asset. The public PMTiles *source* — local files, remote archives, its caching — stays deferred (D-44) and will wrap the same code. A partial change to D-44's slice, made knowingly: roughly 250 to 300 lines move earlier (an estimate, unverified). |
| D-59 | 2026-09-19 | Red-team round 2, R2-Q3 — what v0.1.0 does when the host's depth hint is 16 colours, before the 16-colour ramps are built | "A" — *basemap and features in 16 colours; fields and images in their no-colour form* | **Nothing on screen may misreport the data.** With a 16-colour hint, v0.1.0 draws line work, labels, markers and alert outlines from a small 16-colour palette for basemap roles, and draws anything that needs a ramp — temperature, radar — in its no-colour form (contours, block shades; D-35). The legend and the description say which form is in use, and a warning is retrievable (NFR-20). When the real 16-colour ramps are built the behaviour improves with no change for a host. **Specimen owed before PLAN exit:** a coloured basemap with a colourless overlay has never been rendered; if it fails, the fall-back put to HUM LEAD was to treat 16 colours as no colour. |
| D-60 | 2026-09-19 | Red-team round 2, R2-Q4 — (a) what v0.x promises the first host about compatibility; (b) whether findings from the integration return to HUM LEAD before the remaining shapes are built | "(a) A; (b) 1" — *breaks allowed, but only loudly; a written integration review, then HUM LEAD's GO* | **(a) Before v1.0 the public contract may change only at a minor version, never in a patch.** Every break is listed in the changelog with a migration note; the README states the promise; an automated check compares the public contract against the last tag, so a break cannot happen by accident. **(b) After the integration into the first host at its v0.17.0, a written integration review comes to HUM LEAD before any remaining shape is built:** what the host needed that the contract lacked; memory and timing measured inside the real host; contract changes proposed; whether the plan for the remaining shapes still holds. HUM LEAD rules on the changes and gives GO. This is where the two High risks that depend on real use (RS-2, RS-11) are re-assessed. |
| D-61 | 2026-09-19 | Red-team round 2, R2-Q5 — how a parity row counts when v0.1.0 contains only part of it (P-03, P-05, P-59, P-68, P-72) | "B" — *split the five rows in two* | **Ten rows replace five; the M3 denominator moves from 70 to 75, and v0.1.0 reads 62 of 75.** Each half is a whole row with its own test, so M3's guard — a checked-in test for every counted row — applies to every counted row. The five rows as read from the source are kept unaltered at the foot of the matrix, so the copy can still be checked against the research report. Counts after the split: Match 48, Fix 13, Replicate 2, Extended 12, Superseded 2 — 77 rows, 75 counted. P-59a (blink) returns to *Match*, its disposition before D-56; P-59b (flash and pulse) carries the D-56 *Fix*. **Chosen against the recommendation**, which was to count a row only when wholly shown (57 of 57 in scope); the argument for B was put as the counter-argument: it reports more, and the matrix had already been corrected under control once (D-49). |
| D-62 | 2026-09-19 | Red-team round 2, R2-Q6 — what "temperature is not themeable" (D-55) fixes: colours only, or the values they are pinned to as well | "A - if needed we can provide longer ramps to handle the color disambiguation.  this has been solves numberous times with other weather maps so we don't have re-invent the wheel here" | **A fully absolute scale.** For a temperature grid the library owns the colours, the midpoint (freezing: 0 °C or 32 °F) **and the class breaks**; the host supplies values and a unit, nothing else. The same colour means the same temperature in every host on every day — the strongest reading of D-55, and the one that lets the map be read by convention rather than by legend. **A longer ramp than the seven steps of specimen 16 is expected**, and PLAN is to draw on established weather-map scales rather than invent one. *Recorded tension, for PLAN:* D-53 still requires every default ramp to be colour-vision-safe at every supported depth; several well-known weather scales are not, and whether a long scale stays distinct at 256 colours in a terminal is unverified. A specimen of the chosen scale, with its checker results, is owed in PLAN; if no established scale passes, that comes back to HUM LEAD. **The common part — common to all three options as put, so carried by choosing any of them; HUM LEAD's own words on it are being asked for (red-team R3-PRD-2), and until given nothing is inferred from HUM LEAD's not objecting:** kinds are a closed, library-owned set — *generic* (the default) and *temperature* with its unit; an unknown kind, or a ramp or breaks supplied with a temperature grid, is a typed error; and the documents say plainly that this is a convention, not a control — a host that declares its temperatures generic can colour them as it likes. **Chosen against the recommendation**, which was to anchor the midpoint at freezing and leave the steps to the host. *[The option chosen was put as "flat-looking on ordinary days"; this row first dropped that. Its consequence for M1 is ruled in D-68.]* *[D-69: this scale is the definition of the temperature **preset**, which a host may override; the "common part" recorded above is replaced by D-69.]* |
| D-63 | 2026-09-19 | Red-team round 2, R2-Q7 — a "safe ramps" switch for the person using a themed host (accessibility R2-AX-7) | "A - with a clear semanticToken data contract so hosts can easily theme or integrate into their themes if desired.  Gives us the benefits of A with the flexibility of B" | **(1) A "safe ramps" setting, in v0.1.0.** When on, every overlay uses the library's default colour-vision-safe ramp whatever the theme says; basemap colours stay themed. The app offers it as a flag and a key; the README asks hosts to expose it to their users. It does not reopen D-53: a themed ramp that fails the check is still reported, not refused. **(2) The palette is a documented contract of semantic tokens.** Every colour a host can set has a stable, named role — basemap roles, and each overlay's ramp steps by meaning (an intensity class, a severity), never by position or raw index — published with the contract, so a host can map its own theme onto the tokens, or build a colour-vision-safe theme of its own, without reading the library's source. Tokens are plain data; no design system is imported (D-26). The token list is a named PLAN artefact and falls under the compatibility promise (NFR-22, D-60). |
| D-64 | 2026-09-19 | Red-team round 2, R2-Q8 — who owns the ground the map is drawn on, given a basemap of light line work that nearly vanishes on a light terminal (accessibility R2-AX-11) | "C" — *painted by default, with an opt-out* | **By default the library paints the ground**: every cell of the map rectangle gets a background from a `ground` token in the palette (D-63), so the map looks the same in any terminal and every contrast check (FR-16) is exact. **A host that wants the map to blend sets "do not paint" and declares the ground colour**; the library then picks its style and checks contrast against that declaration, and responsibility for the declaration being true is the host's — the documents say so. With no colour nothing is painted. **Specimen owed in PLAN:** HUM LEAD has never seen a painted ground; every specimen HUM LEAD approved let the terminal show through, and at 256 and 16 colours the painted ground may not match a host's own background exactly. Light and dark grounds are both to be shown. |
| D-65 | 2026-09-19 | Red-team round 2, R2-Q9 — whether the library reaches the network without being told to: D-21 ("the library's default network source") against FR-22b ("no connection unless the host configured a network source") | "A" — *the library is silent until told; the app is not* | **Building a map enables no network source.** OpenFreeMap is an exported, named source that a host passes in one explicit call; with none passed, the map draws from the embedded tiles if imported, and otherwise FR-23's floor. **The standalone app enables OpenFreeMap by default, says so in its help, and offers an offline flag.** D-21 now reads: OpenFreeMap is the default the app uses and the one the examples show — not something the library does unasked. The README states what is sent, to whom, and what is stored (FR-21b). |
| D-66 | 2026-09-19 | Red-team round 2, R2-Q10 — one commit message (`fdab035`) that says more about commit hygiene than D-50's ruled sentence | "A" — *accept it as it stands* | **The message stands; history is not rewritten.** D-50's wording rule governs tracked documents. From here on, commit messages describe the change and never the hygiene process. *Not part of the ruling, added by the coordinator as a carried item:* the SHIP checklist includes a read of the public log's wording before the first push. *[Put to HUM LEAD as one message; there were two — `fdab035` and `ae17b89`. D-70 extends this ruling to both.]* |
| D-67 | 2026-09-19 | Red-team round 3, R3-Q1 — what M1 may use at the smallest map size, after the coordinator changed FR-18a's acceptance to "the frame together with the description" without a ruling while M1 (D-43) still said "from the frame alone" | "B" — *two parts, both must pass* | **M1 has two parts, and a scenario passes only if both do. M1a — the frame alone, judged to the frame's own resolution:** the answer key computes the place's distance from the nearest edge in cells for that view; under one cell, the correct reading of the frame is "on the edge"; otherwise "inside" or "outside". The key decides which, not a person. **M1b — the description (FR-29, D-52) must match the exact key, with no tolerance.** Nothing is asked of the picture that a picture cannot give, and nothing it can give is excused. Put to HUM LEAD with the argument against it: a metric that scores "on the edge" as correct can read 100% at the moment the map matters most; M1b is what answers that, which is why it carries no tolerance. This amends the scenario set under its own change control. |
| D-68 | 2026-09-19 | Red-team round 3, R3-Q2 part (a) — the flat day: with D-62's absolute scale an ordinary day may show two or three colour bands, one at the smallest map size, and with no colour one contour or none, while M1 scenario 4 asks "which way it gets warmer". Put with the ramp tension the coordinator had filed for PLAN instead of telling HUM LEAD: narrower bands need more steps, and more steps are harder to keep colour-vision-safe (D-53). | "A" — *accept flat days; let the text carry the gradient* | **No drawing change; D-62 stands whole.** For scenario 4, M1a is judged to the scale's resolution: where the view sits inside one band, the correct reading of the frame is "no visible change" — the same logic as D-67's "on the edge" — and the answer key decides it. M1b's "which way it rises" comes from the data and is always exact. **Chosen against the recommendation**, which was to add labelled isotherms at a finer fixed interval over the colour bands. *Part (b) of the same question — the common part of D-62, in HUM LEAD's own words — was not answered by this reply and is put again on its own; nothing is inferred from it.* |
| D-69 | 2026-09-19 | Red-team round 3, R3-Q2 part (b), which became a wider question. Asked to confirm the common part of D-62 in HUM LEAD's own words, HUM LEAD instead questioned the premise: "we might be making this too hard in trying to selectively guard certain overlay types with color and ramps. tuimaps on it's own is not *necessarily* a weather app" — the library should accept an overlay type, its scalar set, its colours and its data from the host, and provide the common ones — temperature, radar and precipitation, wind, alert polygons — "out of the box, fully defined, fully colored, full working". The coordinator said this was not option A and put two options: X, presets with no locks (withdrawing D-55); Y, presets with temperature still locked. | "I think X gives us more options and doesnt code us into a corner, and hopefully simplifies our impleentation - if that reverses some other decisions that fine - part of making this \"it just works\" also means making the developer ergonomics for US and for other users as easy as possible as well." | **Presets, not locks.** For any overlay the library accepts a type, its scalar set, the colours that go with it, and the data. Four common types ship as presets, fully defined and coloured, working with no further input: **temperature, radar and precipitation, wind, alert areas** (the wind preset arrives with the wind shape, after v0.1.0 — D-44). A host may override any preset's colours or breaks, or define a type of its own. Every override goes through the checker and is reported, never refused (D-53); the safe-ramps setting returns every overlay to its preset (D-63). **D-55 is withdrawn**: temperature is themeable like everything else. **D-62 stands as the definition of the temperature preset** — the absolute scale, freezing as the midpoint, the library's breaks — and its "common part" is replaced by this ruling: there is no closed set of kinds, no error for colours or breaks supplied with temperature, and "a convention, not a control" is moot. D-67 and D-68 are unaffected: M1 scenario 4 is judged using the temperature preset. **A principle stated with the ruling, for PLAN:** "it just works" includes developer ergonomics — for this project and for other users — being as easy as possible. |
| D-70 | 2026-09-19 | Red-team round 3, R3-Q3 — hygiene, in three parts: (a) D-66 was put as "one commit message" and there are two (`fdab035`, `ae17b89`); (b) how far D-50's "and no more" reaches, after three rounds in which recording a fix created a new breach; (c) one unreachable object, an earlier draft of the specimen findings, read by the reviewer and found benign | "A,A,A" | **(a) D-66 covers both messages;** no history is rewritten, and D-66's record is corrected from one to two. **(b) Records of decisions are exempt from D-50's wording rule:** the red-team record, the rulings and options files, and the brief's change log may say what was found and what was ruled. The rule binds everything that describes the project — requirements, risks, the README, code, and documents to come. Whether the process records are published at all remains a SHIP decision. **(c) The object was removed**, on HUM LEAD's word, as under D-50; branch tips, commit count and integrity unchanged. |
| D-71 | 2026-09-19 | The Discovery Report, presented in full; with it the brief's amendments D-10a to D-10c, the parity matrix as frozen (77 rows, 75 counted, P-40 and P-46 excluded by named ruling), the withdrawal of D-55, eleven extension rows (E-18 to E-28) and the post-SHIP check | "APPROVED 4 PLAN;" — with directives for PLAN: "NO LARGE EMBEDEDED CODE BLOCKS in plan, (\"we write code in BUILD, not in PLAN\") it bloats the plan size and ultimately is not testable"; "API function signatures for illustration or ergonomic examples are OKAY"; "I want FULL DIAGRAMS for EVERY MAJOR PART of the ARCHITECTURE […] it will be IMPORTANT for those diagrams to be: Complete; Comprehenaive; Illustrates how the system works at every important level of details (can be multiple diagrams at differing zoom levels to support)" | **DISCOVER is closed; PLAN opens.** The report and everything it listed for ratification are approved. **Standing directives for PLAN:** (1) no large embedded code blocks — code is written in BUILD; function signatures and short ergonomic examples are allowed; (2) a diagram for every major part of the architecture, complete and comprehensive, at more than one level of detail where that helps, kept as living references — cited when questions are asked and updated when decisions change — because so many questions have been asked and answered in DISCOVER that the diagrams must carry them. |
| D-72 | 2026-09-19 | PLAN kickoff — the order of work for PLAN (entry checks; approaches one at a time; architecture with a diagram set at four levels; owed specimens and the fixture; the implementation plan without code bodies; red-team and Plan of Record), and the medium for diagrams: Mermaid in the tracked documents as the source of truth, or FigJam first | "Approved" | **GO on the order of work.** The coordinator had recommended Mermaid-first and reads "Approved" as covering that recommendation; this reading was stated back to HUM LEAD at once, with the invitation to say otherwise, and is cheap to reverse. Also noted: the framework's PLAN procedure asks for "complete code" in plan tasks; D-71 overrides it as a preference — tasks carry file paths, test names and intent, signatures where they help, and the acceptance check. |
| D-73 | 2026-09-19 | PLAN approach 1 — who runs background work (FR-30, risk RS-4): A, the library owns a small pool; B, the host runs the work; C, B as the engine with a default runner on top | "B" | **The host runs the work. The library never starts a goroutine.** Slow work — fetching and decoding tiles, simplifying shapes, computing descriptions — waits in a capped queue where the newest view wins; `Work(ctx)` does one unit of it, blocking and cancellable, on whatever goroutine the host calls it from; `Pending` says how much there is; `Settle(ctx)` is the same loop run inside the library on the caller's goroutine, which is how a one-shot render completes. Finishing work moves the change counter and the call-me-by deadline (FR-25). **What the library still owns:** the caps and cancellation~~, and a limit on how many decodes run at once however many goroutines the host calls from, so the memory peak (NFR-3) does not depend on the host getting it right~~ *[struck by D-84: the limiter was the coordinator's addition, not part of option B; HUM LEAD ruled against it]*. **Accepted with the ruling, as put:** every host writes a pump, and one that does not gets a map that never sharpens; the examples carry a short pump, including one in the first host's framework idiom, outside the library's contract (D-13 keeps the core framework-neutral). **Chosen against the recommendation**, which was C; the argument for B was put as the counter-argument — no library goroutines at all, the most honest fit for the one host that exists, and about ten lines in that host. |
| D-74 | 2026-09-19 | PLAN approach 2 — the style of the overlay contract (risk RS-2): A, plain structs set by id; B, layer handles; C, functional options | "A" | **Plain data.** One struct per overlay shape, handed to the map with a single set call and replaced by setting the same id again (FR-11); removed by id. Presets are ready-made values for a struct's type, with helper constructors so a preset overlay is one call (D-69). Structs may gain fields later without breaking callers, which is what the compatibility promise needs (D-60, NFR-22). **Carried into the design from the argument put against A, not ruled:** the signal that the borrow of old geometry has ended must be hard to ignore, and setting an overlay must say whether it replaced one or created one, so a mistyped id does not pass silently. *[The end-of-borrow signal is ruled in D-86: reported, never waited for, with an opt-in borrow check.]* |
| D-75 | 2026-09-19 | PLAN approach 3 — the dependency allow-list (NFR-9) and who parses untrusted tiles (risk RS-12; contradiction X-5 from the research): A, standard library only; B, a short allow-list for text tables only; C, the proven tile decoder too | "B" | **The allow-list is two modules: `rivo/uniseg` and `mattn/go-runewidth`** — grapheme clusters and the width table the first host already measures with (NFR-8, FR-34). **Everything else is the standard library or the project's own code**, including the vector-tile decoder: it drops unused layers and place-name translations while decoding, keeps coordinates as small integers, and enforces NFR-10's limits before allocating — the control the memory target needs. It is fuzzed (NFR-10), and the proven decoder (`paulmach/orb`) is kept as a test oracle in a separate test-only module that never enters a host's dependency graph. **The layout that follows**, to be reviewed with the architecture: one public package holding the whole contract; an opt-in assets package; internal parts; and the app, the examples, the tile generator and the test oracle as separate modules. The line count given for the decoder (300 to 400) is an estimate, unverified. *[Amended by D-81: the two modules are `go-runewidth` and `clipperhouse/uax29/v2`; the version of the width library the first host pins does not use `uniseg`.]* *[Refined by D-82: the decoder keeps one configured label language, English by default, and drops the rest.]* |
| D-76 | 2026-09-19 | PLAN — whether the contract gains an intent that fits the view to named places and overlays. Raised by specimen 18: a hazard 36 km south of the place fell off a 12-row map, and M1's guard ("the place marker and the condition must share one frame") turned out to be a demand on the view the host chooses, for which the contract had only "fit the world" | "A" | **Add the intent, in v0.1.0.** One call fits the view to named places and overlay ids: it picks the centre and the largest zoom at which everything named lies inside the rectangle, with a margin in cells, using the projection and the two-to-one cell shape the library already owns. Pure arithmetic; no input or output. M1's guard becomes something a test asserts rather than something a host must get right by hand. An extension, not a parity row (E-29). |
| D-77 | 2026-09-19 | HUM LEAD's verdict on the PLAN specimens, viewed on the review page: group M (M1 scenarios 2, 6 and 7 — specimens 17, 18, 19), group N (the painted ground, light and dark, and the dashed track — specimens 20 and 19c), group O (the absolute temperature scale — specimen 21) | "O - looks good / N - looks good / M - looks good" | **All three groups seen and found good.** The visual approaches they show now rest on HUM LEAD's own look: a zone-shape alert with the place on its edge; point hazards told apart by glyph; a dashed stroke for line features with no colour; a painted ground on light and dark; a 17-class absolute temperature scale, including its 256-colour version. **Not answered by this verdict, and not inferred from it:** which light-ground variant is preferred — coloured lines (20a, 16% of cells under 3:1) or black-and-white lines (20d, none). It does not need a ruling: FR-16 already requires every line to meet 3:1 against what it crosses, so the style keeps a line's colour where that colour passes on the cell and falls back to the higher-contrast black or white where it does not. As before, the look was in a browser; the terminal look at colour files is still asked for before PLAN exit. |
| D-78 | 2026-09-19 | HUM LEAD's reply on specimen group P (radar by the first release's own path, specimen 22) and on the rule the coordinator had chosen while drawing it: each cell shows the heaviest rain inside it, not the average (S22-3) | "L looks good - actually like it. Agree with your logic approach (better to overstate than under)" | **The resampling rule is agreed in HUM LEAD's words: better to overstate than to understate.** For an image overlay each cell takes the heaviest class inside it, never the mean; PLAN applies the same principle wherever coarse cells must summarise finer hazard data. **On "L":** the reply came in answer to group P, and its second sentence is about group P's rule, so the coordinator reads "L" as group P and said so at once, inviting correction; if HUM LEAD meant group L — the diverging ramp whose corrected legend was owed a second look — that is good news too, and either reading leaves the other group unanswered. Not inferred: whether "lighter means heavier" reads the right way round. *[Confirmed by HUM LEAD the same day: "correct - I meant group P (the newest one)". The second look at the corrected groups I, J and L is therefore still owed, before PLAN exit.]* |
| D-79 | 2026-09-19 | HUM LEAD's verdict on specimen group Q (specimen 23): a 16-colour hint drawn as D-59 rules — a basemap in colours chosen from the sixteen, with the radar in its no-colour form | "Q - looks good" | **The D-59 mix stands, now seen.** The fall-back ruled with D-59 — treat 16 colours as no colour — is not needed. The 16-colour depth carries its own small basemap palette, chosen by hand (S23-1). |
| D-80 | 2026-09-19 | The implementation plan for v0.1.0 — its four milestones, its re-estimate checkpoint and the unit of its effort estimate — and the reviewers for the PLAN red-team (the nine lenses confirmed for DISCOVER in D-38, pointed at the plan and the architecture) | "1. Milestones good / estimates mean nothing - I want it done right, so I'm willing to wait / use the time that's needed / 2. Red team approved" | **The four milestones stand. The effort estimate is not a constraint and is not to be managed to:** HUM LEAD wants it done right and will give it the time it needs. The estimate stays in the plan as a record of how big the work looked from PLAN, and nothing more; the checkpoint after the first milestone is kept as a **quality** checkpoint with HUM LEAD — does the first working map hold up — not a re-estimate. **The PLAN red-team's reviewers are confirmed.** This also answers the phase lens's first challenge in advance: the plan optimises for correctness, by the owner's explicit word. |
| D-81 | 2026-09-19 | PLAN red-team, PL-Q1 — the dependency allow-list. D-75 named `rivo/uniseg` and `mattn/go-runewidth` on the coordinator's statement that the width library imports the first. Checked against the first host's own module file: it pins the width library at v0.0.24, and every release since v0.0.19 requires `clipperhouse/uax29/v2` instead; the statement was true only of v0.0.16 and earlier | "A" | **The allow-list is `mattn/go-runewidth` and `clipperhouse/uax29/v2`** — the width table the first host measures with, and the one module that table itself requires, which the library also uses for grapheme clusters. One segmenter, the same one the width function uses, so cluster boundaries and widths cannot disagree (NFR-8, FR-34). Nothing is added to the first host's graph: it carries both today. **Amends D-75**, whose other parts stand: everything else is the standard library or the project's own code; the tile decoder is the project's own; the app, examples, tools and test oracle are separate modules. *The error was the coordinator's: a fact given with a ruling and not checked.* |
| D-82 | 2026-09-19 | PLAN red-team, PL-Q2 — parity row P-36 (label language: `name_<lang>`, `name:<lang>`, `name_en`, `name:en`, `name`, `house_num`; *Match*, v0.1.0) against a decoder that drops every place-name translation (D-75) and embedded tiles with translations stripped (D-33) | "A" | **One configured language is kept, English by default.** The decoder keeps `name` and the configured language's keys, and still drops every other language while decoding — which is where the memory saving and the small embedded asset come from. Lookup follows upstream's order, limited to that language; `house_num` is kept as upstream's last resort. **The embedded tiles keep `name` and English**; the growth of the 1.7 MB asset is measured when the generator is built (an estimate of about 1.9 MB was given, unverified). **The label language joins the tile cache key**, because a decoded tile now depends on it; FR-31 is amended to say so. P-36 stays *Match*. Refines D-33 ("translations stripped" becomes "every translation but English stripped") and D-75. |
| D-83 | 2026-09-19 | PLAN red-team, PL-Q3 — how a cell's colour is chosen where two kinds of basemap line share it: parity row P-08's majority vote, or the line-priority rule every earlier specimen had used. HUM LEAD first answered "C" — decide from a specimen; specimen 24 drew the same two views both ways (3.2% and 5.1% of drawn cells differ) | "24b looks better for general purpose - this is not a navigation tool so road accuracy is less important the greography and topogrphy" | **Upstream's majority vote (24b).** Within the basemap a cell takes the majority colour among its lit dots, ties broken by the count among the eight neighbouring cells; across layers FR-12's compositing order still decides, so overlays are untouched; the three exceptions of D-49 stand. P-08 stays as approved; parity stays at 75 rows. **A principle stated with the ruling, for the styles and the thinning profiles (FR-19, FR-20): this is not a navigation tool — geography and topography matter more than road accuracy.** Where the basemap must give something up, roads go first; coast, water and borders last. *HUM LEAD's words are "geography and topography"; the list of layers is the coordinator's reading of them. It first included "terrain", which this release does not have (red-team round 2).* |
| D-84 | 2026-09-19 | PLAN red-team, PL-Q4 — whether the library limits how many tiles decode at once. The coordinator had written such a limiter into D-73's record although option B, as put and as chosen, said the memory peak becomes the host's job; the addition was not labelled as unruled | "B" | **No limiter. D-73 stands as it was put: the host runs the work, and with it the parallelism and the memory peak.** Nothing inside `Work` ever waits on anything but the network. NFR-3's peak line is stated for a **named pump width — two `Work` calls at a time** *(the option as put said "a named pump width"; two is the coordinator's figure, host-settable, not HUM LEAD's number)* — and measured at that width; the documents tell a host author what each further concurrent `Work` can add (about 1 MB for an ordinary tile, more at the input limits). The limiter is struck from D-73's record, FR-30, NFR-3, the diagrams and the plan. **Chosen against the recommendation**, which was to keep the limiter so the peak stayed the library's promise however wide the pump. *The coordinator's error: an addition written into a ruling's record without saying it was not ruled.* |
| D-85 | 2026-09-19 | PLAN red-team, PL-Q5 — default memory caps, and whether the lines of D-48 (live ≤ 4 MB, peak ≤ 8 MB) cover one map or the three the first host plans. Put with the measurements of a throwaway program and with arithmetic, labelled as such, that peak runs to about twice live plus what two concurrent `Work` calls hold | "B - this makes sense for now - we can adjust if we find we have more of a memory requirement, but in this area lean first makes more sense to me." | **The lines cover three maps sharing caches. Lean first.** Default caps: **tile cache 0.5 MB and shape cache 0.25 MB, both shared by every map in the process; images 0.25 MB a map.** All host-settable; a host with room raises them. Accepted with the ruling, as put: the tile cache then holds about one view, so panning back re-decodes unless the disk cache is on; and a 600×400 radar image only just fits — an image over a map's image cap is refused with a typed error that says what size would fit. The caps are revisited if real use shows a need. M4 and NFR-3 are measured with three maps — two at 149×38, one at 69×12 — as well as one. **Chosen against the recommendation**, which was to size the caps for one map and report three. |
| D-86 | 2026-09-19 | PLAN red-team, PL-Q6 — how a host learns that borrowed geometry is free to reuse. Options: A, the return of `Set` or `Remove` is the signal (the call waits for readers); B, a release signal the host must wait on; C, copy small shapes and borrow large ones. HUM LEAD asked which of A and B harmonises with "the host does the work", HUM LEAD's instinct being B; the coordinator answered that B does — every reader of borrowed memory runs inside a call the host itself made, on a goroutine the host owns, so only the host can know or wait — and that A, which it had recommended, would have the library arbitrate between the host's goroutines, against D-73 and D-84. It then shaped B to need no channel or callback | "B as shaped above" | **`Set` and `Remove` never block; they return a plain fact — old geometry released, yes or no.** With one goroutine calling `Set`, `Work` and `Render` there is never a concurrent reader, so the answer is always yes: the simple host cannot get it wrong. With a multi-goroutine pump, whichever of the host's own `Work` or `Render` calls drops the last reader reports the release in its return value, and a query answers "is this geometry still in use" at any time; nothing fires later, so nothing depends on the host continuing to pump. The old shape keeps drawing from the library's **own** simplified copy until the new one is prepared, and the old borrowed memory is never touched again. **An opt-in borrow check** fingerprints borrowed geometry at hand-in and re-checks it whenever a job reads it; a change while in use raises a warning naming the overlay — off by default for its cost, on in every example and in the tests. Examples never discard the result; the race-detector test runs as a separate process. **Accepted with the ruling:** a multi-goroutine host that ignores "not yet released" gets a silent data race unless the borrow check or the race detector is on. Replaces D-74's carried note that the signal be "hard to ignore". **Chosen against the first recommendation (A).** |
| D-87 | 2026-09-19 | PLAN red-team, PL-Q7 — whether water hides what is drawn over it. The render diagram said water "owns its cells" under any field or image, which for radar would hide rain over a lake or the sea — against the problem statement and against D-78. What HUM LEAD had seen and approved was by kind: temperature stops at the shore (D-32, after seeing the field bleed across Lake Michigan in specimen 01a); radar was always drawn over water (specimens 15, 22, 23) | "A" | **By kind of overlay. Images — radar, precipitation — are never masked by water; the coast stays visible as the water's outline. Scalar fields — temperature — are masked by water, as approved in D-32.** A host can flip either, per overlay. This is what every approved specimen shows, now written down correctly; the single rule in the diagram was the coordinator's error. The compositing order (FR-12) states both defaults, and each is tested. |
| D-88 | 2026-09-19 | PLAN red-team, PL-Q8 — the colour-vision threshold behind D-53, "proposed: 10" since DISCOVER and never ruled; put with the method, because the accessibility reviewer showed the same colours score 11.1 or 2.9 depending on how the simulation is applied | "A" | **A difference of at least 10 between every pair of classes in a ramp — not only neighbours — and between every class and the ground it is drawn on.** Method, pinned: colour-vision deficiency simulated at full severity for protanopia, deuteranopia and tritanopia (Machado, Oliveira and Fernandes, 2009) **in linear light**; difference measured in CIE Lab with a D65 white by the 1976 formula; the worst of the three simulations and normal vision counts. Required of the library's presets and defaults at every supported depth; reported, never refused, for a host's colours (D-53). Put to HUM LEAD with its limits: 10 is a convention, the 1976 formula is uneven, nobody with a colour-vision deficiency has looked at these ramps, and it is the threshold that passes what was already built. |
| D-89 | 2026-09-19 | HUM LEAD's verdict on specimen group S (the corrected ramps: radar on a light ground, 22d and 22e; the retuned temperature scale, 21a to 21c) and the second look, owed since DISCOVER's red-team round 2, at the corrected groups I (no colour: alert areas and wind — specimen 13a redrawn with its place marker) and J (the colour-vision-safe ramp — specimen 14 with its legend and text colour corrected) | "S looks fine - 22d as well / I looks fine / J looks fine" | **Group S, including 22d, seen and found fine:** the darker-is-heavier radar ramp for light grounds and the retuned temperature scale now rest on HUM LEAD's own look. **The second look at groups I and J is done**, on the corrected files. **Not mentioned, and not inferred: group L** (the diverging ramp of specimen 16, whose legend was corrected) — that second look is still owed before PLAN exit, as is a look at the colour specimens in HUM LEAD's own terminal. *[Later the same day HUM LEAD added: "L looks fine as well". The second look at group L — specimen 16 with its corrected legend — is done; D-54 now rests on the corrected files. Still owed: a look at the colour specimens in HUM LEAD's own terminal.]* |
| D-90 | 2026-09-19 | PLAN red-team round 2, P2-Q1 — what gives when the maps on screen need more tiles, or more simplified shapes, than a shared cache's cap (D-85). As written, a tile being drawn could be evicted, so two maps on different views would evict each other's tiles every frame and fetch without end. Put with new measurements: what one 149×38 view draws is 0.33 MB (Gulf coast, zoom 6) to 0.80 MB (Midwest, zoom 5) in kept form, single tiles 0.06 to 0.24 MB, against a 0.5 MB cap; and with the admission that the coordinator's "3.56 MB measured" was the Midwest variant, unlabelled, with its view's tiles outside the filled cache | "A" | **Need first, cap second.** What a live view is drawing is never evicted. Spare tiles are kept only in whatever room is left under the cap. When need alone exceeds the cap the cache holds exactly the need, and says so: one warning, and a figure the host can read. Shapes follow the same rule; a simplified shape is drawn from the host's memory (FR-11's fallback) only when it is larger than the **whole** shape cap — the earlier "quarter of the cache" rule — written into FR-11 and NFR-3 by the coordinator in DISCOVER and ratified with the requirements as a whole, never ruled on by itself — is withdrawn for both caches. **Accepted with the ruling, as put:** the 4 MB line then describes the fixture rather than guaranteeing a limit — three maps on the fixture region stay at about 3.0 MB, three on the Midwest view about 3.3 MB, three on three different dense views about 4.9 MB live, over the line, reported and not prevented. NFR-3 is gated on three maps over the fixture region; a run with three different views is recorded and does not gate. |
| D-91 | 2026-09-19 | PLAN red-team round 2, P2-Q2 — the temperature preset beside a light ground. D-88 requires every class to differ by at least 10 from the ground it is drawn on; the coordinator had checked the radar ramp against both grounds and the temperature scale against neither. Recomputed: on the light ground the two bands either side of freezing are 5.0 and 5.5 from it, at both depths; on the dark ground the closest class is 12.1. Put with where the ground actually shows beside a field — the edge of a grid smaller than the view, no-data cells, and the legend's swatches — and with a feasibility search made that day | "A" | **The temperature preset gets a light-ground variant, as radar has:** the library's defaults for the temperature tokens differ by the ground in effect. The alternative put — scoping D-88's ground rule to patchy overlays and drawing a field's edge instead — was not chosen, so D-88 stands as ruled, with no exception. **What the question said of the variant and what was then found:** the question said only the two freezing bands would change, with a thin margin of 10.4 on the cool side. The finished search moves **three** bands — −10 to −5 °C, −5 to 0 °C and 0 to +5 °C — keeps hue on its side of freezing, and loses no margin: every pair 12.2 apart in truecolor and 11.1 at 256 colours, as on the dark ground, and every class at least 13.8 (13.0 at 256) from the light ground. Specimen 21d; to be seen by HUM LEAD. |
| D-92 | 2026-09-19 | PLAN red-team round 2, P2-Q3 — a shape so large that it is drawn straight from the host's memory (its simplified copy will not fit the shape cache) is replaced. FR-11 says a replaced shape never vanishes; D-86 says a one-goroutine host is always told "released: yes". For these shapes both cannot hold, and the coordinator had resolved it silently in D-86's favour — the shape was not drawn until its replacement was ready — without labelling that as unruled. Options: A, keep drawing the old shape and answer "not yet released" even to a one-goroutine host; B, stop drawing until the new one is ready, as the contract then read; C, have `Set` itself build the cheap index such a shape needs, so the new one draws on the next frame and the old is released at once | "C" | **`Set` builds the index.** For a feature overlay large enough that it could ever need to be drawn from the host's memory, `Set` makes one linear pass over the geometry and builds bounding boxes over runs of 64 vertices, before it returns. The new shape can be drawn from the host's memory on the very next frame; the old geometry is released at once. Both rulings then hold as written: a replaced shape never vanishes (FR-11), and a one-goroutine host is always told "released: yes" (D-86). **Accepted with the ruling, as put:** `Set` does real work on the host's interface goroutine for these shapes — a few milliseconds at the 2,000,000-vertex cap by arithmetic, unmeasured — and because `Set` cannot know a shape will need this until it has been simplified once, the pass runs for every shape above a vertex count whether it turns out to be needed or not. **Set in PLAN after the ruling, not ruled:** that count is the number of vertices whose unsimplified form and run index together fill the shape cache — 30,303 at the default cap *(first recorded here as 31,250, which left out the index's own bytes; corrected after red-team round 3)*; at or below it the fallback can never be needed. The run length of 64 vertices is also the coordinator's, from the constants. **Chosen against the recommendation (A).** |
| D-93 | 2026-09-19 | PLAN red-team round 2, P2-Q4 — HUM LEAD's verdict on specimen group T of the review page: the corrected light-ground radar ramp (22f, which replaces the 22d approved in D-89 after round 2 found it got lighter from class 2 to class 3) and the light-ground variant of the temperature preset ruled in D-91 (21d; 21e shows the defect). The question also told HUM LEAD that 21d's colours pass on the dark ground too and so could be the only temperature scale, and said that unless HUM LEAD asked for that, D-91's two ramps would stand | "22f is fine / 21d is fine" | **Both corrections are seen and fine**, on the review page. HUM LEAD did not take up the single-ramp idea; as the question said, **D-91's two sets of colours stand** — this is not a ruling against the idea, which can be raised again. **Still owed, as for every colour specimen since D-77:** a look at the `.ans` files in HUM LEAD's own terminal; the review page shows them as a browser draws them. |
| D-94 | 2026-09-19 | PLAN red-team round 2, P2-Q5 — the two MAPSCII additions that DISCOVER's research marked worth adding and that were carried, with no disposition, through two red-team rounds and the Discovery Report: (1) per-layer label margin and clustering of nearby points of interest; (2) loading a style by file path. Put with the fact that a user's style is already accepted as bytes in v0.1.0 (FR-20) but the app had no flag to use it | "A" | **The app gains `--style PATH`:** it reads the file and hands the bytes to the library, which still never reads a style file itself. **Per-layer label margin and clustering are not taken up for v1**; the integration review (D-60) may reopen them if the first host's views need them. Both additions now have a disposition. **Accepted with the ruling, as put:** at street zoom in a city, a general map library without clustering shows a clutter of point-of-interest marks, and retrofitting clustering into label placement later costs more than designing for it now. |
| D-95 | 2026-09-19 | PLAN exit — the Plan of Record, presented in full after three red-team rounds. It listed six matters that were the coordinator's and not rulings, for HUM LEAD to accept or strike with the plan: only `Work` and `Settle` report the end of a borrow; the run index is built over 30,303 vertices, in runs of 64; the memory peak is stated two `Work` calls wide; one temperature colour moved by less than the eye can tell, and three small inversions left at 256 colours; the alert preset's colours designed in BUILD with HUM LEAD's look inside M-B; twenty-two ratified requirement rows revised in PLAN. It said HUM LEAD might order a fourth red-team round | "APPROVED; Go 4 BUILD" | **The Plan of Record is approved, with the six matters as listed; none was struck. No fourth round was ordered. PLAN is closed and BUILD opens.** BUILD starts at work package WP-00, test first, and stops at milestone M-A for HUM LEAD's look at the first working map. At BUILD's door: Go is restored to the project's language declaration (D-20) and the first code-quality check runs on the scaffold. **Still owed by HUM LEAD, none blocking:** a look at the colour specimens in HUM LEAD's own terminal, and that terminal's name and font, before reference frames are frozen; the alert colours, inside M-B. |
| D-96 | 2026-09-19 | BUILD, B-Q1 — the code-quality gate asks for an average of two invariant checks a function in every package; `internal/textsafe`'s cleaning functions are total — every input has a defined output, so there is no error to return and nothing to assert — and the package scored 0.25. The same will recur in other packages of pure arithmetic. Put with the framework's exemptions file (an entry is keyed by package, needs a written reason, stays visible in every run), the first host's practice of having HUM LEAD ratify each entry, and the fact that the counter could be satisfied by reshaping predicates into chains of guard clauses that assert nothing | "A" | **Exemptions are made one package at a time, each ratified by HUM LEAD.** The coordinator writes the reason; HUM LEAD ratifies; the entry is recorded with its date in the exemptions file and in the BUILD log. Real bounds are still added wherever they exist. **The first entry, `internal/textsafe`, whose reason was set out inside option A, is recorded as ratified with this ruling**; HUM LEAD was told so plainly, and may withdraw it. Not chosen: a standing rule letting the coordinator exempt without showing each entry (B); no exemptions, with code reshaped to satisfy the counter (C). |
| D-97 | 2026-09-19 | BUILD, B-Q2 — under D-96, the ratification of a code-quality exemption for `internal/fault`: invariant density 0.83 across 6 functions against a bar of 2.0. The entry was put verbatim: the package is an error type and two closed lists; an error type cannot itself return an error; every real check is present; correctness is held by tests against the contract's own text. Put with the counter-argument that two exemptions in the first two real packages is a pattern | "A" | **The exemption for `internal/fault` is ratified as written.** HUM LEAD raised no objection to the `internal/textsafe` entry recorded with D-96, having been told plainly that it could be withdrawn; it stands. The pattern the counter-argument named is watched: the BUILD log's table shows every exemption, and REVIEW's red-team challenges each on its merits. |
| D-98 | 2026-09-19 | BUILD, B-Q3: under D-96, the ratification of a code-quality exemption for `internal/work`: invariant density 0.88 across 32 functions. The entry was put verbatim: a queue under one lock, whose invariants are relations between structures and not preconditions on arguments; every argument that can be wrong is checked; correctness held by 25 tests under the race detector. Put with what BUILD had learned of the counter (it counts only leading, call-free guard clauses) and with the counter-argument that this is the third exemption in six packages and the first for a package that is not a set of total functions, so that it stretches the premise D-96 was put on | "A" | **The exemption for `internal/work` is ratified as written.** HUM LEAD was told plainly that the entry goes beyond D-96's "total functions" and ratified it without restating the premise; **D-96 therefore stands as: one package at a time, each on the reason written for it, each ratified**, not as a rule about total functions only. REVIEW's red-team challenges each entry on its merits. |
| D-99 | 2026-09-19 | BUILD, B-Q4: under D-96, a code-quality exemption for the `assets` package: invariant density 1.00 across 5 functions. Put with the evidence: three of the five functions return an embedded text, take no argument and cannot fail; every argument that exists is checked (a zoom past 3, a column or row off the grid, an absent file); content is held by tests that re-hash every tile against the list. Put with three options - exempt; reshape to one `Text(which)` call that can refuse a name; split existing checks to reach the count - and with the counter-argument that this is the fourth exemption in about nine packages, and that a bar waived this often stops being one | "A" | **The exemption for `assets` is ratified as written.** The public surface stays `Tile`, `Pin`, `Hashes`, `Notice`. Removable if the package gains functions that take arguments. REVIEW's red-team challenges each entry on its merits. |
| D-100 | 2026-09-19 | BUILD, B-Q5: under D-96, a code-quality exemption for `internal/tiles`: invariant density 1.33 across 86 functions. Put with the evidence, by group: about 30 entry points that take arguments meet the bar; seven error constructors can hold no check; about 20 helpers are called only with a lock held and arguments already checked; about 12 are one-line formatters; and the checks that matter most - what was opened is what was looked at, a link is not followed, the TileJSON's depth and size, the address rules - sit after a call, where the meter does not count, and are held by 40 tests and a fuzz target that found a real gap. Put with three options - exempt; reshape by folding some 25 helpers into their callers; add checks that can never fire - and with the counter-argument that this is five packages of ten, and that a package touching the file system and the network is the one to hold the bar on if it is held anywhere | "A" | **The exemption for `internal/tiles` is ratified as written.** Removable if the meter comes to count checks inside a function's body. HUM LEAD was also told, in the same message, of one coordinator matter - the embedded tile is a job of its own and not the third step of one job - and raised no objection; **that silence is not recorded as consent (D-39)**, and the matter stays open to challenge at REVIEW. |
| D-101 | 2026-09-19 | BUILD, B-Q6: under D-96, a code-quality exemption for `internal/colour`: invariant density 0.56 across 18 functions. Put with the evidence, by group: four functions of arithmetic on any colour, for which every 8-bit colour is valid input; seven lookups that answer "no such token" with false, each checking the token's range; seven that take no argument that can be wrong. Put as the category D-96 was first ruled on, with `internal/textsafe` as the precedent. Put with three options - exempt; make the arithmetic return errors no input can produce; add checks that can never fire - and with the counter-argument that this is six packages of eleven, that REVIEW's first challenge will be that the bar is waived by habit, and that HUM LEAD might prefer to settle it once with a standing rule, which D-96 deliberately did not grant | "A" | **The exemption for `internal/colour` is ratified as written.** Removable if the package gains functions that can refuse their input; the ramp checker of task 08.7 may add some. **No standing rule was made:** the invitation to make one was put and the answer was to this package only, so D-96 stands as it was - one package at a time, each ratified. |
| D-102 | 2026-09-19 | BUILD, B-Q7: the contract's closed list of 24 error kinds has none for a user's style file that is refused, nor for a palette that names a token the library does not have. Put with the evidence that each nearby kind names a different thing a host handed in, so a host that switches on kinds would tell its user the wrong thing; that `over-limit` already fits a style's size and nesting; and that nothing is released, so a new kind costs document edits only. Three options: two new error kinds; one new error kind and an unknown token as a warning, the rest of the palette taking effect; no new kind, two existing kinds each coming to mean two things. Put with the counter-argument that a warning is easy to miss, and a host whose override does nothing because of a typing mistake may never look | "B" | **One new error kind, `malformed-style`, and one new warning kind, `unknown-token`.** The error list is 25 kinds and the warning list 14. A style's size and nesting stay `over-limit`. A palette with an unknown token still themes the map with its other tokens (D-53's "reported, never refused"). Both lists are changed in the contract and the errors page first, and in code after the test that compares them failed. |
| D-103 | 2026-09-19 | BUILD, B-Q8: under D-96, a code-quality exemption for `internal/style`: invariant density 1.41 across 37 functions. Put with the evidence, by group: six entry points meet the bar; the parser's eleven steps check after a JSON type assertion, which is a call and is not counted; nine functions of filter evaluation take any attribute set; seven builders and error constructors can hold no check; four are one-liners. Put with the observation that the ban on recursion and the complexity limit each split code into more, smaller functions, which lowers this figure. Three options: exempt now; exempt only after the fuzz target of task 08.20 is written and has run clean, pulling that task from the overlay milestone into M-A; merge helpers until the count passes, which breaks the complexity limit. Put with the counter-argument that M-A never loads a user's style, so the fuzz work hardens a path the milestone does not use | "B" | **`internal/style` is exempted, the condition having been met first:** `TestStyleLimits` and `FuzzStyle` were written (task 08.20, now part of M-A), and the fuzz target ran two minutes, 12.8 million inputs, with no failure. It runs for 60 seconds in every full gate from here on. Removable if the meter comes to count checks after a call. |
| D-104 | 2026-09-19 | BUILD, B-Q9: under D-96, a code-quality exemption for `internal/render`: invariant density 1.24 across 51 functions. Put with the evidence, by group: ten entry points meet the bar; fourteen rasterisers and vote helpers take any integer as a dot, with bounds enforced once in the function they all draw through; fifteen painter and composer steps are called with checked arguments; twelve are accessors, names and error constructors. Three options: exempt now; exempt only after a fuzz target for the painter is written and has run clean; add bounds checks to every rasteriser, repeating the one that exists, in the hottest loops. Put with the counter-argument that the decoder is already fuzzed and bounds what reaches the painter, and the clipping test already drives the rasterisers with extreme values | "B" | **`internal/render` is exempted, the condition having been met first:** `FuzzPaint` feeds arbitrary tiles - every 16-bit coordinate, parts of any length, every kind, uncleaned names - and arbitrary views to the renderer under both the built-in style and a user's; it ran two minutes, 924,000 frames, with no panic, no row of the wrong width and no control character outside a colour sequence. It runs for 60 seconds in every full gate from here on. Removable if the meter comes to count checks inside a function's body. |
| D-105 | 2026-09-19 | BUILD, milestone M-A: HUM LEAD's look at the first map the library drew (specimen 26), put as five numbered matters. Matters 1 and 2: the look itself - the painted dark ground, water as cell background, coasts, country borders, names, every colour a default the coordinator chose; and the built-in style's starting zooms | "1. Look is fine" · "2. Style starting zooms are fine as defaults for tuimaps in general, we'll set specific zooms as default in watchpost" | **The look of the basemap and the built-in style's starting zooms stand as the library's defaults.** The first host sets its own. *Noted for WP-12: a host needs a way to set those zooms; whether that is a user's style, which exists, or a lighter call, is not decided.* HUM LEAD was also asked for the terminal's and font's name; **the reply does not give them, and that silence is not an answer (D-39): still owed.** |
| D-106 | 2026-09-19 | BUILD, M-A, matter 3: the name of the option that passes the embedded tiles, which the coordinator had called `WithEmbedded` and said was HUM LEAD's to change | "3. withEmbedded doesnt make much sense to me, id rather is be something like - 'tuiMapsEmbed(asset.Tile, assets.MaxZoom) to make it super clear" | **The option is `tuimaps.Embed(assets.Tile, assets.MaxZoom)`.** HUM LEAD's words were "something like"; in Go a call carries its package's name, so `tuimaps.Embed(...)` is how `tuiMapsEmbed(...)` reads at the place it is written, and a function named `TuiMapsEmbed` would read `tuimaps.TuiMapsEmbed`. The reading is the coordinator's, said to HUM LEAD in the same turn, and the name stays HUM LEAD's to change. |
| D-107 | 2026-09-19 | BUILD, M-A, matter 4: a short vertical line at the world's western edge near Antarctica (S26-4), reported and not fixed | "4. I noticed, fix approved" | **Fixed, test first.** The real zoom-0 tile's ocean polygon has an edge down the antimeridian that starts one unit inside the tile, so the rule "an edge along the tile's border is not a coast", which wanted both ends on or past the side, let it through. An edge now counts when both ends are within 1/2048 of the extent of the side. |
| D-108 | 2026-09-19 | BUILD, M-A, matter 5: the data notice's sentence offering the embedded tiles under the same Open Database License as their source, flagged in WP-04 as a licence statement made in HUM LEAD's name; and the `landcover` layer, kept and undrawn | "5. License is fine - we'll also add a credit in Watchpost's about - which is what consuming apps should also do" | **The data notice stands as written.** A consuming application also credits the data in its About, as the first host will; the documents for hosts say so. The credit line drawn on the map stays: an About alone is outside what the data's attribution guidelines allow, because a person must go looking for it (research AI-2, section 4). **The reply does not speak to `landcover`; that is not an answer (D-39) and stays open for REVIEW.** |

## Summary of rulings D-11 to D-37

*This table was written at D-37 and is kept as written. Rulings D-38 to D-108 are summarised in the table that follows it; where a later ruling changed a line here, the line says so.*

| Area | Settled |
|---|---|
| Parity | Behavioural, against the code at `3b96072`; defect ledger approved and closed (D-11, D-37). Rivers, parks and airports drawn as flagged extensions (D-12). |
| Deliverables | Core library, released standalone app, headless render; MIT; consumed by the first host as an imported package (D-13). |
| Overlays | All five input shapes in v1, radar included (D-14). The host fetches; the library draws (D-15). The library simplifies and clips, cached, off the drawing path (D-16). |
| Input | Pointer operations in, with a keyboard equivalent for every one — the input parity rule (D-17). |
| Basemap | Must keep working without the network; freshness is not a goal; dependency-free PMTiles reader in v1 (D-18). OpenFreeMap is the default network source, replaceable, never bulk-downloaded (D-21) *(refined by D-65: the default of the app and the examples; the library connects to nothing until a host names a source)*. Embedded tiles in a separate opt-in package (D-27): zoom 0–3, translations stripped, 1.7 MB, generated by a committed script from a pinned planet file (D-33). |
| Module and git | `github.com/branden-thompson/go-tuimaps`; local only until SHIP (D-19). Code-quality gate declared at BUILD entry (D-20). `main ← release/v0.1.0 ← feature/*`, PR-only to `main`, all merges by PR from the first release (D-28). |
| Platform | Pure Go, CGO off, enforced by a five-target cross-compile (D-22). macOS / Linux / Windows; UTF-8; from 69×12 cells; SSH-safe; braille and block both first-class *(refined by D-42: braille is the default, block is opt-in)*; legible at four colour depths (D-23). |
| Look and compliance | Fresh styles written against OpenMapTiles; upstream's files never copied; renaming shim not ported (D-24). Credits reported by the library, on-map line on by default, host may place its own (D-25). Host-supplied palette, no design-system dependency, runtime swap without re-parsing (D-26). Overlay data ranges are themeable palette roles that must stay ordered, distinct and readable; placed images are re-coloured by the library through a required colour-to-intensity table; a legend is required (D-36). |
| No colour and reduced colour | With no colour the form follows the kind of data: contours for smooth fields, block shades for radar and precipitation, basemap thinned (D-34, D-35). The library owns a ramp per colour depth; automatic downgrade fails for overlays (S10). Precipitation with radar is the most common view (D-35). |
| Metrics | M4 ≤ 8 MB added, byte-capped caches, near-zero cost for unchanged frames, flat heap — a target, confirmed by measurement at PLAN exit (D-29). M2 ≤ 1 s warm / ≤ 3 s cold, never blank — likewise (D-30). |
| Still open, by design | keyboard zoom-target mechanism, dependency posture (PLAN) · *colour output depth was listed here; FR-17, D-44 and D-59 have since settled it* · every visual approach, from specimens (D-31). |

### Rulings D-38 to D-108

| Area | Settled |
|---|---|
| Review | Personas for the DISCOVER red-team confirmed (D-38). Three earlier rulings clarified in HUM LEAD's own words (D-39). |
| Specimens | Every group seen by HUM LEAD on the review page (D-40). The block renderer's roads are hard to tell apart (D-41); thinned, it is acceptable as an opt-in, braille is the default (D-42). The diverging temperature ramp is fine (D-54). |
| Metrics | M1 is judged by HUM LEAD against a computed answer key over seven scenarios, and gates SHIP on the library; the live session in the host is the host's metric (D-43). The 8 MB test uses a typical-day fixture, with the worst case tested separately (D-48). |
| Releases | v0.1.0 is the basemap plus features, images and scalar grids; then integration into the first host at its v0.17.0; then the remaining shapes (D-44). "0.01.0" reads as v0.1.0 (D-51). Image loops are in v1, built after v0.1.0, designed into the contract now (D-47). |
| Images | A colour-to-intensity table is required with every placed image, and the project ships a tested example (D-45). |
| Tiles | OpenMapTiles is the only schema v1 reads; the schema stays a seam (D-46). |
| Parity | Six corrections to the frozen matrix approved; denominator 70 (D-49) — 75 since D-61 split five rows. |
| Accessibility | A plain-text description of the view, as data, in v0.1.0 (D-52). Default ramps must be colour-vision-safe; themed ramps are checked and reported (D-53). ~~Temperature is not themeable; the rest is (D-55)~~ *withdrawn by D-69*. Flash slowed, reduce-motion added, nothing hidden on a timer (D-56). |
| Hygiene | Sole-author commits; no tool-generated trailers or watermarks. Publish only from a fresh clone. Framework command names replaced with plain descriptions (D-50). |
| Round 2 (D-57 to D-66) | The first release stays braille-only; the host decides whether the map is drawn and the text description is the fallback (D-57). A minimal archive reader, internal and fuzzed, for the tile generator (D-58). A 16-colour hint draws line work in 16 colours and ramps in their no-colour form (D-59). Breaks only at a minor version, listed and guarded; a written integration review and HUM LEAD's GO before the remaining shapes (D-60). Five partial parity rows split; denominator 75, 62 in v0.1.0 (D-61). Temperature's scale is fully absolute and library-owned (D-62) — since D-69, as the definition of the temperature preset. A safe-ramps setting and a semantic-token palette contract (D-63). The ground is painted by default, with an opt-out (D-64). The library connects to nothing until a host names a source (D-65). Two commit messages stand as written (D-66, D-70). |
| Round 3 (D-67 to D-70) | M1 in two parts, both of which must pass: the frame alone, to its own resolution, and the description, exact (D-67). Flat days accepted under the absolute temperature scale (D-68). **Presets, not locks:** the library takes a type, its scalars, its colours and its data for any overlay, and ships temperature, radar and precipitation, wind and alert areas fully defined; any preset may be overridden; D-55 withdrawn (D-69). Records of decisions are exempt from D-50's wording rule, which binds everything that describes the project (D-70). |
| Phase exit (D-71) | The Discovery Report approved, with everything it listed for ratification. Directives for PLAN: no large embedded code blocks — signatures and short examples only; a diagram for every major part of the architecture, complete, comprehensive, at more than one level of detail, kept as living references. |
| PLAN approaches (D-72 to D-76) | The order of work for PLAN, Mermaid diagrams in the tracked documents as the source of truth (D-72). The host runs background work; the library never starts a goroutine (D-73). The overlay contract is plain data: one struct per shape, set by id, presets as ready-made type values (D-74). Two allowed dependencies, for text tables only; the tile decoder is the project's own, fuzzed, with a proven decoder as a test-only oracle (D-75). An intent that fits the view to named places and overlays (D-76). The PLAN specimens — every M1 scenario, the painted ground, the dashed stroke, the 17-class absolute temperature scale — seen by HUM LEAD and found good (D-77). Radar by the first release's own path liked, and the rule agreed that a cell shows the heaviest rain inside it: better to overstate than to understate (D-78). The 16-colour mix seen and found good (D-79). The plan's milestones approved; estimates are not a constraint — done right over done fast; PLAN red-team reviewers confirmed (D-80). |
| PLAN red-team (D-81 to D-89) | The allow-list is `go-runewidth` and `uax29` (D-81). One configured label language, English by default, joins the tile cache key (D-82). A cell takes the majority colour of its dots, as upstream; geography and topography over road accuracy (D-83). No decode limiter: the memory peak is the host's, stated two `Work` calls wide (D-84). Lean default caps; the memory lines cover three maps (D-85). The end of a borrow is reported, never waited for (D-86). Water masks a scalar field and never an image (D-87). The colour-vision threshold is 10 for every pair and against the ground, method pinned (D-88). The corrected ramps seen and found fine; the second look at groups I, J and L done (D-89). Specimen group S and the second look at I, J and L found fine (D-89). |
| PLAN red-team, round 2 (D-90 to D-94) | What a live view draws is never evicted; a cache over its cap because of need says so; the "quarter of the cache" rule is withdrawn (D-90). The temperature preset gets a light-ground variant, as radar has (D-91). `Set` itself indexes a shape large enough to be drawn from the host's memory, so a replaced shape never vanishes and a one-goroutine host is always told "released" (D-92). The corrected light-ground radar ramp and the temperature variant seen and found fine (D-93). The app gains a style-path flag; per-layer label margin and clustering are not taken up for v1 (D-94). |
| PLAN exit (D-95) | The Plan of Record approved, with the six matters it listed as the coordinator's; no fourth red-team round; BUILD opens at WP-00 and stops at M-A for HUM LEAD's look. |
| BUILD (D-96 on) | A package of total functions may be exempted from the two-checks-a-function rule only one package at a time, each entry ratified by HUM LEAD (D-96). Ratified so far: `internal/textsafe` (with D-96), `internal/fault` (D-97), `internal/work` (D-98), `assets` (D-99), `internal/tiles` (D-100), `internal/colour` (D-101), `internal/style` (D-103) and `internal/render` (D-104), both only after a fuzz target ran clean, each on the reason written for it, not on a rule about total functions only. |
| BUILD, the contract's lists | One new error kind, `malformed-style`, for a user's style file that is refused; one new warning kind, `unknown-token`, for a palette entry the library does not have - the rest of the palette takes effect. 25 error kinds and 14 warning kinds (D-102). |

