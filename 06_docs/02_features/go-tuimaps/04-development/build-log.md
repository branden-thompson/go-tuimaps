# BUILD log — go-tuiMaps v0.1.0

Up: [implementation plan](implementation-plan.md)

| Field | Value |
|---|---|
| Phase | BUILD, opened 2026-09-19 by HUM LEAD's approval of the Plan of Record (D-95) |
| What this is | One row for each plan task as it is finished: the commit, what the failing test was, and anything learned that the plan did not say. Where a task changes the design, the diagram changes in the same commit (D-71) and the row says which |
| Gate for every row | Tests pass with the race detector and on the floor toolchain (Go 1.25.0); the code-quality check reports no finding in changed code; the structure check is 18 of 18 |

## WP-00 — Scaffold and gates

| Task | Commit | The failing test, first | Learned |
|---|---|---|---|
| 00.1 BUILD-entry checklist | `828d712`, `5ccec3d` | — | Go restored to the language declaration (D-20). First code-quality check: every tool ran; the only findings were the invariant-density rule on three placeholder commands. **Decision, the coordinator's:** the placeholders were removed and not padded with checks that assert nothing — each command arrives with its own work package; its module file stays. Floor toolchain 1.25.0 is on this machine and runs the tests with the race detector |
| 00.2 Module layout; no `replace` | `828d712` | `TestModuleHasNoReplace`, `TestReplaceLinesFindsEveryForm`, `TestWorkspaceFileIsIgnored` — failed to compile, then failed on the missing module files | Six module files. The workspace file is ignored before it exists |
| 00.9 The fixture is pinned | `8524ee8` | `TestFixturePinned` and five more — failed to compile | The code-quality check's bounded-loop rule refuses a scanner loop; ranging over the file's lines is bounded by the file. `internal/testkit` holds the loaders |
| 00.3 The dependency allow-list | this commit | `TestDisallowedModulesFindsStrangers`, `TestAllowList` — failed to compile | The checker is tested with planted strangers — an older major version of an allowed module, a path that only starts like one, a replacement arrow — because today the real graph is empty and a test of it alone could not fail. Runs with the workspace file switched off (D-81) |
| 00.10 The scene types | this commit | `TestSceneImportsNothingHere`, `TestTileIDValidate`, `TestTileIDParent`, `TestJobKindValidate`, `TestJobIsSatisfiedByAPlainStruct` — failed to compile | **Only what is certain today is here:** a tile's identity, the job kinds, the job interface. The decoded tile and the prepared overlay join `scene` in WP-03 and WP-10, test first, their fields driven by those tests — not guessed now. A new constant, the deepest tile zoom addressed (22), is in the constants file. The density rule told apart "kind never set" from "not a kind", which is a better error |
| 00.4 The leak check | this commit | `TestLeakCheckFindsAGoroutineInsideTheLibrary`, `TestLeakCheckIgnoresGoroutinesOutsideTheLibrary` — failed to compile | A goroutine counts as the library's by the frames it is *running*, never by who created it: the network stack's goroutines stay out even when a library call started them. A goroutine on its way out gets 200 ms before it is called a leak |
| 00.5 The loopback-only dial hook | this commit | `TestLoopbackOnlyRefusesAPublicAddress` and three more — failed to compile | The check runs on the address actually being connected to, after name resolution and before any packet; names are refused outright. Tested against documentation-range addresses, so the tests themselves send nothing. The helper that installs it for a whole test binary comes with the static check that demands it (00.11) |
| 00.6 The blocking transport | this commit | `TestBlockingTransportNeverReturnsUntilCancelled`, `TestBlockingTransportRefusesAMalformedRequest` — failed to compile | Returns the context's own cause, so a test can tell a cancel from a deadline |
| 00.11 The static rules | this commit | `TestEveryPlantedViolationIsFound` (18 planted violations, one per case), `TestATestPackageWithoutTheDialHookIsFound`, `TestTestFilesAndTheTestKitMayStartGoroutines`, `TestACleanTreePasses`, `TestCheckRefusesBadArguments`; and `TestStaticRules` over the library itself — all failed to compile | Built as a tested Go package, `internal/rules`, not a script, so every rule is shown to fail on a planted violation. **On its first run over the real tree it found one:** the scene package's tests were not under the loopback guard. Every test package now calls `testkit.Main` from `TestMain`. **Wider than the plan's wording, the coordinator's choice:** `time.Sleep` counts as a clock of the library's own, alongside timers, tickers and after; and the L1 rules "only fetch opens a connection" and "only textsafe imports third-party code" are enforced here as import rules. The code-quality check pushed the design: no package-level lookup tables, and each decision (`judgeImport`, `judgeSelector`) a small function with its preconditions checked |
| 00.7 The local gate · 00.12 It fails when it should | this commit | `TestGateIsGreenOnAPassingTree`, `TestGateFailsWhenOneModuleFails`, `TestGateFailsWhenTheWorkspaceFileIsTracked` — run the gate's quick legs over planted repositories; failed because there was no script | `scripts/gate`: every module, vet, tests with and without the race detector on the floor toolchain, fuzz 60 s a target, vulnerability scan, licence files, five-target cross-compile with the C toolchain off, tests on the second architecture (amd64 under emulation here), a throw-away workspace file removed on exit. **Its first full run found three things the plan had not said.** (1) A module with no packages yet makes `go vet` and `go test` fail: such a module is now named and called empty — a test holds that. (2) **On the floor toolchain the vulnerability scan reports 22 standard-library vulnerabilities, every one fixed in a later patch.** The floor is there to keep a newer API out, not to build releases with; the scan now runs on the machine's newest toolchain and names it. The coordinator's decision, recorded here and in L2 Gates. (3) The repository had no licence file; MIT, as ruled (D-13), added. A module of this repository is covered by the repository's licence. Full run: about 70 s, green. What this machine cannot run is printed as NOT RUN, never counted as passed |
| 00.8 Lint: exported names documented | this commit | `TestUndocumentedExportedNamesAreFound` (13 cases), `TestOnePackageCommentServesTheWholePackage`, `TestTheTestKitIsHeldToTheDocumentationRuleToo` — failed to compile | A rule in `internal/rules`, not a linter setting, so it is proved on planted code: functions, methods on exported types (through a pointer too), types, constants and variables; one comment may serve a whole group; a trailing comment counts; every package needs a package comment. It spares no package — the test kit and the rules are documented like any other. It caught one in the rules' own planted "clean" tree, and `Finding.String` in the rules themselves. "No write to standard output or error" — the other half of 00.8 — is the output rule of 00.11 |
| 00.13 The parity mapping | written in PLAN | — | `parity-mapping.md`; task 14.1 will hold it to the tests |

**WP-00 is complete: 13 of 13 tasks.** The full gate is green.

## Code-quality exemptions (D-96: one package at a time, each ratified by HUM LEAD)

The exemptions file is part of the local harness and is not tracked; this table is the tracked record.

| Package | Rule | Reason | Ratified |
|---|---|---|---|
| `internal/textsafe` | Two invariant checks a function | The cleaning functions are total: every input has a defined output, so there is no error to return. Reshaping predicates into guard clauses would satisfy the counter and assert nothing. Correctness is held by the tests and by the fuzz target (02.9). Real bounds are added where they exist | 2026-09-19, with D-96 |
| `internal/fault` | Two invariant checks a function | An error type and two closed lists: a constructor, three accessors, two name lookups. An error type cannot itself return an error. Every real check is present: a kind outside its list becomes internal or "unknown"; a nil receiver is answered; an error missing one of its three sentences becomes internal. Correctness is held by tests against the contract's own text | 2026-09-19, D-97 |
| `internal/work` | Two invariant checks a function | A queue under one lock: short state changes with the lock held, or reads of one counter. Its invariants are relations between structures, not preconditions on arguments. Every argument that can be wrong is checked; the two structural invariants that can be stated as a check are stated. Correctness is held by 25 tests under the race detector | 2026-09-19, D-98 |
| `assets` | Two invariant checks a function | Three of its five functions return an embedded text and take no argument, so there is nothing for them to check. Every argument that exists is checked: a zoom past 3, a column or row off the grid, an absent file. Content is held by tests that re-hash every tile against the hash list and hold the pin and the notice to what they must say | 2026-09-19, D-99 |
| `internal/tiles` | Two invariant checks a function | The entry points that take arguments meet the bar. The shortfall is seven error constructors, which can hold no check; helpers called only with a lock held and arguments already checked; and one-line formatters. The checks that matter most here sit after a call, where the meter does not count: what was opened is what was looked at, a link is not followed, the TileJSON's depth and size, the address rules. 40 tests and a fuzz target hold them | 2026-09-19, D-100 |
| `internal/colour` | Two invariant checks a function | Arithmetic on any colour, for which every 8-bit colour is valid input; lookups that answer "no such token" with false, each checking the token's range; and functions that take no argument that can be wrong. Held by six published contrast pairs, the foreground rule over every default line on every default background on both grounds, and the token list compared with the constants document | 2026-09-19, D-101 |
| `internal/style` | Two invariant checks a function | The entry points meet the bar. The parser's steps check after a JSON type assertion, which is a call and is not counted; filter evaluation takes any attribute set; builders and error constructors can hold no check. The ban on recursion and the complexity limit both split code into more, smaller functions. Held by 26 filter cases, nine kinds of refused style, the five parity rows, and a fuzz target that ran 12.8 million inputs clean before the exemption took effect | 2026-09-19, D-103 |
| `internal/render` | Two invariant checks a function | The entry points meet the bar. The rasterisers take any integer as a dot, and bounds are enforced once, in the function they all draw through; the painter's and composer's steps are called with checked arguments. Held by golden pictures, fills with holes, geometry two thousand million dots long, the vote's three levels, exact row widths, identical frames under shuffled tiles and on both architectures, and a fuzz target that drew 924,000 frames clean before the exemption took effect | 2026-09-19, D-104 |
| `internal/overlay` | Two invariant checks a function | The entry points that take a host's input meet the bar. The validation steps are the most heavily checked code in the repository, but their checks sit inside loops over rings and features, where the meter does not count; the geometry takes any number; the store's internals run under its lock with arguments already checked. **Under the standing rule (D-112):** `FuzzHandIn` found one real gap in eight seconds - a circle's unchecked rings, now refused - and then ran 21.7 million hand-ins clean | 2026-09-19, D-112: the standing rule; HUM LEAD told |
| `.` (the public package) | Two invariant checks a function | The public surface is many small calls, each of which already checks every argument it has: a depth that is none of the four, a layer the library does not draw, a language that is not a code, a place off the world or with an id that would need cleaning. The calls that take nothing - `Pending`, `Changed`, `Places`, `Close` - have nothing to check. **Under the standing rule (D-112):** `FuzzEveryReturnedStringIsClean` found a real defect in under two seconds - the unknown token names of `SetPalette` handed back as the host wrote them, escapes and all - and then ran 153,674 rounds of the whole public path clean | 2026-09-20, D-112: the standing rule; HUM LEAD told |
| `internal/describe` | Two invariant checks a function | A geometry package: the two entry points check that the place is on the globe and that there is geometry to measure against, and everything under them takes numbers and answers numbers. **Under the standing rule (D-112):** `FuzzArea` found three real defects - a ray east that has no end on a circular world, a flat approximation that could answer further than the edge's own end, and one bad vertex taking its good neighbours with it - and then ran 11.2 million rings clean | 2026-09-20, D-112: the standing rule; HUM LEAD told |
| `tools/answer-key` | Two invariant checks a function | The independent half of M1b: everything a scenario file can get wrong is checked where it is read - no places, no shapes, a place off the world, a shape with no geometry, a ring entry that is not a pair, a view that says nothing about its cells - and the rest is arithmetic written out from the definitions. **Under the standing rule (D-112):** `FuzzKey` found a real defect in under a second - a shape with no walkable edge answering an infinite distance - and then ran 18.5 million scenarios clean | 2026-09-20, D-112: the standing rule; HUM LEAD told |

## WP-02 — textsafe and fault

| Task | Commit | The failing test, first | Learned |
|---|---|---|---|
| 02.1 to 02.4 The cleaning pass | this commit | `TestInvalidUTF8BecomesReplacement`, `TestControlsDropped`, `TestBidiDropped`, `TestZeroWidthOutsideClusterDropped`, `TestZeroWidthInsideClusterKept`, `TestTagCharactersStayOnlyInAFlag`, `TestCleanIsIdempotent` — failed to compile | **The tests found a hole in the rule as first written.** "A cluster that occupies no cell is dropped" keeps a tag character that follows a letter, because it joins the letter's cluster — and invisible tag characters can carry text nobody sees. Tag characters now stay only after the black flag, the one base they belong to; a real flag sequence is kept whole. The zero-width table is by category (nonspacing and enclosing marks, format characters, variation selectors), documented in the code. The two allowed modules were added from the local module cache with the network off, at the first host's exact versions. **A tool wrote literal invisible characters into the test file where escapes were meant;** converted back, and a static rule against invisible characters in source is next |
| (not a plan task) A static rule: no invisible character in source | this commit | `TestInvisibleCharactersInSourceAreFound` (ten characters, in a library file, a test file and the test kit), `TestEscapesAndVisibleTextArePermitted` — failed to compile | Added because it had just happened: a tool wrote literal invisible characters where escapes were meant. The rule reads every Go file of the library before parsing it — tests and test-only tooling too — and a file that hides anything is reported and read no further. Accents, ideographs, dashes, tabs and every escape are fine. The test file for it is pure ASCII, checked |
| 02.5 to 02.8 Width, fit, quote, ids | this commit | `TestWidthMatchesHostTable`, `TestEveryClusterIsOneOrTwoCells`, `TestFitDropsWholeCluster`, `TestQuoteCutTo64Clusters`, `TestIDValidation` — failed to compile | Width is the host's own table applied cluster by cluster, and equals the host's figure for whole strings across names, accents, wide scripts, joined emoji, ambiguous symbols and damaged bytes. A cut quotation ends in three dots so it says it was cut. An id comes back byte for byte or is refused; **a new constant, 256 bytes for the longest id, is in the constants file.** The way escapes became literal characters is now understood — the escape is decoded before the shell sees it — and a small converter plus the static rule guard against it |
| 02.9 The fuzz target | this commit | `FuzzClean` — written against finished code, so it could not fail first; its worth is what it finds | 60 s, 1.4 million executions, 187 interesting inputs, no failure. It holds both of FR-34's paths: `Clean` and `Quote` never yield an escape byte, a control, a bidirectional control or invalid UTF-8; `Clean` twice equals `Clean` once and never triples the input; `Fit` never overruns; an accepted id comes back untouched. The gate runs it 60 s on every full run |
| 02.10 The closed glyph list | this commit | `TestClosedGlyphList`, `TestGlyphListMembersAndStrangers`, `TestGlyphListWasSeenInATerminal` — failed to compile | 372 characters: printable ASCII, braille, six markers, eight box-drawing, three hatch strokes, three shades, U+FFFD. The test walks every code point: each member measures one cell under the host's table and survives cleaning unchanged. **The list is tied to its evidence:** every non-ASCII member is on the terminal test card HUM LEAD ran in PLAN. Arrows and block quadrants are on the card and stay out of the list until wind and the block renderer are built |
| 02.11 One enforced way out; the typed error | this commit | `TestSafeOnlyBuiltHere`; in `internal/fault`: `TestKindsEqualTheContract`, `TestNoForeignErrorWrapped`, `TestErrorSaysWhatWhyAndWhatToDo`, `TestAnUnknownKindBecomesInternal`, `TestAnErrorWithASentenceMissingIsTheLibrarysFault`, `TestOutsideTextCanOnlyArriveCleaned`, `TestWarningCarriesKindSubjectAndCount` — failed to compile | `Text` has one unexported field; `Const` takes a string type no other package can name, so elsewhere only a literal can be passed; a test reads the package's source and fails if any other exported function turns a plain string into `Text`. **The two lists of kinds are read out of the contract's text by a test,** so code and contract cannot drift apart. No `Unwrap`; the `cancelled` kind answers `errors.Is` for both context errors and carries none of their text. **Driven by a test, not in the plan:** an error raised without one of its three sentences becomes `internal` and names the kind that was meant |

**WP-02 is complete: 11 of 11 tasks.**

## WP-01 — project

| Task | Commit | The failing test, first | Learned |
|---|---|---|---|
| 01.1 Mercator · 01.11 The non-finite guard · 01.13 Parity rows P-17, P-18, P-20, P-21 | this commit | `TestMercatorRoundTrip`, `TestNoNonFiniteReachesInt`, `TestParityP17_TileZoom`, `TestParityP18_TilePixelSize`, `TestParityP20_Projection`, `TestParityP21_Normalize` — failed to compile | The projection is written as atanh(sin) and asin(tanh), as the constants file says, and **a test holds it equal to upstream's own formula** — logarithm and tangent — to one part in a thousand million across fourteen points and four zooms. Round trip within 1e-9 of a degree to zoom 22, on arm64 and on emulated amd64. **The density rule counts only guards whose condition makes no call,** so `math.IsNaN` did not count; "is finite" is written as a comparison with the largest finite float, which NaN and both infinities fail. No exemption was needed. A latitude beyond the poles is refused, not clamped; one inside them is clamped to 85.0511 as upstream does |
| 01.2 to 01.4 The view, its dots, its tiles | this commit | `TestViewToDot`, `TestFractionalZoom`, `TestDotRoundTrip`, `TestTilesForView`, `TestTilesStayInsideTheWorld`, `TestViewValidate` — failed to compile | **The fixture cross-checks the projection:** the pinned fixture's view (149 by 38 at zoom 6.4 over Tampa Bay) asks for exactly the four zoom-6 tiles PLAN committed, in zoom, column, row order. A view past the edge of the world asks for nothing there (L-17 f, replicated); one closer than zoom 14 asks for zoom-14 tiles. Rounding to 1/256 of a dot is render's (09.28) and is not done here. The documentation rule caught two constants sharing one comment |
| 01.5 to 01.8 Distances, bearings, the cell's span, the seam | this commit | `TestGreatCircle`, `TestBearingAndCompass`, `TestCellSpan`, `TestLongitudeIsCircular`, `TestSphereRefusesNonFinite` — failed to compile | Three published city distances within half a percent on a sphere of the mean radius (6371.0088 km, now in the constants). **The cell span reproduces specimen 17's stated scale** at both of the first host's sizes: 0.8 and 1.6 km, 1.7 and 3.4 km, a row covering twice a column's ground. 342 degrees is "north", as scenario 2's answer key has it; compass words are whole words and leave as cleaned text. One degree across the 180-degree seam is 111 km and due east. **The code-quality counter sees an error check only when it is a statement of its own,** so `if err := f(); err != nil` is written as two lines in this package |
| 01.9, 01.10, 01.12 Fit-to | this commit | `TestFitToContainsAll`, `TestFitToLargestZoom`, `TestFitToEdgeCases`, `TestFitToClampsAndRefuses` — failed to compile | The zoom is solved directly from the span in mercator units and the room the margin leaves; a test shows 1/256 of a level closer pushes a named point out, at both of the first host's sizes. **The short way round is found from what is covered, not from sample points:** points and boxes become stretches of longitude, and the fit is the whole circle less the widest stretch nothing covers — so a box wider than half the world is not mistaken for its complement, and a box that crosses the seam is one stretch. A single point keeps the zoom; nothing named changes nothing. **A view's zoom now stops at 18, as upstream (P-55),** not at the deepest tile zoom: the constant and the view's validation changed together |

**WP-01 is complete: 13 of 13 tasks.**

## WP-03 — the tile decoder

| Task | Commit | The failing test, first | Learned |
|---|---|---|---|
| 03.1 to 03.14, 03.16, 03.17, 03.19 The decoder | this commit | Wire: `TestVarint`, `TestZigZag`, `TestField`, `TestWirePitfalls`, `TestWireAllocatesNothing`. Limits and formats: `TestDefaultLimitsAreTheRequirementsNumbers`, `TestBodyLimit`, `TestGzipLimit`, `TestLayerLimit`, `TestUnsupported`, `TestLimitsValidate`, `TestOnlyWantedLayersAreKept`, `TestNothingWantedDecodesNothing`. Features: `TestGeometryCommands`, `TestKeysAndValues`, `TestFeatureLimit`, `TestGeometryIntegerLimit`, `TestExtentRange`, `TestCursorLeavesInt16IsError`, `TestRetainedLimit`, `TestProtobufPitfalls` (twelve). Parity: `TestParityP26_SortKey`, `TestParityP36_LabelLanguage`, `TestParityP37_GzipSniff`, `TestParityP38_MVTDecode`, `TestParityP39_RingGrouping`. Real data: `TestCompactNeverLargerThanSource`, `TestRealTilesKeepOneLanguage`, `TestNeverPanics`, `TestNeverPanicsOnDamage`, `TestDropUnusedLayer`, `FuzzDecode`, `BenchmarkDecode` — each group failed to compile first | **The decoded tile joined `scene`, its fields driven by these tests,** as 00.10 said it would. **The design changed under measurement:** one coordinate slab for the whole tile allocated 1.7 MB to keep 297 KB, because the slab regrew as each layer arrived. Now each kept layer is counted without decoding it and owns two slabs made once at their exact size, and the value table is eight bytes a value, sized from the header pass: **393 KB allocated to keep 297 KB.** A test holds every real layer's slabs to length equal to capacity. The kept sizes agree with PLAN's throwaway measurement to within 8%. A layer the map does not draw costs no allocation at all, however large. Upstream skips a malformed command stream silently; this decoder refuses it, with a clear error. **Two checks came from the density rule and are real:** a host's limits are validated — lowered freely, never removed, never raised, the extent above all, since a coordinate is 16 bits — and `scene.Layer.Part` is the one checked place the slab arithmetic lives. New limits, in the constants: 4,096 keys and 400,000 values a layer. `TestNeverPanics` cuts every real tile at each of its first 2,048 bytes and every 509th after; cutting a megabyte tile at every byte would be a million decodes. Fuzz: 60 s, no failure; slow (about 100 executions a second) because its seeds are real tiles |
| 03.15, 03.18 The differential tests | this commit | `TestKeptFeaturesEqualTheProvenDecoder`, `FuzzAgree`, `TestTheProvenDecoderPanicsWhereOursDoesNot`, in the separate module `tools/oracle` | **11,252 kept features across the twelve real tiles agree with the proven decoder** — every part's coordinates, every class, every label — on the first run. **The differential fuzz found a defect within seconds, and it is the proven decoder's:** a feature with no geometry field is a nil dereference inside it, where ours keeps the layer and passes the feature over. Evidence for D-75: the borrowed decoder cannot be handed hostile bytes. A panic there now counts as "they refused it"; the input is kept as a seed. **A workspace file alone did not resolve the unpublished library** once the library imported third-party code — the point red-team round 2 had marked unverified. The gate now writes, inside its throw-away workspace file, a replace for the library at exactly `v0.0.0`; tracked module files still carry none, and the gate passes with the network off. The proven decoder and its dependencies are in this tool's module only |

**WP-03 is complete: 19 of 19 tasks.**

## WP-05 — fetch

| Task | Commit | The failing test, first | Learned |
|---|---|---|---|
| 05.1 to 05.11 The fetcher | this commit | `TestGetOverSecureTransport`, `TestPlainHTTPRefused`, `TestRedirectLimit`, `TestNoSecureToPlain`, `TestCrossHostRefusedUnlessAllowed`, `TestPrivateAddressRefusedAtDial`, `TestNoReferer`, `TestUserAgent`, `TestBodyReadThroughLimit`, `TestRangeMustBe206Exact`, `TestErrorNeverCarriesAddress`, `TestStatusChecked`, `TestTimeout`, `TestContextCancel`, `TestProxyFromEnvironment`, `TestReplacementFetcherContract` — failed to compile; every one runs against loopback servers | A fetcher is made for **one named source**, and a request to any other scheme or host is refused before anything is sent — stricter than the plan said, and it is what makes "errors name the scheme and host" always true. **"localhost" is a name, and a name can resolve anywhere:** plain http is allowed to a literal loopback address only, and the dial test uses "localhost" as its public-looking name that lands in private space. The check is on the address actually connected to; the carrier-grade shared range counts as private. **Behind a proxy from the environment the connection lands on the proxy, so that check cannot be made;** the request is marked and the check skipped, as FR-22b documents. The standard client adds a Referer on redirects; it is removed in the redirect check. The transport's error is never returned or wrapped — a test walks the chain and tries `errors.As` for it. A token that could break out of the User-Agent header is refused. `textsafe.Join` was added so an error can name the scheme and host: cleaned pieces are cleaned once more as a whole. **The code-quality check matches calls by name,** so `New` calling `fault.New` and `Get` calling `Header.Get` read as recursion: renamed `ForSource` and `Fetch`. No exemption |
| 05.12 The shaped server | this commit | `TestShapedServer`, `TestShapedServerRefusesABadLink` — written before the code, in the same step, so their failure was not watched | Secure transport with its own trust anchor; a client without it is refused. **Its clock is virtual and is arithmetic:** the connection once, a round trip for each round of as many requests as the pump is wide, every body byte at the link's bandwidth. Four requests for the Midwest fixture's 1,245,163 bytes on NFR-5's link cost exactly 0.300 + 0.300 + 1.245163 s — the constants file's own sum — and nothing really waits |

**WP-05 is complete: 12 of 12 tasks.**

## WP-07 (work)

| Task | Commit | The failing test, first | Learned |
|---|---|---|---|
| 07.1 to 07.15 The queue the host drains | this commit | `TestPendingCounts`, `TestCapDropsOldest`, `TestNewestViewFirst`, `TestWorkDoesOneJob`, `TestWorkCancel`, `TestWorkNeverWaitsOnWork`, `TestLeftViewCancelsJob`, `TestChangeCounterMovesOnCompletion`, `TestNextCallIsEarliest`, `TestNothingDueWhenOffline`, `TestPendingCountsWaitingOnly`, `TestSettleEndsWhenIdle`, `TestSettleEndsOnContext`, `TestSettleSaysNoSource`, `TestSettleReportsWorkInFlightElsewhere`, `TestNoPanicEscapes`, `TestNoWorkCalledWarning`, `TestOnPendingFiresOnceInsideOwnerCall`, `TestOwnerCallFromHookRefused`, `TestTwoWidePumpBothWake`, `TestSharedJobReturnsToQueue`, `TestLeavingAQueue`: failed to compile | **One queue, several members.** A map is a member: its own wake hook, change counter, view and say in what is wanted; two maps wanting one tile is one job, pending for both; any member's `Work` may run it. **Round 3's two findings hold in code:** a shared job given up by a cancelled call returns to the queue and the other map's hook fires from inside that call; and a hook fired that way arms no re-entrancy guard, while one fired by an owner call refuses owner calls with `reentrant-call`. The pump as the contract draws it (a channel with a slot a goroutine, a wake that fills every free slot) is a test, and both goroutines work one burst. A job cancelled because its tile left the view is not its `Work` call's failure. The counter moves when a job fails as well as when it finishes: the frame's status changes either way. A panic's own text is never repeated. Retry times live here as deferred jobs; nothing makes one come due but an owner call with the host's wall clock, and waiting work is not a deadline, so an offline map reports nothing due. The queue imports none of the packages that supply its jobs; the static rules hold that. Thirty runs under the race detector, no flake. **The density counter counts only leading guard clauses,** learned here: two real invariants added in function bodies did not move it |

**WP-07 is complete: 15 of 15 tasks.**

## WP-04 (archive, generator, assets)

| Task | Commit | The failing test, first | Learned |
|---|---|---|---|
| 04.1 to 04.7, 04.15 The archive reader | this commit | `TestHeader` (nine kinds of damage), `TestTileIDHilbert`, `TestFindTile` (flat, and through a leaf directory), `TestRunLengthsServeManyTilesFromOneEntry`, `TestDirectoryLimits`, `TestLeafDepthAndCycle`, `TestMetadataNeverParsed`, `TestReaderFailuresAreTheLibrarysOwnErrors`, `FuzzHeader`, `FuzzDirectory`, `FuzzOpenAndFind`, `TestRealArchive`: failed to compile; the archives are built inside the tests | The reader takes a function that returns exactly a range of bytes and imports nothing that opens a connection. **04.6 is met in two places:** the fetcher already refuses a range reply that is not a 206 with exactly the range asked for (05.6), and the reader refuses any read that returns a different length; the adapter that joins them is `tiles`' (WP-06). A directory's decompressed size and its entry count are checked before anything is allocated; an entry count larger than a quarter of the bytes that follow is refused as a lie. Offsets are checked in a form that cannot overflow. Leaves that lead only to more leaves end in an error after three levels, and the test counts the reads. The metadata block is never read: a test records every range asked for. **Against a real archive** (16 MB, zoom 0 to 3, from the research phase, too large to commit): all 85 tiles found in 87 reads. That test runs only when an environment variable names an archive, and prints NOT RUN otherwise. The code-quality check refuses a hand-written binary search as an unbounded loop; the standard library's search took its place |
| 04.6 Range reads through the fetcher | this commit | `TestRangeReaderInsistsOn206`: a loopback server serves a test-built archive by ranges, then answers every range with a 200 and the whole file; failed to compile | **A correction to the row above:** no task in WP-06 joins the fetcher to the reader, so the join belongs here and is now built. It is one small function. The fetcher also learned to **describe** a file - its length and entity tag, which the pin needs - from a first range request, held to the same 206 rule. The tag is outside text and is cleaned |
| 04.8 The encoder | this commit | `TestEncodeRoundTrip` over four real tiles: what the library's decoder keeps of the re-encoded tile is exactly what it kept of the source. `TestEncodeRefusesWhatItCannotWrite`: eight refusals, the first of which was a panic on a missing source | The encoder lives in the generator's module and nowhere else. A ring that ends where it began is written with a close command; rank is written signed, so a negative rank survives |
| 04.9, 04.10 The hash list and the pin | this commit | `TestGeneratorWritesHashList`, `TestPinChangesTogether` (a changed pin, a changed tile, a stray tile, a missing tile), `TestGenerateRefusesAnIncompleteSource` | The hash list's first line is the hash of the pin, so neither can change alone. **The pin is the planet file's versioned name, and the length and entity tag its publisher reports (FR-28a)**; the address is never written down |
| 04.13, 04.14 Strip, and the same bytes every run | this commit | `TestGeneratorStripsTranslations`, `TestGeneratorDeterministic`, `TestGeneratesFromThePinnedArchive` (a loopback server: honest, the whole file for every range, a tile missing) | **Run for real against the 86.5 GB planet file: 85 tiles in 55 seconds, every directory inside the reader's limits. A second run wrote the same bytes, file for file.** One thing only the real host could teach: it **ignores a range of one byte** and answers with the whole file, while honouring a range of 127. The generator refused the 200, as built. The describe call now asks for the archive header's length, and says why in its comment |
| 04.11 The embedded tiles | this commit | `TestAssetsDecodeThroughGate`, `TestOnlyTheEmbeddedZooms`, `TestAssetsMatchTheHashList`, `TestNoticeSaysWhatTheHashesProve`: failed to compile | **1,015,126 bytes** for all 85 tiles, against a bound of 2.5 MB and an estimate of 1.7 MB made before English names were kept. Every tile passes the hardened decoder on default limits. Decoded, the set keeps 3,535,546 bytes; the largest tile keeps 209,131. The test re-hashes every tile against the list in every run, which is the re-hash FR-28a asks of continuous integration. The data notice says what the hash list proves and what it does not. **For HUM LEAD's eye at REVIEW:** the notice offers the tiles under the same Open Database License as their source - the cautious reading (D-78) - and that sentence is a licence statement made in HUM LEAD's name |
| 04.12 Importing changes nothing | this commit | `TestAssetsRegisterExplicitly` reads the package's own source: no init function, one package-level variable (the embedded files), nothing of the library imported | The package exports one function a host passes as an option, and three texts. The contract, two sequence diagrams, the tile states and the tiles page said "assets imported"; they now say what the architecture page and this task always said: passed as an option |

**WP-04 is complete: 15 of 15 tasks.** The full gate is green with the generator's module in it.

## WP-06 (tiles)

| Task | Commit | The failing test, first | Learned |
|---|---|---|---|
| 06.3, 06.4, 06.5 The memory cache | this commit | `TestMemoryCacheByteCap`, `TestPinnedNeverEvicted`, `TestStandInIsPinnedWhileItStandsIn`, `TestCacheUnderNeedWarnedOnce`, `TestCacheUseReportsNeedHeldCap`, `TestCacheKeyIncludesLanguageNotStyle`, `TestAncestorStandIn`: failed to compile | **A view publishes chains, not tiles.** For each tile it wants: the tile, then what may stand in for it, nearest first. The first of a chain on hand is what is drawn and what is need (D-90); the cache works that out afresh at every change, so an ancestor is need exactly while it stands in, and a spare the moment the tile arrives. Need alone over the cap: the cache holds exactly the need and warns once for each stretch spent there. A chain may cross sources, which is how the embedded tile stands in for the network's |
| 06.1, 06.2, 06.6, 06.7, 06.15, 06.18, 06.19, 06.20 The pipeline | this commit | `TestNoSourceNoConnection`, `TestSourceOrder`, `TestAssetsNeverOverrideChosenSource`, `TestAncestorsAreWanted`, `TestDedupInFlight`, `TestNotBeforeDoubles`, `TestTileStates` (the table of every state and event, and the pipeline walked through it), `TestSourcesChanged`, `TestOverzoomAboveSourceMax`, `TestBadTileFromTheSource`, `TestPipelineRefusals` | **A design change, recorded on the tiles page:** the chosen source's tile (disk, then network) is one job, and the embedded tile is a job of its own. One job trying all three in order could not draw the embedded tile while a slow fetch ran, which is what L-13's wording promises. **No clock is read:** a failure is counted in the job, and the owner call after it stamps the not-before time from the host's clock. A job dates what it writes by the clock of the last plan. With no source, the one job is what finds that out and says why, which is what the queue's `Settle` reports. With a one-byte cache a test showed D-90 at its barest: the world tile stood in, became a spare when the tile arrived, was evicted, and was wanted again as a stand-in for the next view. A job's name carries a short hash of the source, never the source: it may hold a key |
| 06.14 The schema seam | this commit | `TestSchemaMappingSeam` | One mapping in this release. A source that declares nothing gets it; one that declares layers of which the map knows none is refused |
| 06.16, P-49, P-50, P-51 The named source | this commit | `TestTileJSONLimits`, `TestTileJSONAddressesObeyFetchRules`, `FuzzTileJSON`, `TestParityP49_UrlModes`, `TestParityP50_Http`, `TestParityP51_FetchFailure` | **The fuzz target found a gap in six seconds:** tokens after a `#` are never sent, so every tile would be one request. A tile address with a fragment is refused, and the input is kept as a seed. The TileJSON is read inside the first tile job, never when the source is named (D-65); two jobs may both read it the first time, and the first answer is kept. A refusal is kept; a failed fetch is not, because the network may come back. Nesting is counted outside strings before the document is parsed |
| 06.8 to 06.13, 06.17, P-48 The disk cache | this commit | `TestDiskCacheOffByDefault`, `TestCachePathConfined`, `TestParityP48_DiskCache`, `TestWriteOnlyAfterFullDecode`, `TestBadCachedFileDeleted`, `TestRootWritableByOthersRefused`, `TestEvictLeastRecentlyRead`, `TestRecencyWrittenAtMostHourly`, `TestPurgeAndVerify`, `TestSymlinkInsideRootNotFollowed`, `TestCachedFileSizeCheckedBeforeRead`, `TestReadOnlyRootTolerated`, `TestMemoryHitRefreshesDiskRecency`, `TestCacheRootOpenedOnce` | The standard library's confined directory handle does the confinement; the test moves the root aside and puts a stranger's directory in its place, and the write still lands under the handle. The handle follows links that stay inside, so every entry is looked at without following first, and the opened file is compared with the one looked at. Writing replaces a link and never writes through it. **A plan does no input or output,** so a tile served from memory is noted in memory and its recency written by the next job. Three tests are skipped on Windows with a stated reason; the package is vetted for Windows in the gate |
| 06.21 Parity | this commit | The four tests the mapping names for this package: P-48, P-49, P-50, P-51 | All four pass |

**WP-06 is complete: 21 of 21 tasks.**

## A defect found in BUILD: what the decoder keeps (WP-03, WP-04)

| What | Found | Fixed | Learned |
|---|---|---|---|
| The decoder kept `class`, the name, and upstream's `localrank` and `scalerank`. Real OpenMapTiles data carries neither rank key - a place's importance is `rank` - and a boundary has no `class`: it has `admin_level`, and `maritime` for a line drawn across the sea. **Every real place had rank 0, and a country border could not be told from a region's** | While starting the style work (08.21), by listing the attributes of a real tile's layers before writing roles against them | `TestKeepsWhatTheRolesNeed` and `TestRealBoundariesAndPlaces`, written first and failing. A feature now keeps its administrative level and its maritime flag in two bytes that were padding, so the kept size is unchanged (3,535,546 bytes for the embedded set, as before); rank falls back to `rank` after upstream's two keys. The generator's encoder writes both; the embedded tiles were regenerated from the same pin (1,023,118 bytes; pin, hash list and tiles changed together); `TestEmbeddedTilesCarryWhatTheRolesNeed` holds the embedded world tile to it | **The comparison with the proven decoder never compared ranks**, so two decoders reading the same wrong keys agreed. It now compares rank, level and the maritime flag over all 11,252 features, and the fuzz target holds the same. Parity tests written from upstream's code inherit upstream's schema; the data has the last word |


## WP-08 (style and colour), the basemap half

| Task | Commit | The failing test, first | Learned |
|---|---|---|---|
| 08.1 to 08.5, 08.16 Tokens, palette, contrast, the foreground rule, the ground | this commit | `TestTokensMatchTheDocumentedList` (it reads the constants document's table, which was written first: 60 names), `TestPaletteOverridesToken`, `TestUnsetTokenFallsBack`, `TestContrastFormula` (six published pairs, among them the grey that misses 4.5:1 by 0.02 and the one that passes), `TestForegroundRule` (every default line on every default background, both grounds), `TestKeepColourWherePasses` (specimen 20's cases), `TestGroundPaintedByDefault`, `TestDeclaredGroundNotPainted`, `TestStyleByGroundLuminance`: failed to compile | **The built-in dark and bright styles are one set of roles and two sets of defaults**, chosen by the ground in effect; the defaults start from the colours of specimen 20, which HUM LEAD has seen. A host's colour for a token is its colour on either ground. The palette gives no default for the ramps - alerts, radar, temperature, a host's own type: a preset gives those (M-B). A name that is no token is returned to the caller and the rest of the palette takes effect; **which error kind that is, and which a refused style file is, the contract's closed list does not say - put to HUM LEAD** |
| 08.17, 08.21, and P-41 to P-45 The style | this commit | `TestBuiltInStylesCoverEveryRole`, `TestUserStyleLegacyFilters`, `TestParityP41_StyleMatch`, `TestParityP42_FilterOps` (26 filters, upstream's three oddities among them), `TestParityP43_ConstantsAndRef`, `TestParityP44_ColourPick`, `TestParityP45_LineWidth`, `TestAttrsOf`; and `TestWithin` for the shared size-and-depth check: failed to compile | **The built-in style is fourteen rules, each naming a token**, so dark and bright are one style and the ground picks the colours. Its filters ask what the data really carries (see the defect above): a border's administrative level, never at sea. Water is drawn by two rules - its edge, and its fill. **A filter nests, and the project bans a function that calls itself,** so a filter is compiled once into steps worked out with a short list of answers; matching a feature allocates nothing for filters up to 32 parts deep. A colour's stops step at each zoom; a width's run in a straight line between them; upstream read only the first of either. A style written with expressions is refused by name, in the new `malformed-style` kind (D-102); an operator nobody knows is true, as upstream has it. A palette name that is no token is reported as the new `unknown-token` warning by the public package (WP-12). **Noted for the render work:** the decoder keeps the `landcover` layer and no role draws it; either a role is added or the layer is dropped from what is kept |
| 08.20 A user's style is untrusted | this commit | `TestStyleLimits` (a filter nested 70 deep is over the limit; one nested 30 deep, and a group of 101 parts, work out; a chain of twelve refs is refused; zooms and widths outside any real map are read as none), `FuzzStyle` | Pulled forward from the overlay milestone by D-103. Two minutes and 12.8 million inputs found nothing; the target runs in every full gate |

## WP-09 (render), the basemap tasks

| Task | Commit | The failing test, first | Learned |
|---|---|---|---|
| 09.1 to 09.5, and P-01, P-02, P-03a, P-08, P-09, P-13 to P-16 The braille canvas | this commit | `TestEmptyCellIsBlankBraille`, `TestParityP01_PixelGrid`, `TestParityP02_DotBitmask`, `TestLineRaster` (a golden picture of two shallow lines; eight directions out and back), `TestParityP14_ThickLine`, `TestFillEvenOdd`, `TestParityP15_PolygonFill`, `TestParityP16_DegenerateRings`, `TestClipNothingOutside` (lines and a fill two thousand million dots long), `TestCellColourVote`, `TestParityP09_ForcedPixels`: failed to compile | An ink is a small number; the caller gives it meaning at draw time (D-26). **Upstream's thick line of width 3 is two dots thick, not three:** its half width is 1.5 and is spread to one side. Matched as it is, and the test says so. Upstream's loops run until a condition; here each is bounded by the line's length and width. A line is cut to the rectangle grown by 64 dots before it is rastered, so one from far away costs what its visible part costs, and a line wholly inside that margin rasters exactly as upstream's. The fill is even-odd by rows of dot centres, which D-11 leaves free; it looks at every edge for every row, **which is the first thing to measure when the benchmarks of WP-13 exist**. The vote's last resort is the ink met first scanning the cell, as upstream's is |
| 09.26, 09.28 Painting a tile | this commit | `TestWaterwaysAndParksDrawn`, `TestWaterOwnsItsCells`, `TestStandInDrawnLarger`, `TestProjectionRounded`, `TestPainterRefusals`; and in `internal/project`, `TestTilePlace` | A tile is painted through the style onto two canvases: line work as dots, and areas as the cells they own - **water is a cell's background, never dots** (L2 Render, step 2), and a cell is water's when water covers half of it. **A polygon's edge along its tile's border is where the tile was cut, not a coast,** and is not stroked; without that every tile draws its own outline. A stand-in is the same tile placed at a larger side. A coordinate is rounded to 1/256 of a dot before it is rastered, and the product is converted before it is added, so that no machine fuses the two. Frames are identical on both architectures the gate runs |
| 09.10, 09.11, 09.16 (notice, credit, scale), 09.17, 09.18, 09.20, 09.21, 09.22, and P-06, P-07, P-10, P-11, P-12, P-34, P-35 The frame | this commit | `TestEveryLineExactWidth` (four sizes, measured with the width library directly), `TestOnlyColourSequences`, `TestParityP06_SgrForms`, `TestParityP07_RowSelfContainment`, `TestNeverBlank`, `TestDeterministicAcrossMapOrder`, `TestFrameStatus`, `TestScaleMark`, `TestLabelCollision`, `TestLabelByCluster`, `TestRenderImportsNoSlowPackage`, `TestRenderRefusals`; in `internal/textsafe`, `TestEach` | **The first frame: the world from the embedded tiles, 149 by 38, with coasts, country borders, water, names that do not collide, a scale mark and the credit line.** Every row sets its colours afresh and puts the terminal's back; a colour is sent only when it changes. Names are placed most important first, centred by width in cells and not by bytes (L-7), a cluster a cell, a wide character two; a name that would leave the rectangle is skipped whole. **Seen in the first frame and changed:** region borders, rivers and roads drawn at world scale were clutter, so the built-in style's finer roles now start at the zoom where they can be read - a first setting, HUM LEAD's to tune at M-A. **The zoom gate includes both ends, as upstream's does (P-27);** the style had excluded the upper one and was corrected, test first |
| The painter's fuzz target (D-104) | this commit | `FuzzPaint` | Two minutes and 924,000 frames found nothing. A tile whose parts run past its coordinates is refused by the painter, not drawn |
| 09.19, 09.24, 09.25, and P-19, P-22 to P-25, P-28 to P-30, P-32, P-33 The rest of the basemap | this commit | `TestParityP19_VisibleTiles`, `TestParityP22_OceanOutsideWorld`, `TestParityP23_DrawOrderZ2`, `TestParityP24_DrawOrderBelowZ2`, `TestParityP25_LabelDeferral`, `TestParityP28_FeatureCull`, `TestParityP29_Scaling`, `TestParityP30_LineClipPad`, `TestParityP32_LabelAnchor`, `TestParityP33_LabelBounds`, `TestReuseKey` (ten changes, each forcing a redraw, and back), `TestOnlyChangedRowsRebuilt`, `TestFrameValidUntilNextRender`, `TestUnchangedFrameZeroAllocs` (built only without the race detector) | Beyond the world's edge is ocean, in the style's water colour. Layers are drawn in one order whatever order a tile carries them in, so a border lying on a road shows as a border. A feature whose box misses the view grown by the pad is not rastered. Consecutive points on one dot are one point, and a polygon edge along the tile's border is still known as one after that. A name is tried at each of its vertices in turn. **An unchanged frame is returned as it stands, at no allocation. A palette cannot be compared, so whoever changes it raises a counter** - the public package's job. When something does change, a row is rebuilt only if a sum of its cells changed: turning on the scale mark rebuilds one row of sixteen. The credit line is placed before the scale mark: on a narrow map the credit, which the data's licence asks for, is what stays |

## WP-12 (the public package), the three calls

| Task | Commit | The failing test, first | Learned |
|---|---|---|---|
| 12.1, 12.2, 12.4, and P-56 The three-call path | this commit | `Example_threeCalls` (create, settle, render, and nothing else), `TestThreeCalls`, `TestNewStartsNothing` (no goroutine, no file; the dial guard watches for a connection), `TestSizeIsStateBeforeSettle`, `TestSettleWithoutSizeRefused`, `TestNoTilesSaysWhy`, `TestRenderTakesTheSize`; in `internal/project`, `TestFitWorld` | **The embedded tiles are passed as `WithEmbedded(assets.Tile, assets.MaxZoom)`** - the option's name is the coordinator's, and is HUM LEAD's to change. Until the view can be moved, a map shows upstream's fitted world (P-56) at whatever size it has. `Settle` notes what the view wants, then drains the queue; `Render` notes the same, so a render before any work gives the notice and leaves work pending. The lock is not held while work runs. **The code-quality check takes a public method that wraps an internal one of the same name for recursion,** and the public names are the contract's, so the internal ones were renamed: the error constructor, the queue's drain and backlog, the pipeline's release, the renderer's draw. The public package meets the density bar without an exemption. **Two defects were found only by drawing through the public calls** (specimen 26): a blank frame below zoom 0, and names placed a world away |

## Milestone M-A: a map from embedded tiles

Reached. The three public calls draw the world from the embedded tiles with no network, no goroutine and no file; the frame is never blank, is the exact size asked for, holds colour sequences and cleaned text only, and is byte-identical across tile order and on both architectures the gate runs. Frames for HUM LEAD's look: specimen 26. **Work stops here for that look.**

**HUM LEAD's look (D-105 to D-108):** the look is fine; the starting zooms stand as the library's defaults and the first host sets its own; the option is `tuimaps.Embed(assets.Tile, assets.MaxZoom)`; the line at the world's edge is fixed, test first (`TestBorderSliverNotACoast`); the data notice stands, and a consuming application also credits the data in its About. The terminal and font were named afterwards (D-109): cmux with zsh, JetBrains Mono, on macOS; Ghostty with fish, JetBrains Mono, on Linux (D-110), where the test card has not been run. Still open for REVIEW: the `landcover` layer, kept and undrawn.

## WP-08 (style and colour), the rest: milestone M-B

| Task | Commit | The failing test, first | Learned |
|---|---|---|---|
| 08.6 The colour-vision simulation | this commit | `TestSimulation` (the published Lab of a primary, red as protanopia sees it, five pairs under each of the four kinds of vision), `TestSimulationIsInLinearLight` | A port of the script PLAN's specimens were measured with, held to its figures to three decimal places on both architectures. **The trap D-88 was ruled on, as a test:** two dark reds of the temperature scale are 27.1 apart with the deficiency simulated in linear light, and 6.8 with the same matrix applied to gamma-encoded values. *The plan's task names a pair that scores 11.1 and 2.9; which pair that was is not recorded anywhere, so the test uses one found by search in the approved scale and says so here* |
| 08.7, 08.18 The ramp checker | this commit | `TestCheckerOrdered`, `TestCheckerVisionSafe`, `TestCheckerAllPairs`, `TestCheckerGroundRule`, `TestCheckerReadable`, `TestCheckerDistinct`. The ramps are read from PLAN's data files, which were written first | All four filed ramps pass every rule on their own ground. The dark-ground radar ramp on a light ground: its heaviest class is 3.1 from the ground, as red-team found. The light-ground radar ramp as first filed gets lighter from class 2 to 3, as red-team found. **A many-hued scale in the style of broadcast maps fails three ways** - not ordered, neighbours 6.7 apart, two classes that are not neighbours 0.07 apart - as S21-2 says; its colours were approximate and not kept, so the test has its own. **The temperature scale's two variants are lightest at different classes:** class 6, just below freezing, on a dark ground, and class 7, just above, on a light one (D-91 changed those bands). Each carries its own midpoint. The temperature scale as first filed is not kept either; the all-pairs test builds its flaw |
| 08.13, and P-05a in part: colour depths | this commit | `Test256UsesOnlyFixedIndices`, `TestParityP05a_ColourDepth`, `TestContrastIsExactAt256` | One depth type, the colour package's, used by the renderer. At 256 colours only entries 16 to 255 are used, every filed 256-palette colour is one of them, and **the foreground rule is applied to the colours the palette will really show**, so 3:1 checked is 3:1 on the screen. Until task 08.14 builds the 16-colour palette, a 16-colour hint draws with no colour, which is always safe (D-59) |
| 08.8, 08.9 The temperature and radar presets | this commit | `TestTemperaturePresetPasses`, `TestBreaksExactInFahrenheit`, `TestRadarPresetPasses`, `TestRadarRampFollowsDeclaredGround` | The eight ramps are the filed ones, compared with PLAN's data files entry by entry, and each passes the checker on its own ground at its own depth. A ramp token's default is the library's own ramp for the depth, never a conversion. **The class the temperature scale is lightest at differs by variant** - class 6 in truecolor on a dark ground, class 7 on a light ground and at 256 colours - so the midpoint is the preset's to say. **A fourth lightness inversion at 256 colours, not recorded before:** on the dark ground the two pale classes swap under protanopia by 0.4. The constants say so now, and the test holds the exact counts |
| 08.10 The alert colours | this commit | `TestAlertPreset`: every outline at least 3:1 on its ground and on its own tint; text readable on every tint; every pair of outlines, every pair of tints, each against its ground and each tint against water at least 10 apart; on both grounds, at both depths | **Designed here by search with the script PLAN's ramps were measured with**, each colour held to its own hue family so the result is a design and not only a pass: magenta for extreme, red-orange for severe, amber for moderate, green-blue for minor, grey for unknown. The two pairs PLAN drew, which HUM LEAD has seen, are kept. On a light ground the scheme turns over - dark outlines, mid-tone tints - because pale tints could not be told apart under simulated colour vision. **The truecolor sets converted to the 256 palette fail on both grounds** (a tint 4.6 from water, two tints 6.6 apart), so the preset has its own 256-colour sets, searched within that palette: S10-1 once more. Worst pair: 12.6 dark (the two kept pairs), 16.2 light; at 256 colours 14.5 and 11.7. **A swatch card, specimen 27, is for HUM LEAD's early look; the look the plan asks for (08.23) is at rendered alert areas, once overlays draw** |
| 08.11, 08.12 Overrides and safe ramps | this commit | `TestOverrideIsWarnedNotRefused`, `TestSafeRampsForcesPreset` | A host's colour for a ramp token is used, and the ramp it lands in is checked: one `ramp-rule-broken` warning a ramp, counting its breaches. Alert colours are a set of five, not a scale, so they are not asked to run light to dark. With safe ramps on, the host's ramp colours are passed over and its other colours stand; the palette it was made from is unchanged |
| 08.14, 08.19, and the rest of P-05a: sixteen colours | this commit | `TestSixteenPalette`, `TestSixteenReferenceTable`, and the sixteen-colour half of `TestParityP05a_ColourDepth` | The basemap's own palette at 16 colours, chosen from the sixteen: specimen 23's choices, with one change. **Specimen 23 drew roads white and borders bright white, which differ only in brightness; PL-AX-6 asks for more, so major roads are yellow. For HUM LEAD's eye.** A light-ground set is new - only black, red, blue, magenta and grey read on white in the reference table, so parks are grey there. Ramp tokens have no entry: what needs a ramp is drawn in its no-colour form (D-59). The renderer emits the sixteen's own sequences, and applies the foreground rule against the reference table, which is indicative only. The painter's fuzz target now draws at all four depths |
| 08.15 Depth selection | this commit | `TestNoColourSelectedByEnv` | A hint from the host wins, because the host knows where its output goes; with none, a non-empty `NO_COLOR` selects no colour; a hint that is no depth gives no colour, the form that is safe anywhere. The environment is passed in as a function: the library reads nothing by itself |
| 08.22 A palette swap keeps the tiles | **moved to WP-12** | - | It needs the public calls that set a palette and report the change counter, which WP-12 builds. The renderer's half is already held: the palette's change counter is part of the frame's reuse key (09.24), and no tile is touched by a colour |

## WP-10 (overlay)

| Task | Commit | The failing test, first | Learned |
|---|---|---|---|
| 10.6 to 10.9 Shapes: buckets, simplification, the drop rule, the antimeridian | this commit | `TestZoomBuckets`, `TestSimplifyIterative` (a 14,001-vertex ring at three buckets, every dropped vertex within the tolerance; a 200,001-vertex comb), `TestSubDotRingsDropped` (three squares and a sliver), `TestSplitAtAntimeridian` | **A vertex is two 32-bit fractions of the world's side, not two 32-bit floats.** Both are the 8 bytes the shape cache's arithmetic assumes, but a float has 24 bits of precision and a braille dot at zoom 18 is a 26-bit fraction of the world; the fixed form gives 64 units to that dot. **Simplification's work is bounded:** a shape built so that every split is lopsided costs a plain version the square of its length, which at the vertex cap is hours inside one `Work` call. Spans the budget does not reach are kept whole, which is never coarser than the tolerance. A ring across the antimeridian is followed the short way round and cut into a ring either side, each ending on the line; one that only touches the line is left alone |
| 10.12, 10.13, 10.21 Grid classes, types, freshness | this commit | `TestGridClassify`, `TestGridPresetSuppliesBreaks`, `TestOwnTypeNeedsBreaks`, `TestClassCap`, `TestFreshnessStates` | A value's class is the number of breaks at or below it; a value that is no number is no data. A preset supplies its breaks in the unit named and may be overridden; a host's own type needs breaks, rising, with no repeat, for at most 21 classes. Freshness is current, stale or uncertain; a valid time more than five minutes ahead of the host's clock is uncertain and is drawn as stale. When a current overlay goes stale is given, for the moment the host is told to call again by |
| 10.1 to 10.5 in part, 10.11, 10.24, 10.25 The store: set, replace, remove, borrowed geometry | this commit | `TestSetReportsCreatedOrReplaced`, `TestRemoveReportsFound`, `TestRemoveUnknownReportsNotFound`, `TestDrainingReportedByWorkReturn`, `TestBorrowNotCopied`, `TestHandInMistakes` (thirteen mistakes, each with its kind and a message that says what to do, each leaving a `set-refused` warning), `TestVertexCaps`, `TestWarningsCappedAndDeduplicated`, `TestNearDuplicateIDWarned`, `TestSetAndRemoveNeverBlock`, `FuzzHandIn` | **The struct behind `Set` is designed here; the documents left its fields open:** an id, a valid time and a currency, a credit, and features - each a kind, rings that are the host's own memory, or a centre and radius, a token to draw in, and a label. `Set` and `Remove` never wait: a version that is still being read is retired, and the reader's own return reports the release, which is how `Work` comes to report it (D-86). A replaced overlay counts its new vertices against the cap, not both. A near-duplicate id is one that differs by case, punctuation, or one character at the end. **The fuzz target found a gap in eight seconds:** a circle's rings were never looked at, so a circle handed in with stray rings of junk was accepted and failed later; a circle with rings is now refused, and is the thirteenth mistake in the table. Then 21.7 million hand-ins in two minutes, clean |
| Preparing shapes, and drawing them (WP-10 and WP-09's overlay tasks, in part) | this commit | In `internal/overlay`, `TestPrepare`; in `internal/render`, `TestAlertAreaDrawn`, `TestOverlayLabelBeforeBasemap`, `TestLineAndPointShapes` | An overlay is prepared for a zoom bucket into the library's own copy - cut at the antimeridian, projected, simplified, rings smaller than a dot dropped, a circle made a ring of 64 sides each the radius from its centre along the ground - in a form the two packages share, because the renderer imports nothing that prepares. **An alert area is its tint, as the background of the cells it owns, and its outline, which locks its cells' colour so that it shows whatever basemap lines share them** (FR-16: a tint is never the only edge). Overlay labels are placed before any name of the basemap's, and stay when the basemap's labels are turned off. With no tile at all the overlays are still drawn (FR-23). A shape whose box misses the view costs finding that out and no more. **Specimen 28 is HUM LEAD's look at the alert colours on a map (task 08.23)** |
| 08.23 HUM LEAD's look at the alert colours | record | - | Specimen 28, four frames. "alert draws look fine" (D-113). RS-26 is closed; the colours may be frozen into reference frames |
| 10.5 (the borrow check), 10.10, 10.26 The run index, the borrow check by run, the cache of prepared shapes | this commit | `TestRunIndexBuiltInsideSet` (the count tested on both sides: 30,303 and 30,304), `TestBorrowCheckByRun`, `TestFallbackChosenByWholeCap`, `TestShapePinsNeverEvicted`, `TestPrepareJobReportsRelease`, `BenchmarkSetIndexAtVertexCap` | The job that prepares an overlay is its reader: it holds the host's geometry while it simplifies, without the store's lock, and keeps its result only if the geometry is still the overlay's. A release `Set` could not report is collected for the `Work` call to return. A prepared form is cached if its vertices times 8.25 fit the whole cap, and otherwise the overlay is marked as drawn from the host's memory. The cache puts need first, as the tile cache does: forms at a bucket some live view draws at are never evicted, and need alone over the cap is told once. **PLAN's estimate for the pass inside `Set` was low by about eight times.** As first written it cost 75 ms at two million vertices, 101 ms with the borrow check. The projection never turns back, so a run's box is found from its extremes and two corners are projected, not sixty-four vertices; and the fingerprint is taken a word at a time. **Now 23 ms, and 25 ms with the check** - about 11 ns a vertex, 1 ms at 100,000. The contract carries the measured figure and says what it means for a host |
| 10.12 (hand-in), 10.25 (the unit warning) Scalar grids | this commit | `TestGridHandIn` (nine mistakes), `TestImplausibleUnitWarned`, `TestGridPrepared` | A grid is a regular longitude and latitude grid, rows from the north, with a type. An overlay is features, or a grid, or an image, and one only. Preparing a grid classifies each value once, on the host's own grid, into one byte a cell; that is where preparing ends, and sampling into cells is the renderer's at draw time. Temperatures all above 60 declared as Celsius are accepted with a warning; values that are no number are no data and say nothing about the unit |
| 10.15 to 10.20, 10.22, 10.23 Images | this commit | `TestImageHeaderFirst` (fourteen refusals, every one from the first 33 bytes), `TestImageBytesCappedBeforeDecode`, `TestTableRules`, `TestExactTableZeroUnmatched`, `TestUnmatchedCountedAndSampled`, `TestBrokenImageIsRefusedByTheJob`, `FuzzImage`, `FuzzTable` | **With the provider's own 256-entry table and no tolerance at all, every one of the fixture image's 240,000 pixels matches**, as PLAN found. Size, depth and the map's image cap are read from the header before anything is decoded; the larger fixture image is refused with both figures, 770,000 and 250,000. The PNG is decoded directly, never through a registry of formats, inside the job. Each colour met is remembered, so an image of few colours costs few searches. A fully transparent pixel is nothing and is not counted; an unmatched one is no data, counted, and at most sixteen distinct colours are kept as samples; a "missing" entry is no data and is matched. The warning carries the count of pixels. Two fuzz targets: 8.8 million images and the table, clean |
| 10.14 Contours | **moved to the renderer** | - | The tiles page has always said contours are found at draw time, from the classes of the cells in view, with work bounded by the cell count; and the renderer may not import this package. The tests keep their names and live with the renderer's overlay tasks |
| 09.7, 09.23, 09.27, and 10.14 here: fields and images in the renderer | this commit | `TestGridSampledAtDrawTime`, `TestWaterMasksField`, `TestWaterNeverMasksImage`, `TestHeaviestInCell`, `TestImageResampledAtDrawTime`, `TestContoursAtBreaks`, `TestFlatDayNoContour`, `TestImageBlockShadesNoColour` | A field colours the cells of land, each sampled at its centre, and stops at the shore; an image colours land and sea alike, because rain falls on the sea; a host can flip either. The background of a cell, bottom to top: the ground, water, a field or an image, an alert's tint. The projection keeps longitude and latitude apart, so a frame finds one longitude a dot column and one latitude a dot row, not one a sample. **D-78's eight samples a cell were not enough by themselves:** where an image is finer than the dots, a sample at a dot's centre can fall either side of a storm's core one pixel wide, and the test lost it. Each dot now takes the heaviest pixel under its whole footprint - which is D-78's own words, "better to overstate than under". A pan redraws the same prepared image in its new place with no work in between. With no ramp to draw in - no colour, or sixteen - a field is lines where its class changes, carrying their values, and nothing at all on a flat day (D-68); an image is block shades, heavier for heavier classes |
| 10.5, the last two | this commit | `TestOldShapeDrawsFromOwnCopy`, `TestBorrowReuseUnderRaceDetector` (two sub-process programs) | Borrowed geometry is the host's to write over again only once the release has been reported. A shape prepared before that goes on drawing as it was: what was prepared is the library's own copy, in the fixed point the renderer draws in, and it survives the geometry it came from. The rule itself is shown rather than asserted about: a program that writes over the memory while a call is still reading it, and one that waits for the word, are each run as a sub-process under the race detector, and the first is required to fail with a data race reported while the second must be clean. A finding in this process would fail the run itself, which is why they are sub-processes; both are skipped when the detector is off |
| 09.8, 09.9 Basemap profiles and the host's layer switches | this commit | `TestProfileGivesUpInOrder`, `TestProfileThinsUnderOverlay` (in the style and in the frame), `TestProfileBySize`, `TestLayerToggle`, `TestLayerToggleInFrame`, `TestProfileChosenForTheFrame`, `TestProfileKeepsAUsersOwnStyle` | A profile is a floor on the roles drawn. The built-in style already ranks its roles by what the basemap gives up first - roads, then rail, parks, region borders, names, rivers, country borders, water, coast (D-83) - so a profile needs no list of its own: it raises the floor. A field or an image over the map raises it to 2 and thins the roads that are left; a map smaller than 80x20 raises it once more and caps the names. The renderer chooses the profile itself, from what it was given and the host's switches, which are applied last and take a layer out whatever the profile would have drawn. A rule of a style the host wrote has no rank and is always drawn; the host's switches still reach it. **The built-in style draws every line one dot thick, so the thinning shows only on a style that asks for more** - the test uses one that asks for three |
| 09.13, 09.14, 09.29, and P-58, P-59a, P-60 Markers, and motion that is safe | this commit | `TestParityP58_MarkerShapes`, `TestParityP59a_MarkerAnimation`, `TestParityP60_MarkerCullAndLabel`, `TestBlinkPhaseFromTime`, `TestFrozenClockDrawsOn`, `TestReduceMotionSteady`, `TestMarkerGlyphIsACell`, `TestMarkerLabelAfterTheMapsNames`, `TestBlinkingMarkerFollowsThePhase` | Upstream's six shapes, at upstream's sizes; a ring and a filled circle are one midpoint rule, so the filled one holds every dot of the ring. A marker is kept while it is within twenty dots of the rectangle, because a big enough one still reaches in, and its label begins four dots to its right, checked after the map's own names. **The blink is the one thing the renderer remembers between frames, and it remembers only the clock:** the phase is worked out from the time the host supplies and never from a count of calls, so drawing twice at one instant draws one frame twice. A clock that does not advance - one that never moved, or one that stopped in the half of the blink where the marker is not drawn - always draws the marker on, which is what NFR-21 asks for, and reduce-motion draws it steadily with nothing ever due |
| 09.12, 09.15 The no-colour strokes and the hatch | this commit | `TestHatchAndLabelNoColour`, `TestDashedLineNoColour`, `TestMarkerOverHatch` | With no colour an alert area is hatched and carries its plain word, and the hatch is closer and in a different stroke for a graver severity, so two areas can be ranked with no colour at all. A line overlay is dashed - five dots on, four off, two thick (D-77) - which an area's outline is not: an outline is closed, and the hatch inside tells it from the basemap. **The hatch is laid last, over the cells nothing else has taken**, which is the answer to specimen 13a's defect: the marker, the names and the basemap's own line work all keep their cells, and the hatch fills what is left |
| 09.6 The compositing order | this commit | `TestCompositingOrder` (the nine layers, every pair) | A cell has one background, one set of dots and one piece of text. Within each the later layer is what shows; across them all three are seen at once, which is why the table asserts the layer underneath is gone only where the two want the same part of the cell. **The text is a contest, not a painting:** the first layer to claim a cell keeps it, so for text the order is the order cells are claimed - the frame's own furniture, then a marker, then an overlay's label, then a place name, then a marker's own label (P-60). Steps 7 and 8 of the render diagram were swapped to say so: a place name never hides a marker |
| 09.16, the rest The stale mark and the footer | this commit | `TestStaleMark`, `TestFooterLineOffByDefault`, `TestFurnitureKeepsItsCells` | Data that is no longer current is marked in a word - `stale`, at the right end of the first row, in its own token - so that it reads with no colour at all. The footer is the host's own line, drawn at the left of the row above the last and only when the host gives one: off by default, as P-57 has it |
| Defect, found by the renderer's fuzz target: a mark with no base | this commit | `TestNoHeadlessMark`, and the seed it was found from, kept | A credit line of one Javanese pangkon - a spacing combining mark - made a row one cell narrower than its characters said. A cluster that begins with a combining mark has no base of its own, so it joins whatever character comes before it wherever it is put: measured alone it is one cell, measured in the row it is none. The cleaner now drops such a cluster, since the mark is not the text's to place; a mark after its own base is part of that cluster and is kept. NFR-8's rule holds again |
| 09.31 Drawing from the host's own memory | this commit | `TestDrawFromRunIndex`, `TestBorrowedSplitAtAntimeridian`, `TestBorrowedDrawnInTheFrame` | An overlay whose simplified form would not fit the shape cache is drawn where it lies: one box test for each run of 64 vertices, and only the runs whose box meets the view are read at all. The bound the constants state is asserted as work and not as time - for a line of 20,000 vertices seen through a small window, 313 box tests and under an eighth of the vertices - and the picture is the same as reading all of it, because the index culls and nothing else. A step over the world's edge is two lines, not one across the map. **The box is the renderer's own type now**, and the store's is an alias of it, so an index is never copied to be drawn |
| 09.30, and P-31 Upstream's line simplification | this commit | `TestParityP31_Simplify`, `TestSimplifyOffByDefault` | Ramer-Douglas-Peucker at upstream's half a dot, worked out with an explicit stack rather than by calling itself, and **off unless the host asks for it**, as upstream leaves it. At half a dot in dot space it is a saving in work and not a change to the picture: the frame is identical either way, and what goes is the points already on the line their neighbours draw |
| WP-09 closed: the gate, and two parity rows put right | this commit | `TestParityP35_PoiGlyph`, and the gate | The gate is green: nine fuzz targets a minute each, the tests with the race detector on and off, the vulnerability scan, five cross-compiles and the emulated amd64 run. Not run here, and not assumed: Linux and Windows runs, real amd64 hardware, the nightly soak, the contract check against a tag that does not exist yet. **Two rows were mapped to render tests that did not exist under their mapped names.** Neither behaviour was untested: P-35's was asserted inside another test and now has its own; P-27 is the style's zoom gate and its test lives in that package, so the mapping's row now says so. The book-keeping was wrong, not the code |
| 12.17, 08.22 here, and the counter of 12.20 The look a host asks for | this commit | `TestLookSettingsChangeTheNextFrame`, `TestGroundPaintedOrDeclared`, `TestPaletteSwapKeepsTiles`, `TestChangedCounterMovesOnlyWhenTheFrameWould`, `TestLookOnAClosedMap`, `TestLabelLanguageChecked`, `TestNoColourFromTheEnvironment` | The contract's Look group: a palette of the host's own with the names that are no token handed back, safe ramps, a painted or a declared ground, a colour-depth hint, reduce-motion, the layer switches and the label language. All take effect at the next frame and **none of them reads a tile again - the one exception is the language**, which is part of a tile's cache key, so changing it makes the view want its tiles again (D-82). The change counter moves when a redraw would differ and stands still when it would not, which is what task 08.22 asks of a palette swap: the counter moves, nothing is pending, and a settle afterwards runs no job. With no hint from the host a non-empty NO_COLOR chooses no colour at all (NFR-15); a host that hints is not overruled. **The palette's own safe-ramps call was renamed `KeepRamps`**, because the public call the contract names is `SafeRamps` and the meter reads one calling the other as recursion |
| 12.26, 12.10, and P-61 Places, markers, and the way out | this commit | `TestSetPlaces`, `TestParityP61_MarkerId`, `TestAddPlace`, `TestPlacesRefused`, `TestBlinkingPlaceFollowsTheClock`, `FuzzEveryReturnedStringIsClean` | The host's own places: a name and a position are the least that has to be written (D-69), and the rest - the marker style, its radius, a character of the host's own, whether it blinks - are defaults a host may set. An id left empty is the position to six decimal places and `RemovePlace` takes every place carrying an id, both as upstream (P-61). A place off the world, a latitude that is not a number, an id that would need cleaning and a marker the library does not draw are each refused with a kind from the closed list; **a name is cleaned rather than refused**, being text and not an identifier. A place that blinks follows the clock the host passes to `Render`, and reduce-motion draws it steadily. **The fuzz target of the way out found a real defect at once:** `SetPalette` handed the host's own unknown token names back exactly as they were written, escapes included. They are cleaned now - and matched as written, because cleaning a name before matching it would turn `water.fill` with an escape in it into the real token by accident |
| 12.5, 12.6, 12.28, and 12.3 in part Overlays and the view, through the public calls | this commit | `TestOverlayStructsRoundTrip`, `TestPresetOneCall`, `TestLargeShapeNeverVanishes`, `TestOverlayMistakes`, `TestOverlayWarningsReachTheHost` | `Set`, `Remove`, `InUse`, `Overlays` and `Warnings` on the map, with the aliases that let a host write `tuimaps.Feature` and never name an internal package. A temperature grid is one call - the preset carries the breaks and the colours, and the host says only which unit its values are in (D-69) - and a radar image is the same shape of call. The map registers a view with the shape cache, adds a prepare job for each overlay the view wants and has not got, and at draw time hands the renderer the prepared shapes, the fields, the images, and the ones read straight from the host's memory. **An overlay too big for the shape cache shows on the very next frame with no `Work` call at all** (12.28, D-92), and every frame during a replace shows the old shape or the new. The view gained `Recentre`, `Zoom`, `ZoomBy`, `PanCells`, `FitWorld` and `Centre`; a move that would leave the view invalid is refused and the view it had is kept. **Four internal calls were renamed** - the store's `Set`, `Remove` and `InUse` to `HandIn`, `Drop` and `Reading`, and the projection's `FitWorld` to `WholeWorld` - because the contract fixes the public names and the meter reads a public call reaching one of the same name as recursion |
| D-114, and 12.20's deadline The two clocks | this commit | `TestRenderTakesTheWallClock`, `TestAnimateDrivesTheMarkerPhase`, `TestNextCall` | **`Render(size, now)` takes the wall clock (D-114).** Markers move on it unless the host takes the animation clock over with `Animate(at)`, which `FollowClock()` gives back. Staleness is always the wall clock's, so a frozen animation clock cannot hide old data: the test freezes the animation and the mark still appears nine hours on. An overlay past its currency - or one whose valid time is far ahead of the clock, which is a bad timestamp - marks the frame with the word `stale`, and fresher data takes the mark away. `NextCall` is the earliest of the next marker phase, an overlay going stale and a failed tile's retry, and is false when nothing at all is due: with nothing animated and nothing to go stale a host may sleep until something happens to it. **The blink counts only when something blinks**, and not at all under reduce-motion. The work queue's own deadline call is `DueAt` now, the public one being `NextCall` |
| 12.7, 12.8, 12.9, and P-57 The same facts as data | this commit | `TestLegendShowsDrawnColours`, `TestCreditsCombined`, `TestScaleExposed`, `TestParityP57_Footer`, `TestFactsOnAClosedMap` | `Legend` is what is on the map, class by class, each labelled in the overlay's own unit and coloured **the way the renderer asks for its colour**, so the legend and the picture can never disagree: colours at truecolor and at 256, and none at sixteen or with no colour at all, because what needs a ramp is drawn in its no-colour form at those depths (D-59). `Credits` is the basemap's and each overlay's, once each and in a fixed order. `Scale` is how far a cell spans and the round distance the bar stands for - **the mark on the frame and the call now share one rule**, `render.RoundBar`, rather than each having its own. `Footer` is upstream's own wording, cut with floor as upstream cuts it, off the frame unless the host turns it on. Every one of them is empty on a closed map |
| 12.19, 12.22, 12.23 The work a host does, and the edge of the library | this commit | `TestWorkThroughThePublicPackage`, `TestOnPendingWakesTheHost`, `TestCallsSafeTogether`, `TestCloseWhileWorking`, `TestPanicRecoveredAtEveryPublicCall`, `TestPanicInACallThatAnswersNothing` | `Work` does one unit on the goroutine that calls it, `OnPending` tells a host rather than making it poll, and `InFlight` is what is inside a call somewhere else. The owner's calls run beside two pumps for forty rounds under the race detector - render, set, remove, places, look, the facts, the deadline - and a map closes at once with pumps inside it, every call afterwards answering the closed kind. **No panic escapes a public call:** one planted in each of seventeen calls that answer an error comes back as the internal kind, and one planted in each of eighteen that answer nothing is stopped and left as a warning; in both the map still draws afterwards. The hook that plants them lives on the map and is set only by the library's own tests. Three more internal names moved aside from the contract's - the work member's `Work`, `OnPending` and `InFlight` are `RunOne`, `WhenPending` and `Flying` |
| 12.18 The tiles a host names | this commit | `TestSourceReachesNothingUntilNamed`, `TestCacheRootAndMaintenance`, `TestCacheUseWithNoCaches` | `Source` is the only call that makes the library reach anything, and until it is made nothing is fetched and no connection is opened (D-65): the test settles a map with a fetcher of its own in place and counts zero calls, then names a source and counts them rising. `Fetcher` replaces the way the library reaches out, `CacheRoot` keeps tiles between runs at a cap of the host's choosing - zero meaning the library's own - and `CacheUse` reports each cache's need, what it holds and its cap. `Purge` and `Verify` are the disk cache's two maintenance calls and are the host's to make: with no cache named they say so rather than pretending to work. **Five more internal names moved aside** from the contract's: the disk's `Purge`, `Verify` and `Use` are `Empty`, `ReadBack` and `Held`, the pipeline's is `Holding`, and the memory cache's is `Bytes` |
| 12.3, 12.11, 12.12, 12.21 The intents, two maps, closing, and the closed lists | this commit | `TestIntents`, `TestFitTo`, `TestTwoInstancesIndependent`, `TestCloseReleasesEverything`, `TestErrorKindsClosed`, `TestEveryErrorCarriesAKind` | `Recentre`, `Zoom`, `ZoomBy`, `PanCells`, `FitWorld` and `FitTo`, each refusing what is not a view of anywhere and leaving the view it had. **Fitting needs no work to have run:** where each overlay is, is recorded when it is handed in (D-76), so a map can frame an alert area the moment it is set. Two maps in one process share nothing the host did not tell them to; closing one leaves the other drawing. A closed map lets everything go, the host's borrowed geometry among it, and every call afterwards says so. **The kinds are the host's to name now:** `tuimaps.Kind`, `KindOf(err)` and the two lists, which a test holds to the contract's own row, word for word - until now a host could not tell one failure from another without reaching inside the library |
| 12.13, 12.15 The examples, and the README | this commit | `Example_worldMap`, `Example_alertArea`, `Example_temperatureGrid`, `Example_places`, `Example_pump`, `TestReadmeQuickStartBuilds`, `TestReadmeSaysWhatIsOwed` | Five runnable examples in a module of their own, each checked by its own output: the world in three calls, an alert area with its credit, a temperature field in one call, the host's own places, and **the pump an interactive host writes** - two goroutines, `OnPending` to wake them, a clean stop on quit. The alert example keeps to zoom 3 and says why: the embedded tiles hold the world to that depth, and closer wants a source. The README carries the quick start, which a test parses out of the file and holds to naming the three calls, and the things a host cannot find out for itself: that the map needs a font with braille, that nothing is fetched until a source is named, who owns the work and the clock, what is sent and stored, the credit the data asks for in an About, and what is not built yet |
| 12.25, and 12.29's rows Options, defaults, and the zooms | this commit | `TestParityP52_ConfigDefaults`, `TestParityP53_SizeFromTerminal`, `TestParityP54_MinZoom`, `TestParityP56_FitWorld` | What a map is before a host has said anything: the world, nothing reached, nothing held, nothing due, no places, no overlays, the basemap's one credit, and the footer off. The library draws the exact rectangle it is given and reserves no rows of its own - the three rows upstream takes are the application's business (L-17 g). **The test found a real disagreement:** the public zoom bounds were written as 0 to 20 while the projection's are -8 to 18, so a map fitted to a wide, short rectangle sat at a zoom its own `Zoom` call would have refused. The public bounds are the projection's now, and upstream's closest is 18 (P-55). Fitting the world centres at longitude 0 and at the Mercator midpoint of 84N to 56S, which is about 45N, not the 15N the test first claimed |
| 12.14, 12.16 The radar example's terms, and the public surface written down | this commit | `Example_radarImage`, `TestPublicSurfaceSnapshot` | The radar example carries the provider's terms and credit in the example itself - the United States' National Weather Service, whose imagery is public domain and asks to be credited - and says plainly that a host using another provider carries that provider's own in its place. The public surface is written down in `07-readiness/public-surface.txt`, 167 lines, and a test parses the package's own files and compares: a name added, removed or renamed fails until the file is updated in the same commit, which is the point (NFR-22). The package gained the two image projections by name while this was written, and the snapshot recorded it |
| 12.24, 12.27, and the rest of 12.11 The recipe a first host builds by, and shared caches | this commit | `TestLocalOverrideRecipe`, `TestSharedCacheOwnership` | No remote exists yet (D-19), so the first host builds against this tree by the recipe the documents give: require the library at the placeholder version, replace that version with the tree on disk, carry the library's own sums. **The test follows the recipe exactly, with the module proxy off**, builds a throw-away host, runs it, and checks it drew a complete frame - so the recipe cannot rot unnoticed, and the test reaches no network. It also checks the host's module graph gained the library and the two text modules and nothing else (D-81). Maps may now share one set of caches: `NewShared` makes it, `SharedCaches` is the option every map that shares it is created with, and `Use` reports what they need, hold and may hold together. Two maps of different views both draw, settle with nothing left, and closing one leaves the other drawing from the set |
| 12.20, the last of it The ramp checker, in the host's hands | this commit | `TestCheckRampIsTheHostsToRun` | The check the library runs on its own ramps is a call a host can make on a palette of its own, in its own tests, before anyone sees it: the four rules by name, and a finding that says which rule, which classes and by how much. Three greys two units apart fail it; the library's own temperature ramp passes it on the dark ground. The colour package's own checker is `Check` now, the contract's call being `CheckRamp` |
| Defect, found by the gate: the pump example taught the wrong thing | this commit | `Example_pump`, run repeatedly and under the race detector | The example called `Settle` beside a running pump and then printed the frame's status. **`Settle` waits for the queue, not for work another goroutine has already taken** (D-86), so when the pump's goroutines had the jobs, the frame after it was still empty - and the example failed about one run in ten, which is exactly how a host reading it would have been misled. The pump now tells the host each time it has done a unit, and the host draws again until the frame is as good as it gets, which is what an interactive host actually does. The README says so too. **This is the value of running the gate on the examples module:** the library was right and the example was wrong, and nothing else would have caught it |
| Defect, found by the gate's fuzzing: an address not written as it parses | this commit | `TestAddressWrittenAsItParses`, and the seed it was found from, kept | A tile address whose scheme was written `Https` parsed as https and passed every check - the scheme, the host, the user name, the fragment, the tokens - because the parser lowercases a scheme. **The address is checked as it is parsed and used as it was written**, so every tile the library then asked for carried the odd spelling. The two must now be the same text. Found by `FuzzTileJSON` in the gate, on a seed of forty-four bytes |
| 11.3, 11.4 Where a place is, relative to an area | this commit | `TestInsideOutsideUnsimplified`, `TestNearestEdgeDistanceAndBearing`, `TestSeamIsNeverAnEdge`, `TestEastwardIsTheShortWay`, `FuzzArea` | The first of the description: inside or outside by the even-odd rule the picture is filled with, from the host's own rings and never from the drawn cells, and the nearest edge with its distance and bearing. **Longitude is circular throughout**, which cost three attempts to get right. A ray east has no end on a circular world - it comes back round and crosses everything an even number of times - so the rule counts a ray north, in a frame centred on the place, which turning the world leaves where it was. The nearest point of an edge is worked out flat and then held against the edge's own two ends, because a very long edge could otherwise answer further away than its own vertex. A vertex that is nowhere is passed over on its own, so one bad vertex does not take its good neighbours with it. Longitude is wrapped rather than refused: 180.04 east is 179.96 west and nothing is in doubt. **What the rule cannot promise is written down**: an edge spanning exactly half the world has no short way round, and a place within a kilometre of the boundary is a knife edge that the last bits of a floating-point number decide |
| 11.5, 11.6, 11.7 Points, lines, fields and images described | this commit | `TestNearestPointAndLine`, `TestNearestAcrossTheSeam`, `TestLineAndAreaEdgeAgree`, `TestFieldValueBandAndRise`, `TestImageClassAndNearestHeavier` | The nearest point and the nearest line, each with its own label, measured by **the same three candidates an area's edge is measured by** - a test holds the two to the same answer for the same geometry, so a host never hears two answers for one shape. A field says the value of the sample the place falls in, the class it is in by the same rule the renderer colours by, and which way it rises; **on a flat day nothing rises, and saying so is the answer** (D-68) rather than a bearing made up out of rounding. A value that is not a number is no data, not a reading of zero. An image says the class here and where the nearest heavier class is, which is what a host says when "light rain" is true and not the whole truth; from no data, anything at all is heavier |
| 11.8, 11.9, 11.10 What every answer says, and how it may be said | this commit | `TestUnitsFollowHost`, `TestSpeakable`, `TestNoDataSaidPlainly`, `TestFieldAnswerCarriesItsUnits`, `TestRoundedAndUnderOneCell` | An answer is data, never a sentence of the library's own (D-52): the place, the overlay, its form in a word, where the place is, how far and which way, the label, the value and its band, and what is true of the overlay itself. **The units are the host's and are always stated** - kilometres or miles, Celsius or Fahrenheit - because an answer that says "twelve" and leaves the rest to the reader is no answer; a field of something the library knows no units for keeps its own numbers and says nothing it does not know. An overlay with nothing to say here says so rather than answering zero, and an image with no data here still says where the rain is. **Nothing handed out is a character a speech engine reads as noise:** no braille, no box drawing, no blocks or shades, no marker glyphs, and the compass is a word in full. Numbers are said to a length a voice can carry, and a distance under one cell is flagged, because there the picture cannot settle the question and the words must (D-67) |
| 11.11, 11.12, 11.16 `Describe`, the public call | this commit | `TestDescribeReadyOrPending`, `TestDescribeEveryForm`, `TestDescribeMemoised`, `TestDescribeNeverInsideRender`, `TestDescribeOnAClosedMap` | Every overlay described against every place, in its own terms: an area the place is in or out of with its nearest edge, the nearest point or line with its label, a field's value and which way it rises, an image's class and where it gets heavier - in the host's units, always stated. **Nothing is ever pending**: the description is worked out from the data the host handed in, not from anything the library must prepare first, so the call answers at once with no `Work` at all. The contract asks that each part be marked, and a part that is ready says so. Asking twice with nothing changed allocates nothing, and a change to the overlays, the places, the units or the staleness works it out again. **Drawing does not describe and describing does not draw**: a test compares what a frame allocates before and after a description, and the two are the same |
| 11.1, 11.2, 11.13 The answer key, and M1b | this commit | `TestKeyModuleRequiresNothing`, `TestKeyOnHandMadeShapes`, `TestKeyNamesDimensionByBearing`, `TestKeyWholeScenario`, `FuzzKey`, and in the library `TestM1bAgreesWithTheIndependentKey` | **The independent half of M1b exists** (D-43, D-67): a tool in a module that requires nothing at all, which a test enforces, so it cannot share a line with the library even by accident. It is written from the definitions rather than from the library's code - the winding number where the library counts crossings, haversine written out, the bearing formula as the textbooks give it, a hundred steps along each edge where the library solves for the nearest point. Its own answers are checked first against shapes worked out on paper. **"Under one cell" names the dimension the bearing points along** (S17-2): a column's width east or west, a row's height north or south, because a cell is twice as tall as it is wide on the ground and saying "cells" without saying which way is saying half of it. The library's description is then held to the key over a scenario of four shapes and three places, and agrees on every one: inside or outside, the distance to within the key's own step, and the compass word |

**Next:** the scenario data for the M1 scenarios that have none yet (11.17), the describe benchmark (11.15), and then WP-13, the app.
