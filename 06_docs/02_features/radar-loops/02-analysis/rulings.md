---
title: "go-tuiMaps v0.2.0 Radar loops — HUM LEAD rulings"
date: 2026-09-22
phase: intake / DISCOVER (RCC)
sev: SEV-0
authority: HUM LEAD
status: "LIVE — every ruling is written here the moment it is made."
---

# v0.2.0 — rulings

Numbered from D-1 for this release. Rulings of v0.1.0 are cited as **v0.1.0 D-n**
(`../go-tuimaps/02-analysis/rulings-discover.md`); watchpost's as **watchpost 0.18.0 D-n**.

| # | Date | Question | HUM LEAD verbatim | Ruling |
|---|---|---|---|---|
| D-0 | 2026-09-22 | This release exists, and ships before watchpost's radar (made in watchpost 0.18.0 D-11) | "A approved - this makes sense to me, and the main mitigator for the parallel is to focus HEAVILY on ensuring our artifacts and durable context medium are *rigorously* kept up to date so our context/memorory/learnings are durable across session compactions or new sessions. We should also whenever possible codify learnings into executable tests, as previous learnings have demonstrated "the rule an agent has to remember is a rule that will eventually be passed over in a new session."" *(quoted in full under D-38)* | Paired release; v0.2.0 carries watchpost's HR-1..HR-5. Recorded here so this repository's record starts with the ruling that created it. |
| D-1 | 2026-09-22 | Branch | "A - we always do work on <work-type>/ branches, then squash-merge to a release/<version> branch when ready to ship" | `feature/radar-loops`, cut from `feature/go-tuimaps` (full history); squash-merged `release/v0.2.0` at SHIP. |
| D-2 | 2026-09-22 | Level, severity, directives | "A approved; FULL DIRECTIVE SET for LEVEL-1/SEV-0" | **LEVEL-1, SEV-0**; FULL GIT; FULL DOCS; FULL REPORTS; FULL DIAGRAMS; FULL RCC; FULL PLAN; FULL TDD; FULL INST. |
| D-3 | 2026-09-22 | The gate failed on this machine: `govulncheck` missing in every module, and `FuzzAgree` stalled (0 execs/sec from 18 s, then "context deadline exceeded") | "B approved (A + deeper dig)" | `govulncheck` installed; `~/go/bin` added to PATH in the shell profile. The stall recorded as **L-6.4** and investigated: 221 cached inputs timed through both decoders (slowest 58 µs); a 1-second watchdog on both decoders over ~9 min and >27 M executions (minimization off for 5 min, on for 2×2 min) never fired; every stall observed began right after a "new interesting" input. **Probable cause: the fuzz engine's minimization stalling all workers — not proven**, because the input being minimized at a stall is never saved. No exemption; the fix is a PLAN ruling. |
| D-4 | 2026-09-22 | How go-tuiMaps commits proceed while L-6.4 (fuzz legs failing at their deadline) is open | "A approved" | **Fix the gate first, as v0.2.0's first work item, test-first:** a failing reproduction (a fuzz leg that must end cleanly when a new input arrives at the deadline), then the change to `scripts/gate`. Gate tooling only — no library code before DISCOVER closes. No exemption is granted meanwhile. |
| D-5 | 2026-09-22 | Problem statement (proposed 4.5/5; options A lock, B edit, C widen to the contract gaps) | "A approved" | **LOCKED:** "A developer embedding go-tuiMaps can show where precipitation is, but not where it is going: the library draws one radar frame, so the person reading the map cannot see a storm's motion, and the host has no way to give the library the frames that would show it." L-3..L-6 are justified as constraints and host requirements, not by the statement. |
| D-6 | 2026-09-22 | Metrics of success (M1 motion seen, M2 loop honesty, M3 bound holds, M4 embed cost with a loop — primary; M5 contract truth, M6 gate trust — secondary) | "A" | All six ADOPTED as proposed, each with the anti-solution it closes. Numeric target for M4 set in PLAN from measurement; M6's run count stated in PLAN. |
| D-7 | 2026-09-22 | Approve the v0.2.0 brief | "Approved" | Brief APPROVED as presented. It is the body of **go-tuimaps#2** ("v. 0.2.0 - Radar Loops", `enhancement`), which cites watchpost#22 (GitHub back-links it). The v0.2.0 PR closes #2. INTAKE CLOSED; DISCOVER opens. |
| D-8 | 2026-09-22 | D-4 needs a failing reproduction, but 44 isolated fuzz runs (trivial target, warm cache, gate-shaped 60 s, cold cache) produced none — while both full gate runs failed | "C approved" | **The gate is the reproduction:** run `scripts/gate` three times in a row, alone, recording the failure rate and any failing leg's output. If it fails, that is the red and the fix must take the same three runs to zero. **If all three are clean**, fall back to fixing on the source evidence (a run-count budget, which creates no deadline) and verifying by M6 — a stated number of consecutive clean full runs — with the departure from test-first recorded. |
| D-9 | 2026-09-22 | Three full gate runs came back clean, so no reproduction exists; fix on the source evidence (A), turn off minimization (B), or leave L-6.4 open (C) | "A approved" | **The fuzz budget becomes a count of runs, not a stretch of time** (`-fuzztime Nx`): the count budget exits through the execution limit and creates no deadline, so the failure cannot occur. **Recorded departure:** this is a fix without a red reproduction, taken on source evidence; M6 (consecutive clean full runs) is its verification. |
| D-10 | 2026-09-22 | A single five-million count made the gate 41 min (slow targets 10 min each, fast ones seconds) against an 18 min baseline: accept it (A), calibrate per target (B), or split the difference (C) | "B approved" | **A per-target count table in `scripts/gate`**, each calibrated so a leg does about the sixty seconds of work the clock used to give it, plus **every leg prints its duration** (the only drift signal a count budget has) and **an uncalibrated target is named and run at a default**, never skipped. First calibration overshot (legs 100–139 s, gate 27 min) because a short sampling run misses each leg's corpus-gathering overhead; rescaled from the legs' own durations. |
| D-11 | 2026-09-23 | watchpost's 0.18.0 DISCOVER ran three adversarial rounds whose accessibility and security reviewers raised five new host requirements against this library (HR-6..HR-10), after this brief was approved at D-7 | "Amend the tuiMaps brief before we reset" | **Brief AMENDED.** L-1.5 (playback control over an overlay's loop — off / slow / normal, the library's control, exposed to the host and through it to the listener, because the host supplies the data and this library turns it into frames), L-7 (a host can write a fetcher), L-8 (an alert pattern independent of colour depth), L-9 (cache retention and a purge call), L-10 (tile-host confinement as contract). **C-8 records where they came from:** a host's reviewers, not this library's own rounds — the requirement set grew by half after approval, and the library's blind spots are where a host looks and it does not. |
| D-12 | 2026-09-23 | Which release resumes first after watchpost's DISCOVER closed: this library's DISCOVER (A), watchpost's PLAN (B), or both in parallel (C) | "A approved - can use watchposts 0.18.0 discover report as reference material" | **v0.2.0 DISCOVER opens now, ahead of watchpost's PLAN**, because its ten host requirements are the contract watchpost's PLAN designs against. **Watchpost's 0.18.0 DISCOVER record is admissible reference material** — its wave-1 measurements, its three red-team rounds, and its rulings — cited as *watchpost 0.18.0 …* and re-verified against this tree where a claim is about this library. |
| D-13 | 2026-09-23 | *(clerical)* The brief amendment and the resume ruling were first written as D-9 and D-10, numbers the gate investigation had already used on 2026-09-22 — assigned from memory of the last number, not read from this log. The duplicate D-9 was committed in `532806b` and cited in issue #2 | *(clerical, under D-11 and D-12)* | **Renumbered D-11 and D-12**, every citation corrected, and issue #2 updated. D-9 and D-10 remain the gate rulings. Recorded as a row, per this log's rule that a correction is never a silent edit. |
| D-14 | 2026-09-23 | Inside an alert area, which is drawn on top of a radar image: the tint (as built, v0.1.0 FR-12: specimen 29a), the radar with the outline and label kept (29b), or a blend of the two (29d–f, 20/35/50 %). HUM LEAD first preferred 29b over 29a, then asked for the blend | "the tint solution looks the best in terms of conveying the information, and would be what most users would expect. This approach is approved, and we can make tweaks to ensure it doesnt overly wash out or cause contrast concerns. The light mode solution is a known thing, and I think we already have some light mode specimens (Group T) that solve this problem - we'll either have to adjuste the precipation color ramps or the alert geometry colors to make this work, but it's a solvable problem." | **The tint blends over the radar**: inside an alert area each radar cell keeps its class colour, shifted toward the alert's tint, and the outline and label stay on top. This **replaces v0.1.0 FR-12's step 4 (the tint hiding the image) for images and fields**. It changes how frames look, not the API, so C-7 is not engaged. **Tuning is PLAN's job, and the checker holds it:** the blend must not wash out, and every tinted class must stay at least 10 (v0.1.0 D-88's measure) from every other class, both inside and outside the area. Measured at 29d–f: on a dark ground only 20 % clears this for all five severities; on a light ground no fixed strength does. **HUM LEAD rules the light ground solvable** by adjusting the radar ramps or the alert colours, as group T's light-ground corrections were (specimen 22f, v0.1.0 D-93). Separately, the **no-colour defect specimen 29 found** (the radar's shades erase outline, hatch, label, marker, scale mark and credit, against FR-18a) is a defect, not part of this ruling, and becomes a v0.2.0 requirement. |
| D-15 | 2026-09-23 | A full gate costs 20 to 40 minutes, and has never changed its result for a commit that touches only Markdown. Keep full rigor for every commit (A), or add a fast docs lane that runs only the checks that read documents (B) | "Fast docs lane approved; commit once the gate's green" (after: "if we keep finding that sweeps are green just for changning .md files, we may just commit those changes ... I agree that anytime product or test code is touched, we should run with full rigor.  I'm not so sure for scenarios of *only* .md files being touched.") | **A fast docs lane, in both repositories.** A commit whose changes are **all** `.md` files runs only the checks that read documents. In go-tuiMaps that is the tests that read Markdown (`readme_test`, `surface_test`, `reference_test`, `kinds_test`, the M1 answer-key tests, `parity_suite_test`). In watchpost it is `lint-identity`, `lint-authoring`, the ledger lint and the Go tests that read `06_docs`. **Any other file in the change, including a specimen `.ans` or `.txt`, a script or a test, means the full gate.** It is not "skip the checks": a watchpost ledger `.md` turned its gate red at HEAD during 0.18.0 DISCOVER. **The lane is to be codified as a script** that decides from the diff and refuses a mixed change, so it doesn't depend on anyone remembering the rule. The commit that follows this ruling carries specimen files and took the full gate. *Implemented (D-38 note): the lane runs every module's tests uncached, broader than the tests listed here, which is the safe direction.* |
| D-16 | 2026-09-23 | The gate for the specimen 29 commit went red on two new fuzz finds in code unchanged since the last green gate. **`FuzzArea`**: `InArea` counts an edge with a vertex off the globe, which `NearestEdge` passes over, so the test's 1 km knife-edge skip missed a place on that edge (unreachable through the public calls, which refuse such vertices). **`FuzzAgree`**: this decoder closes a ring whose last vertex already equals its first by adding the first again; the proven decoder does not. Fix both test-first (A), defer them to BUILD and commit on a red gate (B), or narrow the two properties (C) | "A approved." | **Both fixed now, test-first**, as a stated exception to D-4 (no library code before DISCOVER closes), limited to these two defects. The saved failing inputs (`internal/describe/testdata/fuzz/FuzzArea/69fc082cabf0c831`, `tools/oracle/testdata/fuzz/FuzzAgree/fb1ffe4164f68657`) are the reds and are committed with the fixes. `InArea` passes over an edge with a vertex off the globe, as `NearestEdge` does. The decoder does not repeat a ring's first vertex when the ring is already closed. Then the full gate, then commit. Not re-running a red gate until it passes (the standing rule) is why this had to be ruled on. |
| D-17 | 2026-09-23 | How severity reads without colour. With no colour only the hatch shows severity, and three strokes cannot rank five levels: Extreme and Severe share `╳`, Minor and Unknown share `╱` (`hatch.go:27-36`; wave 1 W1-C). Specimen 29 also found radar shading erasing the hatch, outline and label. A severity word in the label plus a distinct outline dash per level, at every depth (A); the word only (B); the dash only (C); five distinct hatch patterns (D) | "A approved" | **Both, at every colour depth:** the area's label carries its severity as a word (e.g. "Flash Flood Warning · SEVERE"), and each of the five levels has its own outline dash pattern. The hatch stays as a secondary cue. **Specimen owed before PLAN commits to it**, because both risks are untested: a longer label cut short at 69×12, and five dashes that may not be distinguishable at braille resolution. Depends on fixing the specimen 29 defect (radar shades erasing outline and label with no colour), which this release fixes in any case. |
| D-18 | 2026-09-23 | v0.1.0 was tagged and published before its close-out ran. The integration review's measurement, M1 and recommendation sections are blank (`integration-review.md:70-110`), the UAT findings table is empty (`uat-record.md:84`), `release-checklist.md` reads "Not started", no ruling authorised the tag, and v0.1.0 D-123's "nineteen-item triage" was never written. Reconstruct it (A); record it as not recoverable and make the gap impossible to repeat (B); B plus a v0.1.1 patch for the `0.1.0-dev` version string (C) | "B approved - we'll tag our first 'release' as 0.2.0" | **Recorded as not recoverable:** one row in v0.1.0's record says the close-out did not run, with pointers to the evidence that does exist, and no review is written after the fact. **A gate test refuses a release tag while its checklist is unfinished.** The `0.1.0-dev` version string is fixed in v0.2.0, with no patch release. **v0.2.0 is this library's first release:** its REVIEW and close-out cover everything v0.1.0 shipped, because v0.2.0 replaces it. **The `v0.1.0` tag stays** (HUM LEAD: "leave the v0.1.0 tag alone, proceed"). |
| D-19 | 2026-09-23 | Does the MRMS table (L-2) ship, and in what form? Measured (`wave2-measurements.md`): live frames use a small fixed palette (88–101 colours a frame, **111 across 18 frames**, levelling off by the 12th), while the server's legend is a 1,232-colour gradient. With exact matching against a table sampled from the legend, **71–80 % of rain pixels are unmatched** *(sic: the measured figure is 80.0–80.6 %, corrected in `wave2-measurements.md` after red team round 1)*; with default matching, under 0.2 %. About 11 % of rain pixels sit near a class boundary. The heavy-rain oranges and reds are 3–30 from any legend colour, and today reached only about 48 dBZ. A table sampled from the legend (A); a table of the colours MRMS actually uses, each valued from the legend, with default matching (B); IEM only, with MRMS deferred, which would reverse watchpost 0.18.0's "leverage both" ruling (C) | "B approved for now - we can also always "pre-plan" and provide some color affordances for the heavy end of the spectrum for a severe weather day" | **An observed-palette table, labelled approximate**: every colour MRMS has been seen to use, each valued from its position on the legend, matched exact-first with the tolerance fallback kept for colours not yet seen, and those counted and reported. It is **approximate** because the values are derived (L-2.2). **"For now"**: open to revision once more of the palette has been seen. **PLAN pre-plans the heavy end**: colour affordances for the heavy-rain colours not yet observed, so a severe-weather day does not rest on the tolerance fallback, together with a better way than nearest-legend-colour to value the oranges and reds. A test that fails when the fallback carries a meaningful share of pixels is a candidate instrument for PLAN. |
| D-20 | 2026-09-23 | How much memory a radar loop may use, and at what frame size. Measured (`wave2-measurements.md` M-B): 12 frames cost +0.59 MB a map at 298×152 and +2.19 MB at 596×304, on an empty map of 0.72 MB. NFR-3 is 4 MB live. 298×152 is exactly the braille dot grid of a 149×38 map. Nothing today caps the total image memory across a map (`store.go:20` caps vertices only), and the per-image cap cannot be raised by a host. A fixed library limit sized for dot-resolution frames (A); allow 596×304 and raise NFR-3 to about 6 MB (B); a host-settable total image budget per map (C) | "C approved" | **A total image budget per map, settable by the host**, closing the gap wave 1 found. **The default fits NFR-3's 4 MB live**: about 3 MB of images, enough for one 12-frame loop at 596×304 or two at dot resolution *(sic: about five at dot resolution, 0.59 MB each; corrected under D-32)*. A hand-in that would go over the budget is **refused with an error that says so**, never silently trimmed. The contract's guidance steers hosts toward frames at the dot grid of the view they draw (298×152 at 149×38). NFR-3 is stated for the default: a host that raises the budget owns the memory it asks for, and the contract says so. The exact default is PLAN's, from these measurements. |
| D-21 | 2026-09-23 | `a2dh validate` stood at 93.75 % for the DISCOVER exit: P10 had 7 live findings, none from v0.2.0 work (the check compares against `main`, so all of v0.1.0 counts as changed, and v0.1.0's close-out never ran it, D-18). **Six** are `P10-00-BUILD-COVERAGE` on platform files in `cmd/tuimaps`. P10 was run four times, under darwin, linux, windows and freebsd (for the `_other` files): every file was analysed under the OS it is built for, and none had a finding, but no single run covers them all. **One** is `P10-05` invariant density in `tools/atlas/main.go` (1.00 against 2.0). Exempt the six on that evidence and add real checks to the atlas tool, through the full gate (A); exempt all seven (B); waive validate for this exit (C) | "A" | **The six platform files are exempted** in `.a2dh-p10-exemptions.yml`, each entry citing the four-OS evidence. **The atlas tool gains real checks**, test-first: a page template missing a placeholder the tool fills, a diagram with no source, and a section id that is not safe in HTML are refused rather than written into the page. Its doc comment, which says it reads `docs/` as well as `06_docs/`, is corrected. Code, so the full gate runs before commit. Run under D-4's exception class (tooling, not library). |
| D-22 | 2026-09-23 | D-21's atlas checks were added and each is mutation-proven, but P10-05 still reads 0.83 against 2.0 across the tool's six functions: the meter counts only leading, call-free guard clauses, so checks that call `strings.Contains` or a regular expression, or sit inside a loop, are not counted. Exempt `tools/atlas` (A); restructure so each check is a leading guard in its own function, written to the meter (B); exempt and file a follow-up (C) | "A" | **`tools/atlas` exempted for P10-05** in `.a2dh-p10-exemptions.yml`, on the reason the four ratified package exemptions share (v0.1.0 D-96..D-100): the real checks exist and are proven, and the meter does not count their shape. |
| D-23 | 2026-09-23 | Red team round 1, R1-1: v0.2.0 has no current written requirement set. The brief (issue #2's body) is unchanged since D-11. It still asks for the triage D-18 cancelled (L-5.1) and for a purge call that exists (L-9.2), and it carries none of D-14, D-17, D-18, D-19, D-20 or the specimen 29 defect. There is no risk register and no list of owed work. A normative `requirements.md`, with a risk register and an owed-items list, the brief corrected and pointing to it (A); grow the brief itself (B) | "A" | **`01-objectives/requirements.md` is the normative requirement set.** Every requirement traces to its L-, D- or S29- source and names its instrument or says NO INSTRUMENT YET. It holds the risk register and the owed-items list with the phase each is due. **On any conflict, `requirements.md` wins**, and every change to it is a row in this log. The brief's stale rows are corrected and it points to `requirements.md` for the set. **Issue #2's body carries the corrected brief followed by `requirements.md`** (HUM LEAD, same exchange: "we can always update issue #2's body with the consolidated doc"), and is refreshed from them whenever either changes. Round 1's later rulings land in `requirements.md` directly. The brief keeps its L-numbers, and `requirements.md` extends them rather than renumbering. |
| D-24 | 2026-09-23 | Red team round 1, R1-2 (A1, B-4, P-1): storm motion has no form that does not need the animation. The locked problem is that the reader "cannot see a storm's motion", but `Describe` answers for the newest frame only and M1 is scored by sight, so a screen-reader user or anyone with motion off keeps the original problem. The description reports motion from the frames (A); accept the exclusion for v0.2.0 (B); A plus a still "trail" picture (C) | "A approved" | **The description reports observed motion from the loop's own frames.** For each described place: where the heavier rain was at the oldest usable frame and at the newest (distance, direction, time), whether it came closer, moved away or held, and the span of time the loop covers. **Worded as observation, never forecast** ("heavier rain 30 km west at 14:05, 18 km west at 14:55"), because rain cells grow and decay as well as move. **M1 gains a non-visual arm**, scored from the description alone; its grader is PLAN's. Lands as L-1.12 in `requirements.md`, and closes RK-8. |
| D-25 | 2026-09-23 | Red team round 1, R1-4 (A2–A6, A15, A16, P-5): the loop has no accessibility requirements of its own. Adopt seven as proposed (A); adopt with changes (B); adopt only the safety and honesty ones, deferring stepping, the state read and the separate tick signal (C) | "A approved" | **Seven requirements, as proposed.** (1) The shown frame's time, or "gap", is text on the map at every depth, beside the `stale` word. (2) A host can step to the previous, next and newest frame and seek to any frame; the library's own animation clock drives the loop. (3) `ReduceMotion(true)` forces playback off; turning it off restores the host's setting. (4) A state read gives the playback in effect (including "off because reduce motion is on"), the frame index, the count, the frame's time, and whether it is a gap. (5) A frame advance is signalled apart from a data change and does not change the description's cache key, so a screen-reader host is not made to re-announce every tick. (6) Off shows the newest non-gap frame with its age. (7) The library owns a numeric ceiling on frame changes a second; every frame change counts toward WCAG 2.3.1, bounded by v0.1.0 D-56's 2.5 a second; a gap is never drawn as an empty frame that flashes the radar off: the picture holds the last real frame while its time reads "gap 14:10", which keeps L-1.2 (a missing frame is never passed off as a neighbour). Slow and normal speeds are PLAN's, within the ceiling. Lands as L-1.10a–g. |
| D-26 | 2026-09-23 | Red team round 1, R1-5 (A14): the default playback state when a host has not chosen one (WCAG 2.2.2). Off (A); normal (B); slow (C) | "A - default off, but tuimaps needs to have a standard API so hosts can easily hook into playback and looping controls and/or settings." | **Playback defaults to off**: the newest non-gap frame shows with its time until the host chooses (L-1.10f), and the state read reports "off (default)". **Paired with a requirement on the API itself:** go-tuiMaps offers **one standard playback API** that a host can wire straight to its own controls and Settings with little glue: every control of D-25 (play/pause, slow/normal, step, seek, newest), the state read, and a change signal, in one consistent shape across every looped overlay. The API's shape is PLAN's, under the no-code rule (signatures only). Its measure: a host wires loop controls and a Settings row to it without its own playback state. |
| D-27 | 2026-09-23 | Red team round 1, R1-6 (A10, B-6): D-14 rules the light ground solvable, but nothing says what ships if no blend passes there (every strength tried failed, S29-6). Search as D-14 directs, with 29b's order as the named fallback on a light ground (A); 29b on light grounds from the start (B); no fallback, the search must succeed (C) | "A" | **PLAN searches, as D-14 directs:** the light-ground radar ramp, the alert colours, or a per-class tint, the way the ramps were built, held by the checker (L-11.2). **If nothing passes, the light ground falls back to 29b's order**: radar over the tint, the warning carried by its outline, its label and D-17's severity word and dash. The dark ground keeps the blend either way. Accepted cost: a listener who switches theme may see a warning drawn two ways. |
| D-28 | 2026-09-23 | Red team round 1, R1-7 (A8, A9): D-17 and D-14 can each fail silently. A label that does not fit is dropped whole, severity word included, and nobody is told; the dashes must also differ from the line-overlay dash; no second cue at 16 colours; D-14's check measures class distance only, not outline or label contrast, and at truecolor only. Adopt four requirements (A); 1–3, leaving contrast to PLAN (B); 1 and 4 only (C) | "A approved" | **Four requirements.** (1) A label that does not fit falls back to the severity word alone; if that does not fit either, the description still carries it and the frame reports the dropped label to the host. (2) The five outline dashes differ from each other, from the line-overlay dash (5 on, 4 off) and from every basemap stroke. (3) The hatch draws at Colours16 too, which the library already treats as rampless, so severity has a second cue there. (4) The checker holds outlines at 3:1 (WCAG 1.4.11) and labels at 4.5:1 against the blended inside and the plain radar outside, on both grounds, at truecolor and 256 colours, and at 16 colours against its reference table. Accepted cost: over rain, an outline may be forced to black or white and lose its severity hue; D-17's dash carries severity there. |
| D-29 | 2026-09-23 | Red team round 1, R1-8 (A11–A13): description gaps. Proposed: an area answer names its alert and severity ("…: you are inside"); an image answer gives its intensity as words, marked approximate; with no places, a summary of what is in view. Then, raised by the coordinator: D-24's motion description is also place-relative — keep it (A), recast it without a place (B), or defer it (C) | "I'm not sure we need to do this, actucally - I think we should simply describe the alerts being shown, and it's only "you are inside" if the user somehow told us that's their current location - which we actually CANNOT reasonably infer in OBSERVER since 'default loation' does not necessarily mean 'my current location.'  I think we should mark this kind of "smart description" deferred as 'necessity unproven yet'", then "A is fine - when we're talking about discrete places, then we can desribe whether that location is "iside," "outside," or "nearby" etc..." | **The description never says "you."** A host's selected place is not the listener's position, so the description names the place ("Hamlin: inside the Flash Flood Warning"). **Adopted:** the description lists the alerts being shown, each with its name, its severity word (as D-17 puts on the map) and its valid time or stale mark, independent of any place. **For a discrete place the host names**, the description says whether it is inside, outside or nearby, as v0.1.0 already does and watchpost's M1b relies on *(error in this write-up: v0.1.0 has only inside and outside; "nearby" is new, D-43)*. **D-24 stands as ruled** (motion described relative to a named place; *widened by D-42 to views with no named place*). **Deferred, "necessity unproven yet":** intensity in words for image answers, a summary of what is in view, and any new statement relating a place to the weather beyond inside / outside / nearby. |
| D-30 | 2026-09-23 | Red team round 1, R1-9 (InfoSec F1–F9): threats never carried into L-1, L-7, L-9 and L-10. Adopt nine requirements (A); 1–7, leaving guidance and the checklist to REVIEW (B); 1, 2, 5 and 7 only (C) | "A" | **Nine requirements.** (1) Frames are copied at hand-in, never borrowed; decode re-checks the cap and the budget. (2) The budget counts retained PNG bytes as well as pixels; a frame's file size is capped relative to its pixel count; a hard maximum frame count, gaps included; the playback interval is library-owned and floored, never derived from host-supplied times. (3) Every host fetcher is wrapped by `fetch.Checked` (read limit, timeouts); the contract states what a host fetcher takes on (TLS, redirects, private addresses). (4) Setting a fetcher takes effect at once. (5) Purge empties everything — every source, the memory caches, decoded pictures; in-flight writes cannot land after it; maximum age is enforced at `CacheRoot` and in every job, and future-dated files count as expired. (6) `cache-write-failed` is raised, and only removals that succeeded are counted. (7) One allow-list for fetching and TileJSON, covering scheme, host and port under any fetcher; plain http is refused unless the host allows it. (8) Contract guidance: the cache root under the OS user cache directory, owner-only, and purge is not secure erasure. (9) The release checklist records a pinned `govulncheck` at the tag commit. Accepted cost: the largest single addition to v0.2.0's scope. |
| D-31 | 2026-09-23 | Red team round 1, R1-10: defects in the gate. `FUZZ_TIMEOUT` is inert, so a fuzz stall hangs the gate (confirmed in the go1.25.0 source); the docs lane exits 0 with nothing to check; a module `go list` cannot load reads as "empty"; the docs lane loses `git diff`'s exit status; the decoder fix has no unit test (confirmed by reverting it); gate runs leave no trace, so M6 cannot be counted. Fix all six now (A); 1–5 now, the run log in PLAN (B); defer to BUILD (C) | "A - fix the tools now" | **All six fixed now, test-first, under D-4's gate-tooling class**, then the full gate. (1) `FUZZ_TIMEOUT` is deleted; each fuzz leg runs under a real wall-clock limit that fails it as STALLED. (2) The docs lane exits non-zero when there is nothing to check. (3) A module `go list` cannot load fails the gate. (4) The docs lane checks each git command's exit status. (5) `internal/mvt` gets unit cases for an already-closed ring and a one-point ring. (6) The gate appends one line per run (date, commit, uncommitted files, mode, result, duration) to the tracked `06_docs/gate-runs.md`, which M6 counts from. Accepted cost: each run leaves a Markdown change for the next commit to carry. |
| D-32 | 2026-09-23 | Red team round 1, R1-11: the evidence behind D-19 and D-20. Wave 2 states no limits; the two blend results measured different pairs (tinted against plain on the dark ground, tinted against tinted on the light); D-20 under-states its default (3 MB holds about five dot-resolution loops, not two); the programs were throwaway, so nothing re-runs. Limits sections, the blend re-measured on both pair kinds and both grounds, D-20 corrected, the programs and raw output filed beside the record as reference files outside the build (A); the programs as a gated module (B); limits and D-20 only (C) | "A approved" | **Done as proposed.** Each wave 2 measurement gains a Limits section; the blend is re-measured with both kinds of pair on both grounds and specimen 29's findings restated from it; D-20's capacity is corrected in place with a note; the programs and their raw output are filed under `02-analysis/programs/` as reference files, not built by the gate. Accepted cost: reference programs go stale as the API changes, and nothing notices. |
| D-33 | 2026-09-23 | Red team round 1, R1-12 (H-1): the v0.2.0 record exists on one disk. `origin` has only `main` and `release/v0.1.0`; neither feature branch was ever pushed (251 commits), and public issue #2 cites commits nobody else can open. Checked before asking: no machine paths in any tracked file, one author, no AI attribution in any message. Push both feature branches (A); push `feature/radar-loops` only, which carries `feature/go-tuimaps`'s whole history (B); keep them local and record why (C) | "B", then: "- we do our work on <work-type>/  branches (feature/bugfix/etc) - we can push the working branch to the repo - we only use squash-merge release/<version> branches to PR - only release/<version> branches get merged to main - once the release/<version> branch is merged, we clean up old branches, including the <work-type>/ branch. If B supports this, then that's fine - those branches are needed so I can pull things down to a different machine if necessary, but are not meant to be permanent records in the repo (that's the release/ squash-merge commit history)" | **`feature/radar-loops` is pushed to `origin`** and kept current after each commit, **so the work can be pulled down on another machine. It is not a permanent record:** the permanent history is the squash-merged `release/v0.2.0` (D-1). Only `release/<version>` branches are PR'd and merged to `main`, and once that merge lands, the work branches are deleted, `feature/radar-loops` included. `feature/go-tuimaps` stays local; its history is inside `feature/radar-loops`. **Consequence:** a commit hash cited from the work branch (issue #2, this log) stops resolving on GitHub once the branch is deleted, so the record cites rulings and documents rather than work-branch hashes wherever it must outlive the branch. |
| D-34 | 2026-09-23 | Red team round 1, R1-13 (D-3): the README promises a user-agent carrying "a token you set", and no public call sets it (`tiles.go:52` passes empty options). Correct the README now and extend L-4's contract-truth test to the README (A); leave it until L-7.2 makes it true (B); A without extending L-4 (C) | "A" | **The README is corrected now**: the user-agent names the library and its version, and a host's own name is not yet settable. The same pass corrects "`Purge()` empties it" to what `Purge` does today (the current source's tiles; L-9.3 widens it). **L-4 extends to the README**: the check that holds `contract.md` to the code covers the README's promises too. |
| D-35 | 2026-09-23 | Red team round 1, R1-14 (H-10): F-2's "consider before v0.2.0 adds a second radar source" has fired with D-19. In library terms the second source is a second **colour table** the library ships (the library never fetches radar, L-1.4), not a data-source plugin. A narrow, additive seam for provider tables designed in PLAN, the broad restructure (F-2) and root cleanup (F-1) left to the quality pass (A); the full F-2 restructure now (B); no seam (C) | "A" | **PLAN designs one additive way for the library to offer provider colour tables**, with IEM and MRMS as its first two entries, so a third provider is added the same way rather than improvised. F-2's broad restructure and F-1 stay with the quality pass; F-2's row is updated to say its first seam was taken here. |
| D-36 | 2026-09-23 | Red team round 1, R1-15 (B-9, P-4): round 1 grew v0.2.0 considerably. Split, with the host fetcher, cache retention and purge, and hosted CI in a v0.2.1 (A); no split (B); split, keeping L-10 in v0.2.0 (C) | "B no split - it needs to work and be correct" | **No split.** Everything ruled ships in v0.2.0, watchpost's radar waits for all of it, and there is one REVIEW. The measure of v0.2.0 is that it works and is correct, not that it is early. |
| D-37 | 2026-09-23 | Red team round 1, R1-15, the rest (P-1, P-2, B-5, P-6, B-8, P-4): a scope statement beside the locked problem; the "every host benefits" claim; the MRMS heavy end before SHIP; the cost of each frame advance; `retract v0.1.0`; a target date | "Adopt all" | **(1)** The brief gains a scope line naming the release's three parts — loops; the contract repaired; the library's first reviewed release, with the accessibility and security work round 1 added — and the problem statement stays locked. **(2)** "Who benefits" says watchpost, and that other hosts are hypothetical. **(3)** L-2.3's heavy-end colours are required and tested before SHIP; if the palette is still incomplete at SHIP, the release notes say so. **(4)** The render cost of each frame advance, the blend included, is measured against a target PLAN sets. **(5)** ~~v0.2.0's `go.mod` retracts v0.1.0~~ *reversed by D-48: no retraction*. **(6)** No target date, by design (D-36); watchpost's ship-without-radar rule is its fallback. |
| D-38 | 2026-09-23 | Red team round 1, R1-16: the minor batch. Record corrections, D-18's owed v0.1.0 row, small tooling fixes under one full gate, and a README develop section; plus (1) the ratified P10 exemptions live in a git-ignored harness file, and (2) local cleanup: reset local `main`, delete the diverged local `release/v0.1.0`, delete the 14 MB test binary | "Approved to batch, 1, and 2" | **The batch is done as listed.** **(1)** The rulings rows (D-21, D-22) are the durable record of the exemptions, and the four-OS P10 output is filed beside the record as evidence; the harness's ignore convention is unchanged. **(2)** Done after checking that nothing is lost: local `main` fast-forwarded to `origin/main` (`bbc039a`); local `release/v0.1.0` deleted (it was `1cac1ce`, whose tree is identical to the remote's `7c78d2b`; its two other local-only commits, `e68ede5` and `3ed6c96`, have trees that exist as `6a1de40` and `3084bfb` on `feature/radar-loops`); `go-tuimaps.test` deleted. |
| D-39 | 2026-09-23 | Red team round 2, R2-4 (A F1): which frame drives the `stale` word and the description. The shown frame makes `stale` toggle every cycle (a false alarm and a blinking element) and changes the description's cache on every cycle, breaking L-1.10e; the newest frame contradicts L-1.3 as worded. Newest non-gap frame drives both, stepping sight-only for content (A); A plus a per-frame answer for a stepped-to frame (B); the shown frame drives both (C) | "A" | **The newest non-gap frame drives the `stale` word and the description.** Stepping changes the picture and the frame-time text (L-1.10a), not the description. L-1.3 is reworded to match. Accepted cost: a listener who steps back hears the stepped-to frame's time, not its content. A per-frame answer stays with D-29's deferred "smart description". |
| D-40 | 2026-09-23 | Red team round 2: three batches of fixes that need no choice — R2-1 (the gate's remaining ways to report green without checking), R2-3 (the four security rows reworded so they cannot be met while their exploit works, plus S-5..S-10), R2-12 (record corrections and contradictions) | "Approved to the three fix batches." | **All three done**: R2-1 test-first under D-4's gate-tooling class with a full gate; R2-3 and R2-12 as record changes. Each change is listed in the round 2 ledger's disposition column. |
| D-41 | 2026-09-23 | Red team round 2, R2-2 (C F-5, H-4): round 1's "the docs lane judges the tree, not the commit" was dropped from D-31 without a ruling. The lane judges what is staged — exactly what `git commit` records (A); the whole working tree, as a deliberate choice (B); the commit after it is made, as a hook (C) | "A approved" | **The docs lane judges the staged change**: stage the Markdown, run `scripts/gate --docs`, commit. Unstaged and untracked files are ignored, and the lane says how many. Nothing staged fails loudly ("nothing to check"). |
| D-42 | 2026-09-23 | Red team round 2, R2-5 (A F2, B N-2, H D-14): D-24 describes motion only relative to a named place, so a view with no named place gives a listener no motion; and "heavier" means heavier than the place's own rain, which moves between frames, so two frames can compare different storms. A fixed intensity threshold and the no-place gap ratified (A); the fixed threshold plus motion described with no place, relative to the view (B); the threshold only (C) | "B - users may move to a view in the map with no names, and it should continue to work." | **"Heavier" is one intensity threshold, set by the colour table and held for the whole loop.** **Motion is described with or without a named place**: with none, relative to the view (its centre and edges), so a listener who pans or zooms to a view with no names still hears where the heavier rain is and how it moved. This **supersedes D-29's deferral in part**: motion relative to the view is required; D-29's other deferrals (intensity in words, a general view summary, other new place-to-weather statements) stand. Still observation, never forecast, and data rather than sentences (L-1.12). |
| D-43 | 2026-09-23 | Red team round 2, R2-6 (A F4, F5): the description rows cannot be built as written. v0.1.0 has only Inside and Outside — **"nearby" was wrongly attributed to v0.1.0 in the coordinator's write-up of D-29** — area answers carry no alert name and merge every feature of an overlay, L-13.5's list has no place-independent surface, valid time is per overlay, and severity exists only as a colour role. Build it: a host-settable "nearby", per-alert answers, a list of alerts shown, severity and valid time as data (A); A without "nearby" (B); per-alert answers and data only (C) | "A" | **(1) "Nearby" is new in v0.2.0**: within a distance the host sets, with a default (proposed 10 km; PLAN confirms it), per the standing settings principle. **(2) Area answers name each alert** that contains the place or is near it, each with its severity word, instead of one answer merged across an overlay. **(3) A list of the alerts shown**, needing no place: each alert's name, severity and valid time. **(4) Severity and valid time are data on each alert**, not read from its colour role. L-13.5 and L-13.6 are reworded; the D-29 write-up's error is corrected in place with a note. Accepted cost: new API surface. |
| D-44 | 2026-09-23 | Red team round 2, R2-7 (B N-1, Critical): severe weather was never examined — MRMS seen only to about 48 dBZ, one warning over moderate rain, no overlapping warnings over heavy radar; L-2.3's heavy-end test has no oracle, and MRMS keeps about two hours of history. IEM archives radar back to 2011 and archives NWS warnings. An archive specimen of a past outbreak, a register row, a triggered MRMS capture, and a stated fallback if none comes (A); A without the archive specimen (B); block SHIP until a live capture (C) | "A" | **(1) A specimen of a past outbreak from IEM's archive** — overlapping warnings over heavy radar, both sizes, with and without colour — owed at PLAN entry. **(2) A register row** for the MRMS heavy end depending on live weather. **(3) An owed capture with a named trigger:** when a Storm Prediction Center Moderate or High risk, or a tornado watch, is issued over US radar coverage, MRMS frames are captured across the whole two-hour window and the palette extended. **(4) If no such day comes before SHIP**, MRMS ships with its heavy end marked unverified, the library warns whenever a colour takes the fallback, and the release notes say so (with D-37). Accepted cost: in a quiet season, MRMS ships unverified where it matters most. |
| D-45 | 2026-09-23 | Red team round 2, R2-8 (B N-4): nothing requires the warning to stay visible inside the blend — L-11.2 guards only against wash-out, and at 20 %, the only passing strength, the tint is faint. A visibility floor of 5 between each class inside and the same class outside, tuned together with the wash-out floor, outline/label/dash carrying the warning wherever both cannot hold (A); no floor, outline/label/dash carry the boundary (B); a floor of 10 (C) | "A" | **Each rain class inside an alert area differs from the same class outside by at least 5** (v0.1.0 D-88's measure), on each ground where the blend is used. PLAN tunes the blend so this and L-11.2 both hold; **where they cannot, the outline, label and D-17's word and dash carry the warning there**, and the record says where. The 5 is the coordinator's proposal, not a measurement; PLAN measures it. |
| D-46 | 2026-09-23 | Red team round 2, R2-9 (H-7): the gate never runs `gofmt` — `gate_test.go` sat unformatted from `0665ae7` through several green gates — and the coordinator recorded that as OW-8, with a due date, without a ruling, which D-23 forbids. A formatting leg now, test-first, under D-4's gate-tooling class (A); OW-8 ruled for PLAN (B); drop it (C) | "A" | **The gate gains a formatting leg**, test-first: any tracked Go file `gofmt` would change fails the run, in every mode but the docs lane (which carries no Go). OW-8 is closed by it. **Recorded as a process error:** an owed item was self-issued. |
| D-47 | 2026-09-23 | Red team round 2, R2-10 (A F12): D-29 deferred intensity in words, so a listener hears "class 4" where a sighted user reads colour (WCAG 1.1.1). The host can already word it: `Legend()` returns each class's range in words and its unit (`facts.go:17-28`). The library words it (A); M1's non-visual grader decides (B); stay deferred, the contract telling hosts to word intensity from `Legend()`, and the legend carrying "approximate" for a derived table (C) | "C" | **Intensity in words stays deferred** (D-29). **The contract states that a host words an image answer's class from `Legend()`**, and the example shows how. **A legend entry says when its table is approximate** (D-19), so a host can say so. Accepted cost: a host that does not word it leaves a listener with a class number. |
| D-48 | 2026-09-23 | Red team round 2, R2-11 (B N-5): D-37 has v0.2.0 retract v0.1.0, while watchpost's ship-without-radar fallback runs on v0.1.0 (and also loses HR-3 and HR-7). Keep the retraction and state the fallback's cost (A); drop the retraction (B); make watchpost's move a condition of publishing (C) | "B - 0.1.0 currently "works" - no one is currently using it other than watchpost for our integration work - we'll fix forward to 0.2.0 and the blast radius is currently 0 becuase no one is using it.  Let's not overcomplicate things, as we've already gone VERY DEEP into lots of process questions in this project (we're not even coding yet)" | **No retraction.** D-37's item (5) is reversed and L-5.6 is removed. v0.1.0 works, watchpost is its only user, and the project fixes forward to v0.2.0; the blast radius is zero. RK-4 states the fallback's full cost (no radar, no HR-3, no HR-7). **Standing calibration from this ruling: match process depth to the real blast radius; do not over-complicate.** |
| D-49 | 2026-09-23 | The DISCOVER report (`08-reports/discover-report.md`), presented in full for the DISCOVER exit: requirements normative and traced, 11 risks, two red-team rounds dispositioned, `a2dh validate` 100 %, the gate green with its logged tree matching the commit, P10 clean | "approved; GO 4 PLAN" | **DISCOVER CLOSED; PLAN OPENS.** The report is approved as presented. PLAN inherits the report's list: instruments for the 63 NO INSTRUMENT YET rows, the loop as one design, the numbers set from measurement, OW-2, OW-9 and OW-11 at or before PLAN, and the contract rewrite. No code in PLAN: signatures and API shape only. |
| D-50 | 2026-09-23 | *(clerical)* PLAN entry item OW-9 — diagnose why 16 colours drew every rain class as `░` — found **no defect**: re-counted directly, the 16-colour specimen draws `░ ▒ ▓` exactly as the no-colour one does (2,692 / 1,375 / 83 at 149×38, identical). The claim came from the coordinator's round 1 verification, whose shell pipeline miscounted multi-byte glyphs, and it was reported without a cross-check. It had become L-8.4, OW-9, a row of the round 1 verification table and a line of the approved DISCOVER report | *(clerical, under D-49's PLAN)* | **L-8.4 is withdrawn and OW-9 closed**; the verification row and the DISCOVER report are corrected in place with a note. **Lesson recorded:** count multi-byte text with a program that decodes it, never with byte-oriented shell tools, and cross-check a surprising count before it enters the record. The rest of R1-3 stands: at 16 colours, as with no colour, radar erases furniture (L-8.3). |
| D-51 | 2026-09-23 | *(clerical)* PLAN entry item OW-11 done: specimen 30, a past outbreak from IEM's archive (2011-04-27 21:00 UTC, west-central Alabama, 20 warnings over heavy radar), as built and with the 20 % blend. Its findings: the blend shows the radar inside overlapping warnings (S30-1); most warnings go unlabelled in an outbreak (S30-2); the named place's name is drawn at neither size (S30-3); with no colour, radar erases outlines and hatch (S30-4) | *(clerical, under D-44 and D-49)* | **OW-11 closed.** **OW-3 widened by S30-3.** S30-2 is carried to PLAN as a design input for labelling overlapping areas; no requirement changes. |
| D-52 | 2026-09-23 | How PLAN runs: approach documents only where a real design choice exists (the loop's data model and playback API; the fetcher's shape; cache fetch-time and purge; the description additions), the contract rewrite plan, the implementation plan, mermaid diagrams, the numbers set from measurement, one PLAN red-team round, the PLAN report | "Approved; PLAN is to understand what we're building, and providing API and architectural approaches to make that possible. If needed you can also cross reference the A2DH PLAN skillsets to help. but this approach seems fine given our very deep parallel discovery process. I think we also need to do watchpost and tuimaps PLAN and likely should be done "somewhat" in parallel (they need to cross reference each other for ordering in implementation plans and to ensure architecture is correct for both)." | **PLAN as proposed, and in parallel with watchpost 0.18.0's PLAN** (watchpost D-39). The two plans cross-reference: each library task a watchpost task depends on is named in both, and an integration map (`03-architecture-design/integration-map.md`) holds the order and the HR↔L traces. **PLAN is understanding and API/architecture, not code**: signatures and API shape only — the A2DH `writing-plans` rule of "complete code in every step" is set aside as a local preference (and filed upstream as backlog item `writing-plans-drop-complete-code-in-ever`). |
| D-53 | 2026-09-23 | A diagram edit changes the generated atlas page (`06_docs/architecture-atlas.html`), which is not Markdown, so every diagram edit fell out of the docs lane into the full gate; with FULL DIAGRAMS in PLAN it would do so constantly. Found when the coordinator committed approach 1 over a red docs lane (`6291726`, the commit chained with `;` not `&&` — recorded as a process error). The lane accepts the atlas page when it is exactly what the tool generates (A); every diagram edit takes the full gate (B); rebuild the atlas only at phase exits (C) | "A for Docs Lane" | **The docs lane accepts `06_docs/architecture-atlas.html` alongside Markdown**, and only when it is exactly what `tools/atlas` generates from the staged documents — the atlas's drift test, which the lane runs, is the check. Any other non-Markdown file is still refused. Test-first, under D-4's gate-tooling class. |
| D-54 | 2026-09-23 | PLAN approach 1 (`03-architecture-design/approach-1-loop-model.md`): how a host hands in and refreshes a loop. Declarative — `Image` gains `Frames`, a refresh is another `Set`, frames reused by valid time and a hash of their bytes (A); incremental — `AddFrame` and `DropFramesBefore` (B) | "Declarative model approved" | **A, declarative.** `Image.Frames []LoopFrame`; a refresh is another `Set`; each frame keyed by valid time and a hash of its bytes, so only new frames are decoded (L-1.7); validation, copy and budget apply to the whole list at hand-in. The shared playback API and time model in the approach stand as written: `SetPlayback`, `Step`, `Seek`, `ShowNewest`, `Loop` (the state read), `FrameTicks`; the loop driven by the animation clock; `NextCall` gains the next frame change. Accepted cost: each refresh re-copies and re-hashes every frame (about 70–250 KB). |
| D-55 | 2026-09-23 | PLAN approach 2 (`03-architecture-design/approach-2-fetcher.md`): the host fetcher's shape. **A deviation from DISCOVER:** D-40's L-7.3 ("bounds the bytes and the time of every host fetch, whatever the host fetcher does") cannot hold for time, because the library starts no goroutine (v0.1.0, enforced by `RuleGoroutine`) and so cannot abandon a host call that blocks and ignores its context. The host supplies a transport and the library keeps its client (A); a streaming fetch function (B); A or B plus a goroutine per host fetch (C) | "A approved." | **A.** `SetFetchOptions(FetchOptions{Transport, UserAgent, Timeout, AllowHTTP})`, taking effect at once; the library keeps its `http.Client` — request shape, redirect confinement to the one allow-list, headers, the body limit; `CheckedDialer()` offered so a host transport can keep the private-address refusal. **L-7.3 is reworded** to what can be promised: bytes bounded whatever the host code does; a late answer never used; time bounded while the host's code honours its context, which the contract states. |
| D-56 | 2026-09-23 | PLAN approach 3 (`03-architecture-design/approach-3-cache.md`): what records a cached tile's age (L-9.4) — today the file time is last-read, rewritten hourly, so a viewed tile never ages and the disk records when each place was looked at. The file time becomes the fetch time, reads never touch it, eviction oldest-fetched first (A); fetch time in the file name, read-recency kept (B); a journal of fetch times (C). Shared by all: a cache generation so no write lands after Purge, a CacheRoot change or Close; Purge empties everything; failures counted | "A" | **A.** A tile's file time is its fetch time, set once and never touched by a read; maximum age (`MaxAge`, a `CacheRoot` option) and the cap both run on it, the cap evicting oldest-fetched first, tiles in view still protected; a future-dated time counts as expired. **The shared mechanism stands:** the cache generation, `Purge` of everything, replaced roots released, failures counted. Accepted cost: eviction is first-in-first-out rather than least-recently-used. |
| D-57 | 2026-09-23 | PLAN approach 4 (`03-architecture-design/approach-4-description.md`): where the description's new answers live — per-alert answers with name and severity, "nearby", a list of alerts shown, severity and valid time as data, motion relative to a place or the view. Extend `Describe`'s answers (A); a new structured `Report` beside `Describe` (B) | "B - we have no consumers of 0.1.0 right now, so let's just do the better long-term model" | **B.** `Report(places) (Report, error)`, with sections for the alerts shown (no place needed), each named place (each alert's relation — inside, outside, nearby — with name, severity and times; each image's answer), and motion per looped image (relative to each place and to the view). The shared parts stand: `Feature.Severity`, `Valid`, `Expires`; `SetNearby`; `Motion` as data. **The HUM LEAD's reason — no consumers of v0.1.0 yet, so prefer the better long-term model — is recorded as the basis**; whether it also relaxes NFR-1 elsewhere (for example, `Describe`'s future) is asked separately rather than assumed. |
| D-58 | 2026-09-23 | Whether D-57's reason ("no consumers of 0.1.0 right now, so let's just do the better long-term model") relaxes NFR-1 (additive only) for v0.2.0 generally, or is a one-off | "A for this and additive features; Let's be careful about revisting previous decisions, UNLESS there's SIGNIFICANT UPSIDE that fixing it now will provide. I would place higher emphasis on SIMPLIFICATION and BETTER DEVELOPER ERGONOMICS while adhering to our general structure approaches (plugin/discrete domains/etc) It's a nuanced answer. We made a lot of good decisions previously, but now that we're integrating with Watchpost, some of those assumption may be proven false. Now is the cheapest time to fix this with a longer-term vision for performance, maintainability, and quality, but we should be smart and discerning about when to apply that lens." | **NFR-1 is relaxed for v0.2.0, for this and for additive features**: where a new feature's better long-term API means breaking v0.1.0's shape, the break is allowed and listed in the contract's changelog; watchpost is the only consumer. **Revisiting an earlier decision needs SIGNIFICANT UPSIDE**, not merely a cleaner alternative. **Weigh simplification and developer ergonomics highest**, within the structure the projects share (plugins, discrete domains). The lens applies with discernment: where integrating with watchpost proves an earlier assumption false, fixing it now is cheapest; where it doesn't, the earlier decision stands. |
| D-59 | 2026-09-23 | Cross-repo, from watchpost D-41: watchpost draws the map in `Update` and must never show an old frame; one of its four guards needs the frame to carry its own identity | "A - plus any guards or code hardening to ensure we never unintentionally show old frames." (watchpost D-41) | **`Frame` gains the `Changed` and `FrameTicks` values it was drawn at**, so a host can check a stored frame against the library's current counters. Additive. Lands as L-1.16. |
| D-60 | 2026-09-23 | Cross-repo, from watchpost D-42: in specimens 29, 30 and 31 the named place's label is lost — at 69×12 even without radar (S29-3), at both sizes in an outbreak (S30-3), and at both sizes in a partial area (S31-2) | "we should also figure out how to preserve place labels as needed (can be a separate task in plan)" (watchpost D-42) | **The named place's marker and name are preserved** as their own PLAN task: the host's places outrank alert labels, basemap labels and other furniture where they collide, and where the name still cannot fit, a shorter form or the marker alone is drawn and the frame reports it. Lands as L-8.9; OW-3's diagnosis folds into it. |
| D-61 | 2026-09-23 | PLAN numbers, proposed with watchpost's as one batch (watchpost D-43), each from measurement: the image budget's default, "nearby"'s default, the cost of a frame advance, M4's target, and how the blend's strength and visibility floor are settled | "Batch approved" | **(5)** the image budget defaults to **3 MiB** (D-20 confirmed); **(6)** "nearby" defaults to **10 km** (D-43 confirmed); **(7)** a frame advance costs **≤ 15 ms** at 149×38 on the reference machine (27 ms was measured for twelve frames composited at once); **(8) M4:** a 12-frame loop at dot resolution adds **≤ 1 MiB** of heap over an empty map and holds within ±5 % over an hour (measured +0.59 MiB); **(9)** the blend's strength and visibility floor are measured by the checker in BUILD, from 20 % and the floor of 5 (D-45). |
| D-62 | 2026-09-23 | Internal plan check: the plan removed the public `Fetcher` (`tiles.go:13`, `:70`) beside `fetch.Checked`, and no ruling covered it; L-7.1 still asked for an exported request type. Remove both under D-58, list the break, reword L-7.1 (A); keep `Fetcher` and export its request type beside `SetFetchOptions` (B); keep it deprecated, remove later (C) | "A" | **`Fetcher` and `fetch.Checked` are removed** (D-58; listed in the contract's changelog). **L-7.1 reworded**: a host supplies its own transport, user-agent and timeout through `SetFetchOptions` (D-55). The door closed: a non-HTTP tile source needs a later ruling. |
| D-63 | 2026-09-23 | Internal plan check: D-17 made its specimen (OW-2) a condition "before PLAN commits to it", and none exists; the plan marks L3.9–L3.11 PENDING. Draw it now in PLAN, beside the PLAN red team (A); a BUILD spike, PLAN closing with it pending (B); no specimen, the dash table on paper (C) | "A - draw it now to determine if it's feasible or to find out if a different approach is needed. This is fine for plan becasue we're exploring approaches vs. trying to "build the final feature"" | **OW-2 is drawn in PLAN** as a throwaway reference program and a patch against a scratch copy, filed in `02-analysis/programs/`, on the Specimen Bench. **Its purpose is feasibility**: whether a severity word and five dashes can be read at 69×12 and 149×38 over radar, or whether a different approach is needed. L3.9–L3.11 stay PENDING until the HUM LEAD rules on what it shows. |
| D-64 | 2026-09-23 | Specimen 32 (D-63): D-17's word and five dashes are feasible at 149×38, but at 69×12 only two of five labels fit and small areas are too short for a dash to repeat. Keep D-17 with the word primary (A); word plus three dash classes (B); a different approach — a severity letter repeated along the outline (C) | "C first, A as fallback" | **Option C is drawn next** (specimen 33): a severity mark repeated along each alert's outline, the word kept in the label, the outline solid. **If C fails, A** — D-17 as ruled, the word primary, five dashes secondary. |
| D-65 | 2026-09-23 | Specimen 33 (D-64): letters along the outline carry severity at every size, including the three areas whose labels are dropped at 69×12; flaws — a letter beside bottom-row furniture, minor and moderate differing only by case, crowding on very small areas. Adopt C with letters E S M N U (A); C with a digit scale (B); fall back to the dashes (C) | "B - we will already need to have a legend, which we can handle as "picture in picture" modal on the map thorugh a command like <shift+L> - this will give us the abilty to have contextual legends and consolidate space to a single control command ('[ L ] Legend' : with [ L ] being a chip control)"; the mapping confirmed: "1 is fine." | **Severity without colour is a digit repeated along each alert's outline: Extreme 4, Severe 3, Moderate 2, Minor 1, Unknown `?`**, at every colour depth; the word stays in the label; the outline is solid; the hatch stays as a secondary cue in rain-free cells. **Supersedes D-17's per-severity dash** (the word stands). Digits are kept off the furniture rows, and every area keeps at least one. The key lives in the host's legend (watchpost D-44: a contextual picture-in-picture legend, `shift+L`, one `[ L ] Legend` chip); **the library's `Legend()` carries the digit key as data**. |
| D-66 | 2026-09-23 | PLAN red team P-1 (Critical, cross-repo): in v0.1.0 `Changed()` is raised only inside `Render` (`map.go:395-397`); `Work` never raises it, so the contract's "moves whenever a redraw would differ" is false, a host cannot learn that `Work` landed a tile without rendering, and L4.7 cannot hold. Fix the signal in the library and simplify the host around it (A); keep v0.1.0's meaning and correct the text (B); A keeping watchpost's memo key and guard 4 (C). Asked which is "the most foundationally correct — making tuiMaps a better library, and easier for watchpost to integrate", the coordinator answered A, with one refinement | "A with appropriate refinements" | **Each counter has one job.** `Changed()` counts **inputs** — data, look, view, and tiles or images `Work` lands — and **`Render` never raises it; `Work` raises it when it lands something visible**. `FrameTicks()` counts frame advances **as of the last time the map was given** (by `Render` or `Animate`); **`NextCall` is the signal that time has something due** — a frame advance, a blink phase, a retry, a staleness change. `Frame.Changed` and `Frame.FrameTicks` carry the values each frame was drawn at. **A break under D-58**, listed in the contract's changelog; the contract's false sentence is the sixth false claim L1.3 corrects. Watchpost renders on every event it owns and keeps D-41's "never an old frame" as a tested property (watchpost D-45). |
| D-67 | 2026-09-23 | PLAN red team P-4 (cross-repo, watchpost D-48): playback semantics unstated, and playback per overlay id, reset by each refresh. Map-wide, position by valid time (A); per loop, persistent (B); as planned, written down (C) | The HUM LEAD's control flow, recorded verbatim in watchpost D-48, ending: "However this control flow dictates the IDs / position logic / etc - that should be our approach." | **The API follows the listener's control flow.** One playback per map: `SetPlayback(Playback)` sets the rate (off, slow, normal; off disables play) and persists across `Set`. Controls: `Play()` runs from the oldest held frame through "right now" and on through forecast frames; `Stop()` holds the shown frame; `Reset()` returns to "right now" (the newest **observed** frame); `Step(by int)` moves by frames and stops playback. **Position is a valid time, never an index**: a refresh `Set` keeps the same moment, or the nearest one still held; with two loops each shows its frame at or before that moment. The map opens stopped on "right now". **`LoopFrame` gains `Forecast bool`**: forecast frames may carry future valid times (an observed frame may not), are drawn and reported as forecast, and the description's motion stays observed-only (D-42). `LoopState` reports the position, "right now", whether playing, and whether the shown frame is a forecast. The loop holds its last frame 2 s and repeats (watchpost D-43). |
